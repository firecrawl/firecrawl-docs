---
title: 'Agent'
openapi: '/api-reference/v2-openapi.json POST /agent'
---

Use `maxCreditsThreshold` (default `0.8`) with `maxCredits` to stop early and emit a best-effort partial result if the run approaches the credit limit.
