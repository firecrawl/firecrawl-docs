---
title: 'Get Agent Status'
openapi: '/api-reference/v2-openapi.json GET /agent/{jobId}'
---

When an agent run stops due to max credits, the response returns `status: "failed"` with `errorCode: "MAX_CREDITS_EXCEEDED"` and a best-effort `partial` payload.
