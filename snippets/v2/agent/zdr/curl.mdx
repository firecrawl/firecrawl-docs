```bash
curl -X POST "https://api.firecrawl.dev/v2/agent" \
  -H "Authorization: Bearer fc-YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Find the pricing tiers for Firecrawl",
    "zeroDataRetention": true
  }'
```
