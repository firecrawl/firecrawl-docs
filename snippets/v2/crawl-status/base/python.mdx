```python
from firecrawl import Firecrawl
import os

client = Firecrawl(api_key=os.getenv("FIRECRAWL_API_KEY"))
resp = client.start_crawl(url="https://docs.firecrawl.dev", limit=5)
status = client.get_crawl_status(resp.id)
print(status.status, status.completed, status.total)
```


