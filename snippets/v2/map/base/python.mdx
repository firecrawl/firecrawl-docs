```python
from firecrawl import Firecrawl
import os

client = Firecrawl(api_key=os.getenv("FIRECRAWL_API_KEY"))
res = client.map(url="https://firecrawl.dev", limit=50, sitemap="include")
print(res.get("links", [])[:5])
```


