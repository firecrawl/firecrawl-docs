```python
from firecrawl import Firecrawl
import os

client = Firecrawl(api_key=os.getenv("FIRECRAWL_API_KEY"))
resp = client.start_crawl(url="https://docs.firecrawl.dev", limit=10)
ok = client.cancel_crawl(resp.id)
print("cancelled:", ok)
```


