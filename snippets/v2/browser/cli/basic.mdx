```bash CLI
# Launch a cloud browser session
firecrawl browser launch

# Execute Playwright Python code (default) — `page` is pre-configured
firecrawl browser execute 'await page.goto("https://example.com")
print(await page.title())'

# Execute Playwright JavaScript code
firecrawl browser execute --js 'await page.goto("https://example.com"); console.log(await page.title());'

# Execute bash with CDP_URL injected (e.g. agent-browser)
firecrawl browser execute --bash "agent-browser open https://example.com"

# List active sessions
firecrawl browser list

# Close the active session
firecrawl browser close
```
