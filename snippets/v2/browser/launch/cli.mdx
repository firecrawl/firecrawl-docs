```bash CLI
# Launch with live view and custom TTL
firecrawl browser launch --stream --ttl 300 --ttl-inactivity 60

# Launch and save session info to file
firecrawl browser launch -o session.json --pretty
```
