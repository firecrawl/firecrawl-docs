```python
from firecrawl import Firecrawl
import os

client = Firecrawl(api_key=os.getenv("FIRECRAWL_API_KEY"))
job = client.crawl(url="https://docs.firecrawl.dev", limit=5, poll_interval=1, timeout=120)
print(job.status)
```


