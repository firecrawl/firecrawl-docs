```js Node
import Firecrawl from '@mendable/firecrawl-js';

const firecrawl = new Firecrawl({ apiKey: "fc-YOUR-API-KEY" });

// ロケーションを指定して検索（ドイツ）
const results = await firecrawl.search('ウェブスクレイピングツール', {
  limit: 5,
  location: "Germany"
});

// 結果を処理する
console.log(results);
```
