```bash cURL
curl -X POST "https://api.firecrawl.dev/v1/deep-research" \
  -H "Authorization: Bearer <YOUR_API_KEY>" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "量子コンピューティングの最新の動向は何ですか？",
    "maxDepth": 5,
    "timeLimit": 180,
    "maxUrls": 15
  }'
```
