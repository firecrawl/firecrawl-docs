```python Python
from firecrawl import FirecrawlApp

app = FirecrawlApp(api_key="fc-YOUR_API_KEY")

# キャッシュの鮮度が1時間未満（3600000 ms）なら使用します
# 新規スクレイプより最大5倍高速！
scrape_result = app.scrape_url(
    'https://firecrawl.dev', 
    params={
        'formats': ['markdown'],
        'maxAge': 3600000  # 1時間（ミリ秒）
    }
)

print(scrape_result['markdown'])
```
