```js Node
// npm で @mendable/firecrawl-js をインストール

import { FirecrawlApp } from '@mendable/firecrawl-js';

const app = new FirecrawlApp({ apiKey: 'YOUR_API_KEY' });

// リトライロジック付きでスクレイプする関数
async function scrapeWithRetry(url) {
  try {
    // まずはデフォルトのプロキシで試す
    const content = await app.scrapeUrl(url);
    
    // エラーステータスコードかを確認
    const statusCode = content?.metadata?.statusCode;
    if ([401, 403, 500].includes(statusCode)) {
      console.log(`ステータスコード ${statusCode} を受信。ステルスプロキシで再試行します`);
      // ステルスプロキシで再試行
      return await app.scrapeUrl(url, {
        proxy: 'stealth'
      });
    }
    
    return content;
  } catch (error) {
    console.error(`エラー: ${error.message}`);
    // 例外発生時はステルスプロキシで再試行
    try {
      return await app.scrapeUrl(url, {
        proxy: 'stealth'
      });
    } catch (retryError) {
      console.error(`ステルスプロキシでも失敗: ${retryError.message}`);
      throw retryError;
    }
  }
}

// 使用例
const content = await scrapeWithRetry('https://example.com');
console.log(content.markdown);
```
