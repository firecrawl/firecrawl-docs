```js Node
import FirecrawlApp from "firecrawl";

// クライアントを初期化
const firecrawl = new FirecrawlApp({ apiKey: "your_api_key" });

// 非同期ジョブを作成
const job = await firecrawl.asyncGenerateLLMsText("https://example.com", {
  maxUrls: 50,
  showFullText: true
});

// LLMs.txt の生成状況を確認
const status = await firecrawl.checkGenerateLLMsTextStatus(job.jobId);

// 現在の状況を出力
console.log(`ステータス: ${status.status}`);

if (status.status === "completed") {
  console.log("LLMs.txt の内容:", status.data.llmstxt);
  if (status.data.llmsfulltxt) {
    console.log("全文の内容:", status.data.llmsfulltxt);
  }
  console.log(`処理済みのURL数: ${status.data.processedUrls.length}`);
}
```
