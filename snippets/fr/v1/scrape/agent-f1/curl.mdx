```bash curl
curl -X POST https://api.firecrawl.dev/v1/scrape \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <YOUR_API_KEY>' \
  -d '{
    "url": "https://example.com/products?page=1",
    "formats": ["markdown"],
    "agent": {
      "model": "FIRE-1",
      "prompt": "Parcourez les pages de produits en cliquant sur le bouton « Page suivante » jusqu’à ce qu’il soit désactivé. Récupérez chaque page."
    }
  }'
```
