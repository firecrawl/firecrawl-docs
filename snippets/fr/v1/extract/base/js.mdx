```js Node
import FirecrawlApp from "@mendable/firecrawl-js";
import { z } from "zod";

const app = new FirecrawlApp({
  apiKey: "fc-YOUR_API_KEY"
});

// Définir le schéma d’extraction du contenu
const schema = z.object({
  company_mission: z.string(),
  supports_sso: z.boolean(),
  is_open_source: z.boolean(),
  is_in_yc: z.boolean()
});

const scrapeResult = await app.extract([
  'https://docs.firecrawl.dev/*', 
  'https://firecrawl.dev/', 
  'https://www.ycombinator.com/companies/'
], {
  prompt: "Extrait depuis la page la mission de l’entreprise, si elle prend en charge le SSO, si elle est open source et si elle figure dans Y Combinator.",
  schema: schema
});

if (!scrapeResult.success) {
  throw new Error(`Échec de l’extraction : ${scrapeResult.error}`)
}

console.log(scrapeResult.data);
```
