```python Python
from firecrawl import FirecrawlApp

app = FirecrawlApp(api_key="fc-YOUR_API_KEY")

result = app.agent(
    prompt="Extraire les principales fonctionnalités et avantages de Firecrawl",
    model="spark-1-mini"
)

print(result.data)
```
