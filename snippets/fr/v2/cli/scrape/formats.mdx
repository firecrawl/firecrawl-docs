```bash CLI
# Obtenir la sortie HTML
firecrawl https://example.com --html

# Formats multiples (retourne du JSON)
firecrawl https://example.com --format markdown,html,links

# Formats disponibles : markdown, html, rawHtml, links, screenshot, json
```
