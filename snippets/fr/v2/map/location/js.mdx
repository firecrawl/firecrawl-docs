```js Node.js
import { Firecrawl } from 'firecrawl';

const firecrawl = new Firecrawl({ apiKey: "fc-VOTRE-API-KEY" });

const res = await firecrawl.map('https://example.com', {
  location: { country: 'US', languages: ['en'] },
});

console.log(res.metadata);
```
