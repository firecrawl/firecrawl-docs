```js Node
import FirecrawlApp from "@mendable/firecrawl-js";

const app = new FirecrawlApp({
  apiKey: "fc-TU_CLAVE_API"
});

const result = await app.scrape("https://docs.firecrawl.dev/", {
  formats: [{
    type: "json",
    prompt: "Extrae la misión de la empresa de la página."
  }]
});

console.log(result);
```
