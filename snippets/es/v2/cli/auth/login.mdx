```bash CLI
# Inicio de sesión interactivo (abre el navegador o solicita la clave de API)
firecrawl login

# Inicio de sesión con autenticación por navegador
firecrawl login --method browser

# Inicio de sesión directo con clave de API
firecrawl login --api-key fc-YOUR-API-KEY

# O configurar mediante variable de entorno
export FIRECRAWL_API_KEY=fc-YOUR-API-KEY
```
