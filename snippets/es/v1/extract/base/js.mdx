```js Node
import FirecrawlApp from "@mendable/firecrawl-js";
import { z } from "zod";

const app = new FirecrawlApp({
  apiKey: "fc-YOUR_API_KEY"
});

// Define un esquema para extraer el contenido
const schema = z.object({
  company_mission: z.string(),
  supports_sso: z.boolean(),
  is_open_source: z.boolean(),
  is_in_yc: z.boolean()
});

const scrapeResult = await app.extract([
  'https://docs.firecrawl.dev/*', 
  'https://firecrawl.dev/', 
  'https://www.ycombinator.com/companies/'
], {
  prompt: "Extrae desde la página la misión de la empresa, si admite SSO, si es de código abierto y si está en Y Combinator.",
  schema: schema
});

if (!scrapeResult.success) {
  throw new Error(`Error al hacer scraping: ${scrapeResult.error}`)
}

console.log(scrapeResult.data);
```
