```bash curl
curl -X POST https://api.firecrawl.dev/v1/scrape \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer TU_API_KEY' \
  -d '{
    "url": "https://example.com/products?page=1",
    "formats": ["markdown"],
    "agent": {
      "model": "FIRE-1",
      "prompt": "Navega por los listados de productos haciendo clic en el botón «Siguiente página» hasta que esté desactivado. Raspa cada página."
    }
  }'
```
