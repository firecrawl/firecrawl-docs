```bash cURL
# Primeira tentativa com proxy padrão
RESPONSE=$(curl -s -X POST https://api.firecrawl.dev/v2/scrape \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer YOUR_API_KEY' \
    -d '{
      "url": "https://example.com"
    }')

# Extrair o código de status da resposta
STATUS_CODE=$(echo $RESPONSE | jq -r '.data.metadata.statusCode')

# Verifique se o código de status indica que devemos tentar novamente com o modo enhanced
if [[ "$STATUS_CODE" == "401" || "$STATUS_CODE" == "403" || "$STATUS_CODE" == "500" ]]; then
    echo "Código de status $STATUS_CODE recebido; tentando novamente com proxy enhanced"
    
    # Tentar novamente com o proxy enhanced
    curl -X POST https://api.firecrawl.dev/v2/scrape \
        -H 'Content-Type: application/json' \
        -H 'Authorization: Bearer YOUR_API_KEY' \
        -d '{
          "url": "https://example.com",
          "proxy": "enhanced"
        }'
else
    # Exibir a resposta original
    echo $RESPONSE
fi
```
