```python Python
from firecrawl import FirecrawlApp

app = FirecrawlApp(api_key="fc-YOUR_API_KEY")

result = app.agent(
    prompt="Extraia os principais recursos e benefícios do Firecrawl",
    model="spark-1-mini"
)

print(result.data)
```
