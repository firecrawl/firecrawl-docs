```bash CLI
# Obter saída em HTML
firecrawl https://example.com --html

# Múltiplos formatos (retorna JSON)
firecrawl https://example.com --format markdown,html,links

# Formatos disponíveis: markdown, html, rawHtml, links, screenshot, json
```
