```js Node
import FirecrawlApp from '@mendable/firecrawl-js';

// Inicialize o cliente com sua chave de API
const app = new FirecrawlApp({apiKey: "fc-YOUR_API_KEY"});

// Pesquise e extraia conteúdo
app.search("firecrawl web scraping", {
  limit: 3,
  scrapeOptions: {
    formats: ["markdown", "links"]
  }
})
.then(searchResult => {
  // Procese os resultados
  searchResult.data.forEach(result => {
    console.log(`Título: ${result.title}`);
    console.log(`URL: ${result.url}`);
    console.log(`Conteúdo: ${result.markdown?.substring(0, 150)}...`);
    console.log(`Links: ${(result.links || []).slice(0, 3).join(', ')}...`);
  });
}); 
```
