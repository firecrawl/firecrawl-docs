```bash curl
curl -X POST https://api.firecrawl.dev/v1/scrape \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <YOUR_API_KEY>' \
  -d '{
    "url": "https://example.com/products?page=1",
    "formats": ["markdown"],
    "agent": {
      "model": "FIRE-1",
      "prompt": "Navegue pelas listagens de produtos clicando no botão 'Próxima página' até que ele seja desativado. Extraia o conteúdo de cada página."
    }
  }'
```
