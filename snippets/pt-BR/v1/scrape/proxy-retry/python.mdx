```python Python
# pip install firecrawl-py

from firecrawl import FirecrawlApp

app = FirecrawlApp(api_key="YOUR_API_KEY")

# Primeiro, tente com o proxy básico
try:
    content = app.scrape_url("https://example.com")
    
    # Verifique se recebemos um código de status de erro
    status_code = content.get("metadata", {}).get("statusCode")
    if status_code in [401, 403, 500]:
        print(f"Recebemos o código de status {status_code}, tentando novamente com proxy stealth")
        # Tentar novamente com proxy stealth
        content = app.scrape_url("https://example.com", proxy="stealth")
    
    print(content["markdown"])
except Exception as e:
    print(f"Erro: {e}")
    # Tentar novamente com proxy stealth em caso de exceção
    try:
        content = app.scrape_url("https://example.com", proxy="stealth")
        print(content["markdown"])
    except Exception as e:
        print(f"O proxy stealth também falhou: {e}")
```
