```bash CLI
# 获取 HTML 输出
firecrawl https://example.com --html

# 多种 formats(返回 JSON)
firecrawl https://example.com --format markdown,html,links

# 可用 formats: markdown, html, rawHtml, links, screenshot, json
```
