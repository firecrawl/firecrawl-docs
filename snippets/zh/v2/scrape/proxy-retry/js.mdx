```js Node（Node.js）
// 使用 npm 安装 @mendable/firecrawl-js

import { Firecrawl } from '@mendable/firecrawl-js';

const firecrawl = new Firecrawl({ apiKey: 'YOUR_API_KEY' });

// 带重试逻辑的抓取函数
async function scrapeWithRetry(url) {
  try {
    // 先用默认代理尝试
    const content = await firecrawl.scrape(url);
    
    // 检查是否返回错误状态码
    const statusCode = content?.metadata?.statusCode;
    if ([401, 403, 500].includes(statusCode)) {
      console.log(`收到状态码 ${statusCode}，改用 stealth 代理重试`);
      // 使用 stealth 代理重试
      return await firecrawl.scrape(url, {
        proxy: 'stealth'
      });
    }
    
    return content;
  } catch (error) {
    console.error(`错误：${error.message}`);
    // 出现异常时使用 stealth 代理重试
    try {
      return await firecrawl.scrape(url, {
        proxy: 'stealth'
      });
    } catch (retryError) {
      console.error(`使用 stealth 代理仍然失败：${retryError.message}`);
      throw retryError;
    }
  }
}

// 使用示例
const content = await scrapeWithRetry('https://example.com');
console.log(content.markdown);
```
