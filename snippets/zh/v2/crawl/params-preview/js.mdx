```js Node
import Firecrawl from '@mendable/firecrawl-js';

const firecrawl = new Firecrawl({ apiKey: "fc-你的API密钥" });

const params = await firecrawl.crawlParamsPreview('https://docs.firecrawl.dev', '提取文档与博客');
console.log(params);
```
