```python Python
from firecrawl import FirecrawlApp

app = FirecrawlApp(api_key="fc-YOUR_API_KEY")

result = app.agent(
    prompt="提取 Firecrawl 的主要功能和优势",
    model="spark-1-mini"
)

print(result.data)
```
