```javascript JavaScript
import FirecrawlApp from '@mendable/firecrawl-js';

const app = new FirecrawlApp({apiKey: "fc-YOUR_API_KEY"});

// 使用缓存抓取进行爬取——对最近抓取过的页面速度提升 500%
const crawlResult = await app.crawlUrl('https://firecrawl.dev', {
  limit: 100,
  scrapeOptions: {
    formats: ['markdown'],
    maxAge: 3600000 // 若缓存未超过 1 小时则直接使用
  }
});

crawlResult.data.forEach(page => {
  console.log(`URL: ${page.metadata.sourceURL}`);
  console.log(`内容: ${page.markdown.substring(0, 200)}...`);
});
```
