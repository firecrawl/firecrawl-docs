```python Python
# 使用 pip 安装 firecrawl-py

from firecrawl import FirecrawlApp

app = FirecrawlApp(api_key="YOUR_API_KEY")

# 针对具备高级反爬/反机器人机制的网站使用隐身代理
content = app.scrape_url("https://example.com", proxy="stealth")

print(content["markdown"])
```
