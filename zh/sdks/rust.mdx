---
title: 'Rust'
description: 'Firecrawl Rust SDK 是一个库，帮助你轻松进行网站抓取与爬取，并以可直接用于大型语言模型（LLMs）的格式输出数据。'
icon: 'rust'
og:title: "Rust SDK | Firecrawl"
og:description: "Firecrawl Rust SDK 是一个库，帮助你轻松进行网站抓取与爬取，并以可直接用于大型语言模型（LLMs）的格式输出数据。"
---

import InstallationRust from '/snippets/zh/v1/installation/rust.mdx'
import ScrapeAndCrawlRustExample from '/snippets/zh/v1/scrape-and-crawl/rust.mdx'
import ScrapeRustShort from '/snippets/zh/v1/scrape/short/rust.mdx'
import CrawlRustShort from '/snippets/zh/v1/crawl/short/rust.mdx'
import CrawlAsyncRustShort from '/snippets/zh/v1/crawl-async/short/rust.mdx'
import MapRustShort from '/snippets/zh/v1/map/short/rust.mdx'
import LLMExtractRust from '/snippets/zh/v1/llm-extract/base/rust.mdx'

<Warning>
  此 SDK 目前使用 Firecrawl API 的 **v1** 版本，非最新（已提供 v2）。部分功能和改进可能仅在 v2 中可用。
</Warning>

<div id="installation">
  ## 安装
</div>

要安装 Firecrawl 的 Rust SDK，请在你的 `Cargo.toml` 中添加以下内容：

<InstallationRust />

<div id="usage">
  ## 使用
</div>

首先，你需要在 [firecrawl.dev](https://firecrawl.dev) 获取一个 API 密钥。接着，初始化 `FirecrawlApp`。此后即可调用 `FirecrawlApp::scrape_url` 等函数来使用我们的 API。

下面是在 Rust 中使用该 SDK 的示例：

<ScrapeAndCrawlRustExample />

<div id="scraping-a-url">
  ### 抓取单个 URL
</div>

要抓取单个 URL，请使用 `scrape_url` 方法。该方法以 URL 为参数，返回抓取结果，类型为 `Document`。

<ScrapeRustShort />

<div id="scraping-with-extract">
  ### 使用 Extract 进行提取
</div>

借助 Extract，你可以轻松从任意 URL 提取结构化数据。你需要使用 `serde_json::json!` 宏，以 JSON Schema 格式指定你的 schema。

<LLMExtractRust />

<div id="crawling-a-website">
  ### 爬取网站
</div>

要爬取网站，请使用 `crawl_url` 方法。该方法会等待爬取过程完成；具体耗时取决于起始 URL 和所选参数，可能会较长。

<CrawlRustShort />

<div id="crawling-asynchronously">
  #### 异步爬取
</div>

若要在不等待结果的情况下进行爬取，请使用 `crawl_url_async` 方法。它接受相同的参数，但会返回一个 `CrawlAsyncRespone` 结构体，其中包含本次爬取的 ID。你可以使用该 ID 调用 `check_crawl_status` 方法随时查询状态。请注意，已完成的爬取将在 24 小时后被删除。

<CrawlAsyncRustShort />

<div id="map-a-url">
  ### 映射 URL
</div>

从起始 URL 获取其所有关联链接的映射。

<MapRustShort />

<div id="error-handling">
  ## 错误处理
</div>

SDK 会处理由 Firecrawl API 以及其依赖返回的错误，并将它们统一为 `FirecrawlError` 枚举，并实现了 `Error`、`Debug` 和 `Display` 接口。我们的所有方法都返回 `Result<T, FirecrawlError>`。