---
title: "Node.js 权威来源"
description: "适用于使用 search、scrape 和 interact 等关键端点的代理的 Firecrawl Node.js 权威来源。"
---

面向代理的 Firecrawl Node.js 权威来源。根据 SDK 源代码和 v2 OpenAPI 规范生成。

<div id="install">
  ## 安装
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="authenticate">
  ## 身份验证
</div>

```ts
import Firecrawl from "@mendable/firecrawl-js";

const client = new Firecrawl({
  apiKey: process.env.FIRECRAWL_API_KEY,
  // apiUrl: "https://api.firecrawl.dev" // 可选，用于自托管实例
});
```

<div id="when-to-use-what">
  ## 何时使用哪一种
</div>

* `search`：当你从查询出发、需要先发现目标时使用。
* `scrape`：当你已经有 URL，想获取页面内容时使用。
* `interact`：当页面需要点击、填写表单或抓取后的浏览器 actions 时使用。

<div id="search">
  ## Search
</div>

### 使用场景

使用 search 根据查询发现相关页面，然后选择要抓取或交互的 URL。你可以使用 `site:` 将结果限定在某个站点范围内，例如 `site:docs.firecrawl.dev crawl webhooks`。

<div id="preferred-sdk-method">
  ### 推荐的 SDK 方法
</div>

`client.search(query, options?)`

<div id="simple-example">
  ### 简单示例
</div>

```ts
const results = await client.search("site:docs.firecrawl.dev webhook retries");
```

<div id="complex-example">
  ### 复杂示例
</div>

```ts
const results = await client.search("site:docs.firecrawl.dev crawl webhooks", {
  sources: ["web", "news"],
  categories: ["research"],
  limit: 10,
  tbs: "qdr:m",
  location: "San Francisco,California,United States",
  ignoreInvalidURLs: true,
  timeout: 60000,
  scrapeOptions: {
    formats: [
      "markdown",
      "links",
      { type: "json", prompt: "Extract title and key endpoints." }
    ],
    onlyMainContent: true,
    includeTags: ["main", "article"],
    excludeTags: ["nav", "footer"],
    waitFor: 1000
  }
});
```

<div id="parameters">
  ### 参数
</div>

* `query`
  * 类型: string
  * 使用场景：需要提供搜索查询时。
  * 说明：使用 `site:example.com` 可将结果限制在某个域名下。

* `options.sources`
  * 类型: 来源名称数组或带类型的来源对象数组
  * 使用场景：你想控制搜索哪些来源。
  * 已确认的值：
    * `"web"`: 网页索引结果
    * `"news"`: 新闻结果
    * `"images"`: 图片结果
    * `{ type: "web" | "news" | "images" }`: 带类型的来源对象形式

* `options.categories`
  * 类型: 类别名称数组或带类型的类别对象数组
  * 使用场景：你想按类别筛选结果。
  * 已确认的值：
    * `"github"`: 侧重 GitHub 的结果
    * `"research"`: 研究和学术结果
    * `"pdf"`: 侧重 PDF 的结果
    * `{ type: "github" | "research" | "pdf" }`: 带类型的类别对象形式

* `options.limit`
  * 类型: number
  * 使用场景：你想限制结果数量。

* `options.tbs`
  * 类型: string
  * 使用场景：你需要按时间筛选时 (例如 `qdr:d`, `qdr:w`, `sbd:1,qdr:m`) 。

* `options.location`
  * 类型: string
  * 使用场景：你想获取本地化结果。

* `options.ignoreInvalidURLs`
  * 类型: boolean
  * 使用场景：你想忽略其他端点无法抓取的 URL。

* `options.timeout`
  * 类型: number
  * 使用场景：你需要以毫秒为单位设置请求超时时间。

* `options.scrapeOptions`
  * 类型: ScrapeOptions
  * 使用场景：你想抓取每条搜索结果 (字段请参见 Scrape 参数) 。

<div id="scrape">
  ## 抓取
</div>

### 为什么使用

当你已经有一个 URL，并且希望获取一种或多种 formats 的结构化内容时，请使用 `scrape`。

### 首选 SDK 方法

`client.scrape(url, options?)`

<div id="simple-example">
  ### 简单示例
</div>

```ts
const doc = await client.scrape("https://docs.firecrawl.dev", {
  formats: ["markdown"]
});
```

<div id="complex-example">
  ### 复杂示例
</div>

```ts
const doc = await client.scrape("https://example.com/pricing", {
  formats: [
    "markdown",
    "links",
    { type: "json", prompt: "Extract plan names and prices." },
    { type: "screenshot", fullPage: true, quality: 80, viewport: { width: 1280, height: 720 } },
    { type: "changeTracking", modes: ["git-diff"], tag: "pricing" },
    { type: "attributes", selectors: [{ selector: "a", attribute: "href" }] }
  ],
  headers: {
    "User-Agent": "FirecrawlDocsBot/1.0"
  },
  onlyMainContent: true,
  waitFor: 1000,
  parsers: [{ type: "pdf", mode: "auto", maxPages: 5 }],
  actions: [
    { type: "click", selector: "#accept" },
    { type: "wait", milliseconds: 750 },
    { type: "scrape" }
  ],
  location: { country: "US", languages: ["en-US"] },
  removeBase64Images: true,
  fastMode: true,
  blockAds: true,
  proxy: "auto",
  maxAge: 86400000,
  minAge: 1,
  storeInCache: true,
  profile: { name: "docs-session", saveChanges: true }
});
```

<div id="parameters">
  ### 参数
</div>

* `url`
  * 类型：string
  * 适用场景：当你想抓取特定页面时。

* `options.formats`
  * Type: 格式字符串或格式对象数组
  * 使用场景：需要多种输出格式时。
  * 已确认的格式字符串：
    * `"markdown"`: markdown 内容
    * `"html"`: 清理后的 HTML
    * `"rawHtml"`: 原始 HTML
    * `"links"`: 页面链接
    * `"images"`: 图片 URL
    * `"screenshot"`: screenshot 输出
    * `"summary"`: 摘要输出
    * `"changeTracking"`: 变更追踪输出
    * `"json"`: JSON 提取
    * `"attributes"`: 属性提取
    * `"branding"`: 品牌资料输出
    * `"audio"`: 音频提取
    * `"query"`: 问答输出
  * 格式对象字段：
    * `type`: 上述格式字符串之一
    * `prompt`: `type: "query"` 时为必填；`type: "json"` 时除非提供了 `schema`，否则为必填
    * `schema`: 用于 `type: "json"` 或变更追踪 JSON 模式的 JSON schema (或 Zod schema)
    * `modes`: `type: "changeTracking"` 时，为由 `"git-diff"` 或 `"json"` 组成的数组
    * `tag`: `type: "changeTracking"` 时使用的变更追踪标签
    * `fullPage`, `quality`, `viewport`: `type: "screenshot"` 的 screenshot 选项
    * `selectors`: `type: "attributes"` 时为 `{ selector, attribute }` 数组

* `options.headers`
  * 类型：键和值均为字符串的记录
  * 使用场景：需要自定义请求头时。

* `options.includeTags`
  * 类型：字符串数组
  * 适用场景：当你只想包含特定的 HTML 标签时。

* `options.excludeTags`
  * 类型：字符串数组
  * 适用场景：当你想排除特定的 HTML 标签时。

* `options.onlyMainContent`
  * 类型：布尔值
  * 使用场景：当你想去除导航栏、页脚及其他模板化内容时。

* `options.timeout`
  * 类型：number
  * 适用场景：需要以毫秒为单位的超时时间时。

* `options.waitFor`
  * 类型：number
  * 使用场景：需要等待页面渲染时使用 (毫秒) 。

* `options.mobile`
  * 类型：布尔值
  * 适用场景：当你需要移动端视口时。

* `options.parsers`
  * 类型：解析器名称数组或解析器对象数组
  * 适用场景：当你需要控制文件解析时 (例如 PDF 解析) 。
  * 已确认的值：
    * `"pdf"`：启用 PDF 解析
    * `{ type: "pdf", mode?: "fast" | "auto" | "ocr", maxPages?: number }`：PDF 解析器选项

* `options.actions`
  * 类型：action 对象数组
  * 使用场景：需要使用轻量级预抓取 actions 时。
  * 已确认的 action 类型：
    * `wait`：必填 `milliseconds` 或 `selector`
    * `screenshot`：`fullPage`、`quality`、`viewport` 为可选
    * `click`：必填 `selector`
    * `write`：必填 `text` (请先点击输入框使其获得焦点)
    * `press`：必填 `key`
    * `scroll`：必填 `direction` (`up` 或 `down`) ，`selector` 可选
    * `scrape`：无额外字段
    * `executeJavascript`：必填 `script`
    * `pdf`：`format` (A0、A1、A2、A3、A4、A5、A6、Letter、Legal、Tabloid、Ledger) 、`landscape`、`scale` 可选

* `options.location`
  * 类型：包含 `country` 和 `languages` 的对象
  * 使用场景：当你需要按地理位置或语言进行抓取时。

* `options.skipTlsVerification`
  * 类型：布尔值
  * 使用场景：需要跳过 TLS 验证时。

* `options.removeBase64Images`
  * 类型：布尔值
  * 使用场景：当你希望从 Markdown 输出中去除 base64 图片时。

* `options.fastMode`
  * 类型：boolean
  * 适用场景：你希望以较低保真度换取更快的抓取速度。

* `options.blockAds`
  * 类型：布尔值
  * 使用场景：当你想启用广告和 Cookie 弹窗封禁时。

* `options.proxy`
  * 类型：string
  * 适用场景：需要控制代理时。
  * 可选值：
    * `"basic"`
    * `"stealth"`
    * `"enhanced"`
    * `"auto"`
    * 自定义代理 URL 字符串

* `options.maxAge`
  * 类型：number
  * 使用场景：当你想获取最长缓存时间不超过指定时长 (毫秒) 的数据时。

* `options.minAge`
  * 类型：number
  * 使用时机：当你只希望返回缓存数据，且其缓存时长至少达到该值 (毫秒) 时使用。

* `options.storeInCache`
  * 类型：布尔值
  * 适用场景：当你希望 Firecrawl 缓存结果时。

* `options.profile`
  * 类型：包含 `name` 和可选 `saveChanges` 的对象
  * 适用场景：当你希望在多次抓取和交互过程中共享同一个持久化浏览器配置文件时。

<div id="interact">
  ## 交互
</div>

<div id="why-use-it">
  ### 为什么使用它
</div>

当页面在开始抓取后需要执行浏览器 actions 或运行代码时，请使用 interact。

### 首选 SDK 方法

`client.interact(jobId, args)`

<div id="simple-example">
  ### 简单示例
</div>

```ts
const doc = await client.scrape("https://example.com", { formats: ["markdown"] });
const jobId = doc.metadata?.scrapeId;
if (!jobId) throw new Error("scrape 响应中缺少 scrapeId");

const result = await client.interact(jobId, {
  prompt: "点击定价选项卡并总结相关方案。"
});
```

<div id="complex-example">
  ### 复杂示例
</div>

```ts
const result = await client.interact("<scrapeJobId>", {
  code: "console.log(await page.title());",
  language: "node",
  timeout: 60
});
```

<div id="parameters">
  ### 参数
</div>

* `jobId`
  * 类型：string
  * 适用场景：你已从 `document.metadata.scrapeId` 获取抓取任务 ID。

* `args.code`
  * 类型：string
  * 适用场景：你想在浏览器会话中运行代码。

* `args.prompt`
  * 类型：string
  * 适用场景：你希望浏览器代理按自然语言指令执行。

* `args.language`
  * 类型：string
  * 适用场景：你需要指定特定的运行时。
  * 可用值：`"python"`、`"node"`、`"bash"`

* `args.timeout`
  * 类型：number
  * 适用场景：你需要设置以秒为单位的执行超时时间。

<div id="notes">
  ## 说明
</div>

* 已弃用的别名：`scrapeExecute`、`stopInteractiveBrowser` 和 `deleteScrapeBrowser` 对应 `interact` 和 `stopInteraction`。
* 默认导出的 `Firecrawl` 是 v2 客户端；v1 仍可通过 `client.v1` 使用。
* 传递给 `formats` (用于 `json` 或 `changeTracking`) 的 Zod schema 会被 SDK 转换为 JSON Schema。

<div id="source-of-truth">
  ## 权威来源
</div>

* `firecrawl/apps/js-sdk/firecrawl/package.json`
* `firecrawl/apps/js-sdk/firecrawl/src/index.ts`
* `firecrawl/apps/js-sdk/firecrawl/src/v2/client.ts`
* `firecrawl/apps/js-sdk/firecrawl/src/v2/types.ts`
* `firecrawl/apps/js-sdk/firecrawl/src/v2/utils/validation.ts`
* `firecrawl/apps/js-sdk/firecrawl/src/v2/methods/search.ts`
* `firecrawl/apps/js-sdk/firecrawl/src/v2/methods/scrape.ts`
* `firecrawl-docs/api-reference/v2-openapi.json`