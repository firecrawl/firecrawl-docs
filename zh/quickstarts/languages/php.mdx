---
title: "PHP 快速入门"
description: "通过 PHP 直接调用 Firecrawl API。"
---

<div id="overview">
  ## 概述
</div>

使用 PHP 的 cURL 扩展向 Firecrawl 发送抓取请求。

<div id="prerequisites">
  ## 前提条件
</div>

* 一个 Firecrawl API 密钥
* 启用了 cURL 的 PHP 8.1+

<div id="install">
  ## 安装
</div>

无需额外软件包；本示例使用内置的 cURL 扩展。

<div id="configure-your-api-key">
  ## 配置 API 密钥
</div>

将 API 密钥存储在服务器的环境变量中。

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## 发起第一个请求
</div>

```php
<?php

$apiKey = getenv('FIRECRAWL_API_KEY');
$payload = json_encode([
    'url' => 'https://example.com',
    'formats' => ['markdown'],
]);

$ch = curl_init('https://api.firecrawl.dev/v2/scrape');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . $apiKey,
        'Content-Type: application/json',
    ],
    CURLOPT_POSTFIELDS => $payload,
]);

$response = curl_exec($ch);
curl_close($ch);

echo $response;
```

<div id="complete-example">
  ## 完整示例
</div>

```php
<?php

$apiKey = getenv('FIRECRAWL_API_KEY');
$payload = json_encode([
    'url' => 'https://example.com',
    'formats' => ['markdown', 'html'],
]);

$ch = curl_init('https://api.firecrawl.dev/v2/scrape');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . $apiKey,
        'Content-Type: application/json',
    ],
    CURLOPT_POSTFIELDS => $payload,
]);

$response = curl_exec($ch);
if ($response === false) {
    throw new RuntimeException(curl_error($ch));
}
curl_close($ch);

echo $response;
```

<div id="next-steps">
  ## 后续步骤
</div>

* [抓取端点](/zh/api-reference/endpoint/scrape)