---
title: "Rust 快速入门"
description: "使用 Firecrawl Rust SDK 抓取首个 URL。"
---

<div id="overview">
  ## 概述
</div>

使用 Firecrawl Rust SDK 在异步 Rust 代码中调用 v2 API。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl API 密钥
* 包含 tokio 的 Rust 工具链

<div id="install">
  ## 安装
</div>

```bash
cargo add firecrawl tokio --features tokio/full
```

<div id="configure-your-api-key">
  ## 配置 API 密钥
</div>

通过环境变量设置 API 密钥。

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## 发起第一个请求
</div>

```rust
use firecrawl::FirecrawlApp;

#[tokio::main]
async fn main() {
    let api_key = std::env::var("FIRECRAWL_API_KEY").expect("Missing API key");
    let app = FirecrawlApp::new(api_key).expect("Failed to init Firecrawl");

    let doc = app
        .scrape_url("https://example.com", None)
        .await
        .expect("Scrape failed");

    println!("{}", doc.markdown.unwrap_or_default());
}
```

<div id="complete-example">
  ## 完整示例
</div>

```rust
use firecrawl::FirecrawlApp;

#[tokio::main]
async fn main() {
    let api_key = std::env::var("FIRECRAWL_API_KEY").expect("Missing API key");
    let app = FirecrawlApp::new(api_key).expect("Failed to init Firecrawl");

    let doc = app
        .scrape_url("https://example.com", None)
        .await
        .expect("Scrape failed");

    if let Some(metadata) = doc.metadata {
        println!("Title: {}", metadata.title.unwrap_or_default());
    }
    println!("{}", doc.markdown.unwrap_or_default());
}
```

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 接口](/zh/api-reference/endpoint/scrape)
* [Rust SDK](/zh/sdks/rust)