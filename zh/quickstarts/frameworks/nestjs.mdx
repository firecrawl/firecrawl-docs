---
title: "NestJS 快速入门"
description: "在 NestJS 控制器中调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

将 Firecrawl 接入 NestJS 服务，并通过控制器对外暴露。

<div id="prerequisites">
  ## 前置条件
</div>

* Firecrawl API 密钥
* NestJS 应用
* Node.js 18+

<div id="install">
  ## 安装
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-environment">
  ## 配置环境
</div>

将 API 密钥设置为仅服务器端可用的环境变量。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务器路由
</div>

src/firecrawl/firecrawl.service.ts

```ts
import { Injectable } from '@nestjs/common';
import Firecrawl from '@mendable/firecrawl-js';

@Injectable()
export class FirecrawlService {
  private readonly client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

  async scrape(url: string) {
    return this.client.scrape(url, { formats: ['markdown'] });
  }
}
```

<div id="call-it-from-your-app">
  ## 在应用中调用
</div>

```ts
// src/firecrawl/firecrawl.controller.ts
import { Body, Controller, Post } from '@nestjs/common';
import { FirecrawlService } from './firecrawl.service';

@Controller('api/firecrawl')
export class FirecrawlController {
  constructor(private readonly firecrawl: FirecrawlService) {}

  @Post()
  async create(@Body('url') url: string) {
    const doc = await this.firecrawl.scrape(url);
    return { markdown: doc.markdown };
  }
}
```

<div id="notes">
  ## 注意事项
</div>

* 在模块中注册 `FirecrawlService` 和 `FirecrawlController`。
* 不要将 API 密钥暴露在客户端代码中。

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 接口](/zh/api-reference/endpoint/scrape)
* [Node SDK](/zh/sdks/node)