---
title: "Hono 快速开始"
description: "通过 Hono 路由使用 fetch 调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

在 Hono 路由中直接使用 Firecrawl HTTP API，使其可跨不同运行时运行。

<div id="prerequisites">
  ## 前置条件
</div>

* Firecrawl API 密钥
* Hono 应用
* 受支持的运行时 (Node、Bun、Cloudflare、Deno)

<div id="install">
  ## 安装
</div>

```bash
npm install hono
```

<div id="configure-environment">
  ## 配置环境
</div>

通过运行时的环境绑定提供 API 密钥。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务端路由
</div>

src/index.ts

```ts
import { Hono } from 'hono';

type Bindings = { FIRECRAWL_API_KEY: string };

const app = new Hono<{ Bindings: Bindings }>();

app.post('/api/firecrawl', async (c) => {
  const { url } = await c.req.json();
  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${c.env.FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  const data = await response.json();
  return c.json(data);
});

export default app;
```

<div id="call-it-from-your-app">
  ## 在应用中调用
</div>

```ts
const response = await fetch('/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事项
</div>

* 为所选的运行时配置环境绑定 (Wrangler、Deno Deploy 等) 。

<div id="next-steps">
  ## 后续步骤
</div>

* [抓取端点](/zh/api-reference/endpoint/scrape)