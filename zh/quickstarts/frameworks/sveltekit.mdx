---
title: "SvelteKit 快速入门"
description: "从 SvelteKit 服务端端点调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

通过 SvelteKit 的 `+server.ts` 端点在服务器端调用 Firecrawl。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl API 密钥
* SvelteKit 应用
* Node.js 18+

<div id="install">
  ## 安装
</div>

无需额外软件包；请在服务端端点中使用 fetch。

<div id="configure-environment">
  ## 配置环境
</div>

将 API 密钥保存在 `.env` 中，并通过 `$env/static/private` 加载。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务器路由
</div>

src/routes/api/firecrawl/+server.ts

```ts
import { json } from '@sveltejs/kit';
import { FIRECRAWL_API_KEY } from '$env/static/private';

export async function POST({ request }) {
  const { url } = await request.json();
  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  const data = await response.json();
  return json(data);
}
```

<div id="call-it-from-your-app">
  ## 在应用中调用
</div>

```ts
const response = await fetch('/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事项
</div>

* 请将 API 密钥保存在私有环境变量中；避免通过 `$env/static/public` 暴露。

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 端点](/zh/api-reference/endpoint/scrape)