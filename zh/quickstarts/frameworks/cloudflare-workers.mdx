---
title: "Cloudflare Workers 快速入门"
description: "在 Cloudflare Worker 中调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

在 Worker 中使用 `fetch` 调用 Firecrawl 的 HTTP API。

<div id="prerequisites">
  ## 前置条件
</div>

* Firecrawl API 密钥
* Cloudflare Workers 项目
* Wrangler

<div id="install">
  ## 安装
</div>

无需额外软件包；Workers 默认内置 fetch。

<div id="configure-environment">
  ## 配置环境
</div>

使用 Wrangler 将 API 密钥设置为 Worker Secret。

```bash
wrangler secret put FIRECRAWL_API_KEY
```

<div id="add-a-server-route">
  ## 添加服务器端路由
</div>

src/index.ts

```ts
export interface Env {
  FIRECRAWL_API_KEY: string;
}

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    if (request.method !== 'POST') {
      return new Response('Method not allowed', { status: 405 });
    }

    const { url } = await request.json();
    const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${env.FIRECRAWL_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ url, formats: ['markdown'] }),
    });

    return new Response(await response.text(), {
      headers: { 'Content-Type': 'application/json' },
    });
  },
};
```

<div id="call-it-from-your-app">
  ## 在应用中调用
</div>

```js
const response = await fetch('https://your-worker.example.workers.dev', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事项
</div>

* Workers 在边缘环境中运行，因此请始终通过 Worker 代理 Firecrawl 请求。

<div id="next-steps">
  ## 后续步骤
</div>

* [抓取端点](/zh/api-reference/endpoint/scrape)