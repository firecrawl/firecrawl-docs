---
title: "FastAPI 快速入门"
description: "通过 FastAPI 路由调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

在 FastAPI 的路由处理函数中使用 Firecrawl Python SDK。

<div id="prerequisites">
  ## 前置条件
</div>

* Firecrawl API 密钥
* FastAPI 应用
* Python 3.8+

<div id="install">
  ## 安装
</div>

```bash
pip install firecrawl-py
```

<div id="configure-environment">
  ## 配置环境
</div>

在服务器环境变量中设置 API 密钥。

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="add-a-server-route">
  ## 添加服务器路由
</div>

app/main.py

```python
import os
from fastapi import FastAPI
from firecrawl import Firecrawl

app = FastAPI()
client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])

@app.post("/api/firecrawl")
def scrape(body: dict):
    doc = client.scrape(body["url"], formats=["markdown"])
    return {"markdown": doc.markdown}
```

<div id="call-it-from-your-app">
  ## 在应用中调用
</div>

```python
import requests

response = requests.post(
    "http://localhost:8000/api/firecrawl",
    json={"url": "https://example.com"},
)

print(response.json().get("markdown"))
```

<div id="notes">
  ## 注意事项
</div>

* 只初始化一次 Firecrawl 客户端，以便复用 HTTP 连接。

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 接口](/zh/api-reference/endpoint/scrape)
* [Python SDK](/zh/sdks/python)