---
title: "AWS Lambda 快速入门"
description: "在 AWS Lambda 函数中调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

通过 Lambda 处理程序安全代理 Firecrawl 请求。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl API 密钥
* AWS Lambda (Node.js 18 运行时)

<div id="install">
  ## 安装
</div>

无需额外安装任何包；可直接在 Node.js 18 中使用 fetch。

<div id="configure-environment">
  ## 配置环境
</div>

在 Lambda 环境变量中设置 `FIRECRAWL_API_KEY`。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务器路由
</div>

index.mjs

```js
export const handler = async (event) => {
  const { url } = JSON.parse(event.body || '{}');

  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${process.env.FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  const data = await response.json();
  return {
    statusCode: 200,
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  };
};
```

<div id="call-it-from-your-app">
  ## 从你的应用中调用
</div>

```js
const response = await fetch('https://your-api-id.execute-api.region.amazonaws.com/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事项
</div>

* 配置 API Gateway (或 Lambda Function URLs) 以对外暴露你的 Lambda 端点。

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 端点](/zh/api-reference/endpoint/scrape)