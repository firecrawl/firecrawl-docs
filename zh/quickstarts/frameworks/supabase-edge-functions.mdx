---
title: "Supabase Edge Functions 快速入门"
description: "在 Supabase Edge Function 中调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

使用基于 Deno 的 Edge Function 调用 Firecrawl HTTP API。

<div id="prerequisites">
  ## 先决条件
</div>

* Firecrawl API 密钥
* Supabase 项目
* Supabase CLI

<div id="install">
  ## 安装
</div>

无需额外软件包；Supabase Edge Functions 运行在 Deno 上，并使用 fetch。

<div id="configure-environment">
  ## 配置环境
</div>

将 API 密钥设为 Supabase Secret。

```bash
supabase secrets set FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务端路由
</div>

supabase/functions/firecrawl/index.ts

```ts
Deno.serve(async (req) => {
  const { url } = await req.json();
  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${Deno.env.get('FIRECRAWL_API_KEY')}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  return new Response(await response.text(), {
    headers: { 'Content-Type': 'application/json' },
  });
});
```

<div id="call-it-from-your-app">
  ## 从你的应用中调用
</div>

```js
const response = await fetch('https://project.supabase.co/functions/v1/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意
</div>

* Edge Functions 使用 Deno，因此应避免使用仅适用于 Node 的包。

<div id="next-steps">
  ## 下一步
</div>

* [Scrape 端点](/zh/api-reference/endpoint/scrape)