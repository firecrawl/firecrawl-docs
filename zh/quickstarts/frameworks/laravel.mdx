---
title: "Laravel 快速入门"
description: "在 Laravel API 路由中调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

使用 Laravel 的 HTTP 客户端调用 Firecrawl API。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl API 密钥
* Laravel 应用程序
* PHP 8.1+

<div id="install">
  ## 安装
</div>

无需额外的包；Laravel 自带 HTTP 客户端。

<div id="configure-environment">
  ## 配置环境
</div>

将 API 密钥添加到 `.env` 文件中。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务器端路由
</div>

routes/api.php

```php
<?php

use Illuminate\Support\Facades\Http;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

Route::post('/firecrawl', function (Request $request) {
    $response = Http::withToken(env('FIRECRAWL_API_KEY'))
        ->post('https://api.firecrawl.dev/v2/scrape', [
            'url' => $request->input('url'),
            'formats' => ['markdown'],
        ]);

    return $response->json();
});
```

<div id="call-it-from-your-app">
  ## 从你的应用中调用
</div>

```bash
curl -X POST http://localhost:8000/api/firecrawl               -H 'Content-Type: application/json'               -d '{"url": "https://example.com"}'
```

<div id="notes">
  ## 注意事项
</div>

* API 密钥只能保存在服务端环境变量中。

<div id="next-steps">
  ## 下一步
</div>

* [抓取接口](/zh/api-reference/endpoint/scrape)