---
title: "Vercel Functions 快速入门"
description: "在 Vercel Serverless Function 中调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

使用 Vercel Function 代理 Firecrawl 请求，无需暴露您的 API 密钥。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl API 密钥
* Vercel 项目
* Node.js 18+

<div id="install">
  ## 安装
</div>

无需额外安装任何包；直接在函数内使用 fetch 即可。

<div id="configure-environment">
  ## 配置环境
</div>

在 `.env.local` 和 Vercel 项目设置中配置 `FIRECRAWL_API_KEY`。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务端路由
</div>

api/firecrawl.ts

```ts
import type { VercelRequest, VercelResponse } from '@vercel/node';

export default async function handler(req: VercelRequest, res: VercelResponse) {
  if (req.method !== 'POST') {
    res.status(405).send('Method not allowed');
    return;
  }

  const { url } = req.body;
  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${process.env.FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  const data = await response.json();
  res.status(200).json(data);
}
```

<div id="call-it-from-your-app">
  ## 在你的应用中调用
</div>

```js
const response = await fetch('/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事项
</div>

* 在 Vercel 中配置环境变量，以确保 API 密钥不被泄露。

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 接口](/zh/api-reference/endpoint/scrape)