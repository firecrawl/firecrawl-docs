---
title: "Flask 快速入门"
description: "在 Flask 路由中调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

在 Flask 服务器端点中集成 Firecrawl Python SDK。

<div id="prerequisites">
  ## 前置条件
</div>

* Firecrawl API 密钥
* Flask 应用
* Python 3.8+

<div id="install">
  ## 安装
</div>

```bash
pip install firecrawl-py
```

<div id="configure-environment">
  ## 配置环境
</div>

在启动 Flask 之前，先在环境中设置 API 密钥。

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="add-a-server-route">
  ## 添加服务端路由
</div>

app.py

```python
import os
from flask import Flask, request, jsonify
from firecrawl import Firecrawl

app = Flask(__name__)
client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])

@app.post("/api/firecrawl")
def firecrawl():
    url = request.json["url"]
    doc = client.scrape(url, formats=["markdown"])
    return jsonify(markdown=doc.markdown)
```

<div id="call-it-from-your-app">
  ## 在应用中调用
</div>

```python
import requests

response = requests.post(
    "http://localhost:5000/api/firecrawl",
    json={"url": "https://example.com"},
)

print(response.json().get("markdown"))
```

<div id="notes">
  ## 注意事项
</div>

* Firecrawl 客户端只需初始化一次，并在多个请求中重复使用。

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 端点](/zh/api-reference/endpoint/scrape)
* [Python SDK](/zh/sdks/python)