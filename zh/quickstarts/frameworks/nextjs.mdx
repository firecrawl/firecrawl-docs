---
title: "Next.js 快速入门"
description: "在 Next.js Route Handler 中调用 Firecrawl。"
---

<div id="overview">
  ## 概览
</div>

通过在 Next.js Route Handler 中调用 Firecrawl，可将 Firecrawl API 密钥保留在服务器端。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl API 密钥
* Next.js 应用 (App Router)
* Node.js 18+

<div id="install">
  ## 安装
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-environment">
  ## 配置环境
</div>

将 API 密钥添加到 `.env.local` 中，以确保它仅在服务器端可用。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务器路由
</div>

app/api/firecrawl/route.ts

```ts
import Firecrawl from '@mendable/firecrawl-js';

export async function POST(request: Request) {
  const { url } = await request.json();
  const client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

  const doc = await client.scrape(url, { formats: ['markdown'] });
  return Response.json({ markdown: doc.markdown });
}
```

<div id="call-it-from-your-app">
  ## 在你的应用中调用
</div>

```ts
const response = await fetch('/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.markdown);
```

<div id="notes">
  ## 注意事项
</div>

* 仅在服务器路由中调用 Firecrawl，以避免暴露 API 密钥。
* 如果你使用 Pages Router，请改为创建 `pages/api/firecrawl.ts`。

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 端点](/zh/api-reference/endpoint/scrape)
* [Node SDK](/zh/sdks/node)