---
title: "Rails 快速入门"
description: "在 Rails 控制器中调用 Firecrawl。"
---

<div id="overview">
  ## 概述
</div>

使用 Ruby 的 Net::HTTP 在控制器中调用 Firecrawl API。

<div id="prerequisites">
  ## 前置条件
</div>

* Firecrawl API 密钥
* Rails 应用
* Ruby 3.0+

<div id="install">
  ## 安装
</div>

无需额外安装任何软件包；直接使用 Ruby 的标准库即可。

<div id="configure-environment">
  ## 配置环境
</div>

在 Rails 环境中设置 API 密钥。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## 添加服务器路由
</div>

app/controllers/firecrawl&#95;controller.rb

```rb
require 'net/http'
require 'uri'
require 'json'

class FirecrawlController < ApplicationController
  protect_from_forgery with: :null_session

  def create
    uri = URI('https://api.firecrawl.dev/v2/scrape')
    request = Net::HTTP::Post.new(uri)
    request['Authorization'] = "Bearer #{ENV.fetch('FIRECRAWL_API_KEY')}"
    request['Content-Type'] = 'application/json'
    request.body = { url: params[:url], formats: ['markdown'] }.to_json

    response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) do |http|
      http.request(request)
    end

    render json: JSON.parse(response.body)
  end
end
```

<div id="call-it-from-your-app">
  ## 在应用中调用
</div>

```rb
require 'net/http'
require 'json'

uri = URI('http://localhost:3000/api/firecrawl')
response = Net::HTTP.post(uri, { url: 'https://example.com' }.to_json, 'Content-Type' => 'application/json')

puts JSON.parse(response.body)
```

<div id="notes">
  ## 注意事项
</div>

* 在 `config/routes.rb` 中添加 `post '/api/firecrawl', to: 'firecrawl#create'`。

<div id="next-steps">
  ## 后续步骤
</div>

* [Scrape 接口](/zh/api-reference/endpoint/scrape)