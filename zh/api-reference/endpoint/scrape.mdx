---
title: "Scrape"
openapi: 'v2-openapi POST /scrape'
---

## v2 有哪些新内容

### 新增 formats

- `"summary"` - 获取页面内容的精炼摘要
- JSON 提取现以对象格式提供：`{ type: "json", prompt, schema }`
- 截图现以对象格式提供：`{ type: "screenshot", fullPage, quality, viewport }`
- `"images"` - 提取页面中的所有图片 URL

### 关键改进

- **默认更快**：请求启用缓存，`maxAge` 默认为 2 天
- **更合理的默认设置**：`blockAds`、`skipTlsVerification` 和 `removeBase64Images` 默认开启
- **更强大的截图选项**：通过对象格式对截图参数进行完全控制