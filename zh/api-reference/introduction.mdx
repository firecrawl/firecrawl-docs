---
title: "简介"
description: "Firecrawl API 参考"
---

<div id="features">
  ## 功能
</div>

<CardGroup cols={3}>
  <Card title="Scrape" icon="markdown" href="/zh/api-reference/endpoint/scrape" color="FF713C">
    从任意网页提取内容，输出为 markdown 或 json。
  </Card>

  <Card title="Crawl" icon="spider" href="/zh/api-reference/endpoint/crawl-post" color="FF713C">
    爬取整站，提取页面内容与元数据。
  </Card>

  <Card title="Map" icon="map" href="/zh/api-reference/endpoint/map" color="FF713C">
    快速、稳定地获取任意网站的完整 URL 列表。
  </Card>

  <Card title="Search" icon="magnifying-glass" href="/zh/api-reference/endpoint/search" color="FF713C">
    搜索全网，并以任意格式获取完整页面内容。
  </Card>
</CardGroup>

<div id="agentic-features">
  ## 智能代理功能
</div>

<CardGroup cols={3}>
  <Card title="Extract" icon="barcode-read" href="/zh/api-reference/endpoint/extract" color="FF713C">
    使用自然语言从整页网页中提取结构化数据。
  </Card>

  {/* <Card title="Deep Research" icon="brain-circuit" href="/api-reference/endpoint/deep-research" color="FF713C">
      AI-powered web research that autonomously explores and synthesizes information.
    </Card> */}
</CardGroup>

<div id="base-url">
  ## 基础 URL
</div>

所有请求使用以下基础 URL：

```bash
https://api.firecrawl.dev 
```

<div id="authentication">
  ## 身份验证
</div>

进行身份验证时，需要在请求中添加 Authorization 标头。其值应为 `Bearer fc-123456789`，其中 `fc-123456789` 是你的 API Key。

```bash
Authorization：Bearer fc-123456789
```

​

<div id="response-codes">
  ## 响应代码
</div>

Firecrawl 使用常见的 HTTP 状态码来表示请求结果。

通常，2xx HTTP 状态码表示成功，4xx 表示用户相关错误，5xx 表示基础设施问题。

Status | Description
--- | ---
200 | 请求成功。
400 | 请检查参数是否正确。
401 | 未提供 API 密钥。
402 | 需要付款。
404 | 未找到所请求的资源。
429 | 超出速率限制。
5xx | 表示 Firecrawl 服务器错误。

有关所有可能的 API 错误的详细说明，请参见“错误代码”部分。

<div id="firecrawl-error-codes-5xx">
  ### Firecrawl 错误代码（5xx）
</div>

当出现 5xx 错误时，Firecrawl 会提供更具体的错误代码来说明问题。​

Error Code | Status | Description
--- | --- | ---
`SCRAPE_ALL_ENGINES_FAILED` | 500 | 所有抓取引擎均失败。
`SCRAPE_SSL_ERROR` | 500 | 页面 SSL 证书无效。你可以使用 `skipTlsVerification:true` 来跳过此检查。
`SCRAPE_SITE_ERROR` | 500 | 不可恢复的站点错误。
`SCRAPE_DNS_RESOLUTION_ERROR` | 500 | DNS 解析失败。
`SCRAPE_ACTION_ERROR` | 500 | 执行页面 actions 时出错。
`SCRAPE_PDF_PREFETCH_FAILED` | 500 | 预取 PDF 失败。
`SCRAPE_PDF_INSUFFICIENT_TIME_ERROR` | 500 | 处理 PDF 的时间不足。
`SCRAPE_PDF_ANTIBOT_ERROR` | 500 | PDF 被反机器人机制拦截。
`SCRAPE_ZDR_VIOLATION_ERROR` | 500 | 零数据保留冲突：当 `zeroDataRetention:true`，但其他选项（如 `screenshot`）需要临时存储时发生。
`SCRAPE_UNSUPPORTED_FILE_ERROR` | 500 | 不支持的文件类型，或文件大小超过 10MB 限制。
`UNKNOWN_ERROR` | 500 | 通用或意外错误。  

<div id="rate-limit">
  ## 速率限制
</div>

为确保服务的稳定性与可靠性，Firecrawl API 实施了速率限制。该限制适用于所有端点，依据在特定时间窗口内的请求次数进行计算。

当你超出速率限制时，将收到 429 响应码。