---
title: "Démarrage rapide avec Rust"
description: "Scrapez votre première URL avec le SDK Rust de Firecrawl."
---

<div id="overview">
  ## Vue d’ensemble
</div>

Utilisez le SDK Rust de Firecrawl pour appeler l’API v2 à partir de code Rust asynchrone.

<div id="prerequisites">
  ## Prérequis
</div>

* Une clé API Firecrawl
* La toolchain Rust avec tokio

<div id="install">
  ## Installation
</div>

```bash
cargo add firecrawl tokio --features tokio/full
```

<div id="configure-your-api-key">
  ## Configurez votre clé API
</div>

Définissez votre clé API dans une variable d’environnement.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Effectuez votre première requête
</div>

```rust
use firecrawl::FirecrawlApp;

#[tokio::main]
async fn main() {
    let api_key = std::env::var("FIRECRAWL_API_KEY").expect("Missing API key");
    let app = FirecrawlApp::new(api_key).expect("Failed to init Firecrawl");

    let doc = app
        .scrape_url("https://example.com", None)
        .await
        .expect("Scrape failed");

    println!("{}", doc.markdown.unwrap_or_default());
}
```

<div id="complete-example">
  ## Exemple complet
</div>

```rust
use firecrawl::FirecrawlApp;

#[tokio::main]
async fn main() {
    let api_key = std::env::var("FIRECRAWL_API_KEY").expect("Missing API key");
    let app = FirecrawlApp::new(api_key).expect("Failed to init Firecrawl");

    let doc = app
        .scrape_url("https://example.com", None)
        .await
        .expect("Scrape failed");

    if let Some(metadata) = doc.metadata {
        println!("Title: {}", metadata.title.unwrap_or_default());
    }
    println!("{}", doc.markdown.unwrap_or_default());
}
```

<div id="next-steps">
  ## Prochaines étapes
</div>

* [Point de terminaison Scrape](/fr/api-reference/endpoint/scrape)
* [SDK Rust](/fr/sdks/rust)