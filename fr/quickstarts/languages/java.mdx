---
title: "Démarrage rapide Java"
description: "Scrapez votre première URL avec le SDK Java de Firecrawl."
---

<div id="overview">
  ## Aperçu
</div>

Utilisez le SDK Java officiel de Firecrawl pour appeler l’API v2 depuis Java.

<div id="prerequisites">
  ## Prérequis
</div>

* Une clé API Firecrawl
* Java 11 ou version ultérieure
* Maven ou Gradle

<div id="install">
  ## Installation
</div>

```gradle
implementation("com.firecrawl:firecrawl-java:1.1.1")
```

<div id="configure-your-api-key">
  ## Configurez votre clé API
</div>

Définissez votre clé API en tant que variable d’environnement avant d’exécuter votre application.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Envoyez votre première requête
</div>

```java
import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import java.util.List;

FirecrawlClient client = FirecrawlClient.fromEnv();

Document doc = client.scrape(
    "https://example.com",
    ScrapeOptions.builder().formats(List.of("markdown")).build()
);

System.out.println(doc.getMarkdown());
```

<div id="complete-example">
  ## Exemple complet
</div>

```java
import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        FirecrawlClient client = FirecrawlClient.fromEnv();
        Document doc = client.scrape(
            "https://example.com",
            ScrapeOptions.builder().formats(List.of("markdown", "html")).build()
        );

                    if (doc.getMetadata() != null) {
                        System.out.println(doc.getMetadata().get("title"));
                    }
        System.out.println(doc.getMarkdown());
    }
}
```

<div id="next-steps">
  ## Prochaines étapes
</div>

* [Point de terminaison Scrape](/fr/api-reference/endpoint/scrape)
* [SDK Java](/fr/sdks/java)