---
title: "Démarrage rapide Python"
description: "Récupérez le contenu de votre première URL avec le SDK Python de Firecrawl."
---

<div id="overview">
  ## Vue d’ensemble
</div>

Utilisez le SDK Python officiel de Firecrawl pour récupérer une URL et obtenir une sortie structurée.

<div id="prerequisites">
  ## Prérequis
</div>

* Une clé d’API Firecrawl
* Python 3.8+
* pip

<div id="install">
  ## Installation
</div>

```bash
pip install firecrawl-py
```

<div id="configure-your-api-key">
  ## Configurez votre clé API
</div>

Conservez votre clé API dans une variable d’environnement côté serveur.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Faites votre première requête
</div>

```python
import os
from firecrawl import Firecrawl

client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])

doc = client.scrape("https://example.com", formats=["markdown"])
print(doc.markdown)
```

<div id="complete-example">
  ## Exemple complet
</div>

```python
import os
from firecrawl import Firecrawl

def main() -> None:
    client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])
    doc = client.scrape("https://example.com", formats=["markdown", "html"])
    print(doc.metadata.title if doc.metadata else "")
    print(doc.markdown)

if __name__ == "__main__":
    main()
```

<div id="next-steps">
  ## Prochaines étapes
</div>

* [Point de terminaison Scrape](/fr/api-reference/endpoint/scrape)
* [SDK Python](/fr/sdks/python)