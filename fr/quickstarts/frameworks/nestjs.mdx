---
title: "Démarrage rapide avec NestJS"
description: "Appelez Firecrawl depuis un contrôleur NestJS."
---

<div id="overview">
  ## Vue d’ensemble
</div>

Intégrez Firecrawl dans un service NestJS et exposez-le via un contrôleur.

<div id="prerequisites">
  ## Prérequis
</div>

* Une clé API Firecrawl
* Une application NestJS
* Node.js 18+

<div id="install">
  ## Installation
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-environment">
  ## Configurer l’environnement
</div>

Définissez la clé API comme variable d’environnement accessible uniquement côté serveur.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Ajouter une route côté serveur
</div>

src/firecrawl/firecrawl.service.ts

```ts
import { Injectable } from '@nestjs/common';
import Firecrawl from '@mendable/firecrawl-js';

@Injectable()
export class FirecrawlService {
  private readonly client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

  async scrape(url: string) {
    return this.client.scrape(url, { formats: ['markdown'] });
  }
}
```

<div id="call-it-from-your-app">
  ## Appelez-le depuis votre application
</div>

```ts
// src/firecrawl/firecrawl.controller.ts
import { Body, Controller, Post } from '@nestjs/common';
import { FirecrawlService } from './firecrawl.service';

@Controller('api/firecrawl')
export class FirecrawlController {
  constructor(private readonly firecrawl: FirecrawlService) {}

  @Post()
  async create(@Body('url') url: string) {
    const doc = await this.firecrawl.scrape(url);
    return { markdown: doc.markdown };
  }
}
```

<div id="notes">
  ## Remarques
</div>

* Enregistrez `FirecrawlService` et `FirecrawlController` dans un module.
* N’exposez pas la clé d’API dans le code côté client.

<div id="next-steps">
  ## Étapes suivantes
</div>

* [Point de terminaison « Scrape »](/fr/api-reference/endpoint/scrape)
* [SDK Node](/fr/sdks/node)