---
title: "Démarrage rapide ASP.NET Core"
description: "Appelez Firecrawl depuis une API minimale ASP.NET Core."
---

<div id="overview">
  ## Vue d’ensemble
</div>

Utilisez HttpClient dans une route d’API minimale pour servir de proxy aux appels à Firecrawl.

<div id="prerequisites">
  ## Prérequis
</div>

* Une clé API Firecrawl
* Une application ASP.NET Core
* .NET 6+

<div id="install">
  ## Installation
</div>

Aucun package supplémentaire n’est nécessaire ; utilisez HttpClient et les utilitaires JSON intégrés.

<div id="configure-environment">
  ## Configurer l’environnement
</div>

Définissez `FIRECRAWL_API_KEY` dans votre environnement ou dans les paramètres de votre application.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Ajouter une route côté serveur
</div>

Program.cs

```csharp
using System.Net.Http.Headers;
using System.Net.Http.Json;

var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.MapPost("/api/firecrawl", async (HttpRequest request) =>
{
    var payload = await request.ReadFromJsonAsync<FirecrawlRequest>();
    var apiKey = Environment.GetEnvironmentVariable("FIRECRAWL_API_KEY");

    var http = new HttpClient();
    http.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", apiKey);

    var response = await http.PostAsJsonAsync(
        "https://api.firecrawl.dev/v2/scrape",
        new { url = payload?.Url, formats = new[] { "markdown" } }
    );

    var body = await response.Content.ReadAsStringAsync();
    return Results.Content(body, "application/json");
});

app.Run();

record FirecrawlRequest(string Url);
```

<div id="call-it-from-your-app">
  ## Appelez-le depuis votre application
</div>

```csharp
using System.Net.Http.Json;

var http = new HttpClient();
var response = await http.PostAsJsonAsync(
    "http://localhost:5000/api/firecrawl",
    new { url = "https://example.com" }
);

Console.WriteLine(await response.Content.ReadAsStringAsync());
```

<div id="notes">
  ## Remarques
</div>

* Conservez la clé API Firecrawl côté serveur et faites transiter les requêtes via votre API.

<div id="next-steps">
  ## Étapes suivantes
</div>

* [Point de terminaison « Scrape »](/fr/api-reference/endpoint/scrape)