---
title: "Démarrage rapide avec Spring Boot"
description: "Appelez Firecrawl depuis un contrôleur Spring Boot."
---

<div id="overview">
  ## Vue d’ensemble
</div>

Utilisez le SDK Java de Firecrawl dans un service et un contrôleur Spring Boot.

<div id="prerequisites">
  ## Prérequis
</div>

* Une clé API Firecrawl
* Une application Spring Boot
* Java 11+

<div id="install">
  ## Installation
</div>

```gradle
implementation("com.firecrawl:firecrawl-java:1.1.1")
```

<div id="configure-environment">
  ## Configurer l’environnement
</div>

Définissez la clé API dans votre environnement ou dans la configuration de votre application.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Ajouter une route au serveur
</div>

src/main/java/com/example/firecrawl/FirecrawlService.java

```java
package com.example.firecrawl;

import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class FirecrawlService {
    private final FirecrawlClient client = FirecrawlClient.fromEnv();

    public Document scrape(String url) {
        return client.scrape(url, ScrapeOptions.builder().formats(List.of("markdown")).build());
    }
}
```

<div id="call-it-from-your-app">
  ## Appelez-le depuis votre application
</div>

```java
// src/main/java/com/example/firecrawl/FirecrawlController.java
package com.example.firecrawl;

import com.firecrawl.models.Document;
import org.springframework.web.bind.annotation.*;

import java.util.Map;

@RestController
@RequestMapping("/api/firecrawl")
public class FirecrawlController {
    private final FirecrawlService service;

    public FirecrawlController(FirecrawlService service) {
        this.service = service;
    }

    @PostMapping
    public Map<String, String> scrape(@RequestBody Map<String, String> body) {
        Document doc = service.scrape(body.get("url"));
        return Map.of("markdown", doc.getMarkdown());
    }
}
```

<div id="notes">
  ## Remarques
</div>

* Enregistrez les packages service/controller dans l’analyse des composants.

<div id="next-steps">
  ## Étapes suivantes
</div>

* [Point de terminaison Scrape](/fr/api-reference/endpoint/scrape)
* [Java SDK](/fr/sdks/java)