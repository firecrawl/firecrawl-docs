---
title: 'Développement'
description: 'Apprenez à prévisualiser les modifications en local'
og:title: "Développement | Firecrawl"
og:description: "Apprenez à prévisualiser les modifications en local"
---

<Info>
  **Prérequis** Vous devez avoir installé Node.js (version 18.10.0 ou
  supérieure).
</Info>

Étape 1. Installez Mintlify sur votre système :

<CodeGroup>
  ```bash npm
  npm i -g mintlify
  ```

  ```bash yarn
  yarn global add mintlify
  ```
</CodeGroup>

Étape 2. Rendez-vous à l’emplacement où se trouvent les documents (là où se trouve `mint.json`) et exécutez la commande suivante :

```bash
mintlify dev
```

Le site de documentation est maintenant accessible à l’adresse `http://localhost:3000`.

<div id="custom-ports">
  ### Ports personnalisés
</div>

Par défaut, Mintlify utilise le port 3000. Vous pouvez utiliser l’option `--port` pour définir le port sur lequel Mintlify s’exécute. Par exemple, utilisez cette commande pour l’exécuter sur le port 3333 :

```bash
mintlify dev --port 3333
```

Vous verrez une erreur de ce type si vous tentez d’exécuter Mintlify sur un port déjà utilisé :

```md
Erreur : listen EADDRINUSE : adresse déjà utilisée :::3000
```

<div id="mintlify-versions">
  ## Versions de Mintlify
</div>

Chaque CLI est associée à une version spécifique de Mintlify. Mettez à jour la CLI si votre site en local diffère de l’environnement de production.

<CodeGroup>
  ```bash npm
  npm i -g mintlify@latest
  ```

  ```bash yarn
  yarn global upgrade mintlify
  ```
</CodeGroup>

<div id="deployment">
  ## Déploiement
</div>

<Tip>
  Éditeurs illimités inclus dans le [Startup
  Plan](https://mintlify.com/pricing)
</Tip>

Vous devriez voir ce qui suit si le déploiement s’est effectué avec succès :

<Frame>
  <img src="/images/checks-passed.png" style={{ borderRadius: '0.5rem' }} />
</Frame>

<div id="troubleshooting">
  ## Dépannage
</div>

Voici comment résoudre certains problèmes courants lors de l’utilisation de la CLI.

<AccordionGroup>
  <Accordion title="Mintlify ne se charge pas">
    Mettez à jour vers Node v18. Exécutez `mintlify install` et réessayez.
  </Accordion>

  <Accordion title="Aucun fichier ou répertoire de ce type sur Windows">
    Accédez au répertoire `C:/Users/Username/.mintlify/` et supprimez le dossier `mint`. Puis, ouvrez Git Bash à cet emplacement et exécutez `git clone
    https://github.com/mintlify/mint.git`.

    Répétez l’étape 3.
  </Accordion>

  <Accordion title="Erreur inconnue">
    Accédez à la racine de votre appareil et supprimez le dossier ~/.mintlify.
    Puis exécutez à nouveau `mintlify dev`.
  </Accordion>
</AccordionGroup>

Vous vous demandez ce qui a changé dans une version de la CLI ? [Consultez le changelog de la CLI.](/fr/changelog/command-line)