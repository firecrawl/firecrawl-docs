---
title: "Mode avancé"
description: "Utilisez des proxies avancés pour un scraping fiable sur des sites complexes"
og:title: "Mode avancé | Firecrawl"
og:description: "Utilisez des proxies avancés pour un scraping fiable sur des sites complexes"
---

import ProxyPython from "/snippets/fr/v2/scrape/proxy/python.mdx";
import ProxyNode from "/snippets/fr/v2/scrape/proxy/js.mdx";
import ProxyCURL from "/snippets/fr/v2/scrape/proxy/curl.mdx";

<Warning>
  Le paramètre `proxy` est obsolète. Nous recommandons `auto` — la valeur par défaut — qui permet à Firecrawl de gérer la sélection du proxy pour vous.
</Warning>

Firecrawl propose différents types de proxy pour vous aider à effectuer du scraping sur des sites web de complexité variable. Définissez le paramètre `proxy` pour choisir la stratégie de proxy utilisée pour une requête.

<div id="proxy-types">
  ## Types de proxy
</div>

| Type       | Description                                                                          |
| ---------- | ------------------------------------------------------------------------------------ |
| `basic`    | Proxies standard adaptés à la plupart des sites                                      |
| `enhanced` | Proxies avancés pour les sites complexes                                             |
| `auto`     | **Recommandé.** Essaie d’abord `basic`, puis réessaie avec `enhanced` en cas d’échec |

Si vous ne spécifiez pas de proxy, Firecrawl utilise `auto` par défaut.

<div id="basic-usage">
  ## Utilisation de base
</div>

Définissez le paramètre `proxy` pour choisir une stratégie de proxy. L’exemple suivant utilise `auto`, ce qui permet à Firecrawl de décider quand passer à des proxies avancés.

<CodeGroup>
  <ProxyPython />

  <ProxyNode />

  <ProxyCURL />
</CodeGroup>

<Info>
  Les requêtes via un proxy avancé coûtent désormais le même prix que les requêtes de base — **1 crédit par requête**. Il n’y a plus de frais supplémentaires lorsque `auto` passe à une nouvelle tentative avancée.
</Info>

> Êtes-vous un agent d’IA ayant besoin d’une clé API Firecrawl ? Consultez [firecrawl.dev/agent-onboarding/SKILL.md](https://www.firecrawl.dev/agent-onboarding/SKILL.md) pour les instructions d’intégration automatisée.