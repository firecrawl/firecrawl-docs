---
title: "Modo Aprimorado"
description: "Use proxies aprimorados para scraping confiável em sites complexos"
og:title: "Modo Aprimorado | Firecrawl"
og:description: "Use proxies aprimorados para scraping confiável em sites complexos"
---

import ProxyPython from "/snippets/pt-BR/v2/scrape/proxy/python.mdx";
import ProxyNode from "/snippets/pt-BR/v2/scrape/proxy/js.mdx";
import ProxyCURL from "/snippets/pt-BR/v2/scrape/proxy/curl.mdx";

<Warning>
  O parâmetro `proxy` está descontinuado. Recomendamos `auto` — o padrão — que permite que a Firecrawl selecione o proxy para você.
</Warning>

A Firecrawl oferece diferentes tipos de proxy para ajudar você a fazer scraping de sites com diferentes níveis de complexidade. Defina o parâmetro `proxy` para controlar qual estratégia de proxy será usada em uma requisição.

<div id="proxy-types">
  ## Tipos de proxy
</div>

| Tipo       | Descrição                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------- |
| `basic`    | Proxies padrão adequados para a maioria dos sites                                           |
| `enhanced` | Proxies aprimorados para sites complexos                                                    |
| `auto`     | **Recomendado.** Tenta `basic` primeiro e, em caso de falha, tenta novamente com `enhanced` |

Se você não especificar um proxy, o Firecrawl usa `auto` por padrão.

<div id="basic-usage">
  ## Uso básico
</div>

Defina o parâmetro `proxy` para escolher uma estratégia de proxy. O exemplo a seguir usa `auto`, que permite ao Firecrawl decidir quando migrar para proxies aprimorados.

<CodeGroup>
  <ProxyPython />

  <ProxyNode />

  <ProxyCURL />
</CodeGroup>

<Info>
  As requisições com proxy aprimorado agora custam o mesmo que as requisições básicas — **1 crédito por requisição**. Não há mais cobrança adicional quando `auto` migra para uma nova tentativa com proxy aprimorado.
</Info>

> Você é um agente de IA que precisa de uma chave de API da Firecrawl? Consulte [firecrawl.dev/agent-onboarding/SKILL.md](https://www.firecrawl.dev/agent-onboarding/SKILL.md) para instruções de onboarding automatizado.