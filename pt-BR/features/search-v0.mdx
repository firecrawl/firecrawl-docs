---
title: "Pesquisa"
description: "Faça uma consulta SERP e obtenha todo o conteúdo com uma única chamada à API"
icon: "magnifying-glass"
og:title: "Pesquisa | Firecrawl"
og:description: "Faça uma consulta SERP e obtenha todo o conteúdo com uma única chamada à API"
---

<div id="searching-the-web-and-scraping-the-results-with-firecrawl">
  ## Pesquisando na web e extraindo os resultados com o Firecrawl
</div>

O Firecrawl integra sua API de SERP (Search Engine Results Page) à sua infraestrutura robusta de scraping para oferecer, em um único endpoint, uma experiência contínua de busca e extração. Veja por quê:

1. **Consulta de busca unificada:**
   Os usuários enviam uma consulta de busca pelo endpoint de SERP.

2. **Extração automatizada dos resultados:**
   O Firecrawl processa automaticamente os resultados da busca e utiliza seus recursos de scraping para extrair dados de cada página de resultado.

3. **Entrega dos dados:**
   Os dados extraídos de todas as páginas de resultado são compilados e entregues em markdown limpo — pronto para uso.

Essa integração permite realizar buscas na web com eficiência e obter dados abrangentes, já raspados de múltiplas fontes, com esforço mínimo.

Para mais detalhes, consulte a [documentação do endpoint /search](https://docs.firecrawl.dev/api-reference/endpoint/search).

<div id="search-any-query">
  ## Pesquise qualquer termo
</div>

<div id="search-endpoint">
  ### endpoint /search
</div>

Usado para pesquisar na web, obter os resultados mais relevantes, extrair cada página e retornar o markdown.

```bash
curl -X POST https://api.firecrawl.dev/v0/search \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer YOUR_API_KEY' \
    -d '{
      "query": "firecrawl",
      "pageOptions": {
        "fetchPageContent": true // false para uma API de SERP mais rápida
      }
    }'
```

```json
{
  "success": true,
  "data": [
    {
      "url": "https://docs.firecrawl.dev",
      "markdown": "# Conteúdo em Markdown",
      "provider": "web-scraper",
      "metadata": {
        "title": "Firecrawl | Raspagem da web confiável para seus LLMs",
        "description": "IA para CX e Vendas"
        "language": null,
        "sourceURL": "https://docs.firecrawl.dev/"
      }
    }
  ]
}
```


<div id="with-python-sdk">
  ### Com o SDK do Python
</div>

<div id="installing-python-sdk">
  #### Instalando o SDK do Python
</div>

```bash
pip install firecrawl-py
```


<div id="search-a-query">
  #### Pesquisar uma busca
</div>

```python
from firecrawl import FirecrawlApp

app = FirecrawlApp(api_key="YOUR_API_KEY")

result = app.search(query="O que é o Firecrawl?")
```

A resposta será semelhante à exibida no comando curl acima.


<div id="with-javascript-sdk">
  ### Com o SDK de JavaScript
</div>

<div id="installing-javascript-sdk">
  #### Instalando o SDK JavaScript
</div>

```bash
npm install @mendable/firecrawl-js
```


<div id="search-a-query">
  #### Pesquisar uma busca
</div>

```javascript
import FirecrawlApp from '@mendable/firecrawl-js';

// Inicialize o FirecrawlApp com sua chave de API
const app = new FirecrawlApp({ apiKey: 'YOUR_API_KEY' });

// Faça uma pesquisa
const result = await app.search('O que é o Firecrawl?');
```

A resposta será semelhante à exibida no comando curl acima.


<div id="with-go-sdk">
  ### Com o SDK do Go
</div>

<div id="installing-go-sdk">
  #### Instalando o SDK do Go
</div>

```bash
go get github.com/mendableai/firecrawl-go
```


<div id="search-a-query">
  #### Pesquisar uma busca
</div>

```go
import (
  "fmt"
  "log"

  "github.com/mendableai/firecrawl-go"
)

func main() {
  app, err := firecrawl.NewFirecrawlApp("YOUR_API_KEY")
  if err != nil {
      log.Fatalf("Falha ao inicializar o FirecrawlApp: %v", err)
  }

  query := "O que é o Firecrawl?"
  searchResult, err := app.Search(query)
  if err != nil {
    log.Fatalf("Falha ao executar a busca: %v", err)
  }
  fmt.Println(searchResult)
}
```


<div id="with-rust-sdk">
  ### Com o SDK do Rust
</div>

<div id="installing-rust-sdk">
  #### Instalando o SDK Rust
</div>

Adicione o seguinte ao seu `Cargo.toml`:

```toml
[dependencies]
firecrawl = "^0.1"
tokio = { version = "^1", features = ["full"] }
serde = { version = "^1.0", features = ["derive"] }
serde_json = "^1.0"
uuid = { version = "^1.10", features = ["v4"] }

[build-dependencies]
tokio = { version = "1", features = ["full"] }
```


<div id="search-a-query">
  #### Pesquisar uma busca
</div>

```rust
async fn main() {
  let api_key = "YOUR_API_KEY";
  let api_url = "https://api.firecrawl.dev";
  let app = FirecrawlApp::new(api_key, api_url).expect("Falha ao inicializar o FirecrawlApp");

  let query = "O que é o Firecrawl?";
  let search_result = app.search(query).await;

  match search_result {
    Ok(data) => println!("Resultado da pesquisa: {}", data),
    Err(e) => eprintln!("Falha na pesquisa: {}", e),
  }
}
```
