---
title: "Introdução"
description: "Referência da API do Firecrawl (v2)"
---

A API do Firecrawl oferece acesso programático a dados da web. Todos os endpoints compartilham uma URL base comum, um esquema de autenticação e um formato de resposta descritos nesta página.

<div id="features">
  ## Recursos
</div>

<CardGroup cols={3}>
  <Card title="Scrape" icon="markdown" href="/pt-BR/api-reference/endpoint/scrape" color="FF713C">
    Extraia conteúdo de qualquer página da web em formato Markdown ou JSON.
  </Card>

  <Card title="Parse" icon="markdown" href="/pt-BR/api-reference/endpoint/parse" color="FF713C">
    Envie arquivos e converta-os em Markdown ou outros formatos.
  </Card>

  <Card title="Crawl" icon="spider" href="/pt-BR/api-reference/endpoint/crawl-post" color="FF713C">
    Faça o rastreamento de sites inteiros e obtenha conteúdo de todas as páginas.
  </Card>

  <Card title="Map" icon="map" href="/pt-BR/api-reference/endpoint/map" color="FF713C">
    Obtenha rapidamente uma lista completa de URLs de qualquer site, de forma confiável.
  </Card>

  <Card title="Search" icon="magnifying-glass" href="/pt-BR/api-reference/endpoint/search" color="FF713C">
    Pesquise na web e obtenha o conteúdo completo das páginas em qualquer formato.
  </Card>
</CardGroup>

<div id="agentic-features">
  ## Recursos de agentes
</div>

<CardGroup cols={3}>
  <Card title="Agent" icon="robot" href="/pt-BR/api-reference/endpoint/agent" color="FF713C">
    Coleta autônoma de dados da web com IA.
  </Card>

  <Card title="Browser" icon="browser" href="/pt-BR/api-reference/endpoint/browser-create" color="FF713C">
    Crie e controle sessões de navegador para tarefas interativas na web.
  </Card>
</CardGroup>

<div id="base-url">
  ## URL base
</div>

Todas as requisições usam a seguinte URL base:

```bash
https://api.firecrawl.dev
```

<div id="authentication">
  ## Autenticação
</div>

Toda requisição exige um cabeçalho `Authorization` com sua chave de API:

```bash
Authorization: Bearer fc-YOUR-API-KEY
```

Inclua este cabeçalho em todas as chamadas à API. Você pode encontrar sua chave de API no [painel do Firecrawl](https://www.firecrawl.dev/app/api-keys).

<CodeGroup>
  ```bash cURL
  curl -X POST "https://api.firecrawl.dev/v2/scrape" \
    -H "Authorization: Bearer fc-YOUR-API-KEY" \
    -H "Content-Type: application/json" \
    -d '{"url": "https://example.com"}'
  ```

  ```python Python
  from firecrawl import Firecrawl

  firecrawl = Firecrawl(api_key="fc-YOUR-API-KEY")

  result = firecrawl.scrape("https://example.com")
  ```

  ```js Node
  import Firecrawl from '@mendable/firecrawl-js';

  const firecrawl = new Firecrawl({ apiKey: "fc-YOUR-API-KEY" });

  const result = await firecrawl.scrape('https://example.com');
  ```
</CodeGroup>

<div id="response-codes">
  ## Códigos de resposta
</div>

O Firecrawl usa códigos de status HTTP convencionais para indicar o resultado das suas requisições. Códigos na faixa `2xx` indicam sucesso, códigos `4xx` indicam erros do cliente e códigos `5xx` indicam erros do servidor.

Consulte [Erros](/pt-BR/api-reference/errors) para ver a referência completa, incluindo a string `error` retornada para cada tipo de falha, orientações sobre novas tentativas e um snippet de backoff pronto para copiar e colar.

<div id="rate-limit">
  ## Limite de taxa de requisições
</div>

A API do Firecrawl impõe limites de taxa de requisições em todos os endpoints para garantir a estabilidade do serviço. Esses limites são definidos com base no número de requisições em uma janela de tempo específica.

Quando você ultrapassa o limite de taxa, a API retorna o código de status `429`. Aguarde um pouco e tente fazer a requisição novamente após um breve intervalo.