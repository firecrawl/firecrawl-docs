---
title: "Hono"
description: "Use o Firecrawl com Hono para criar APIs leves de scraping e busca na web que funcionam em qualquer lugar."
og:title: "Guia de início rápido do Hono | Firecrawl"
og:description: "Use o Firecrawl com Hono para criar APIs leves de scraping e busca na web que funcionam em qualquer lugar."
---

## Pré-requisitos {#prerequisites}

* Node.js 18+, Bun ou Deno
* Uma chave de API do Firecrawl — [obtenha uma gratuitamente](https://www.firecrawl.dev/app/api-keys)

## Configuração {#setup}

```bash
npm install hono firecrawl
```

Adicione sua chave de API ao `.env`:

```bash
FIRECRAWL_API_KEY=fc-YOUR-API-KEY
```

## Fazer uma busca na web {#search-the-web}

```typescript
import { Hono } from "hono";
import { Firecrawl } from "firecrawl";

const app = new Hono();
const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

app.post("/search", async (c) => {
  const { query } = await c.req.json();
  const results = await firecrawl.search(query, { limit: 5 });
  return c.json(results);
});

export default app;
```

## Fazer o scraping de uma página {#scrape-a-page}

```typescript
app.post("/scrape", async (c) => {
  const { url } = await c.req.json();
  const result = await firecrawl.scrape(url);
  return c.json(result);
});
```

## Interaja com uma página {#interact-with-a-page}

Use `interact` para controlar uma sessão ativa do navegador — clique em botões, preencha formulários e extraia conteúdo dinâmico.

```typescript
app.post("/interact", async (c) => {
  const { url } = await c.req.json();

  const result = await firecrawl.scrape(url, { formats: ['markdown'] });
  const scrapeId = result.metadata?.scrapeId;

  await firecrawl.interact(scrapeId, { prompt: 'Search for iPhone 16 Pro Max' });
  const response = await firecrawl.interact(scrapeId, { prompt: 'Click on the first result and tell me the price' });

  await firecrawl.stopInteraction(scrapeId);

  return c.json({ output: response.output });
});
```

## Faça deploy em qualquer lugar {#deploy-anywhere}

O Hono roda em vários runtimes. Para Cloudflare Workers, passe a chave de API do binding de ambiente:

```typescript
import { Hono } from "hono";
import { Firecrawl } from "firecrawl";

type Bindings = { FIRECRAWL_API_KEY: string };
const app = new Hono<{ Bindings: Bindings }>();

app.post("/search", async (c) => {
  const firecrawl = new Firecrawl({ apiKey: c.env.FIRECRAWL_API_KEY });
  const { query } = await c.req.json();
  const results = await firecrawl.search(query, { limit: 5 });
  return c.json(results);
});

export default app;
```

## Próximos passos {#next-steps}

<CardGroup cols={2}>
  <Card title="Documentação de scraping" icon="file-lines" href="/pt-BR/features/scrape">
    Todas as opções de scraping, incluindo formatos, ações e proxies
  </Card>

  <Card title="Documentação de busca" icon="magnifying-glass" href="/pt-BR/features/search">
    Faça uma busca na web e obtenha o conteúdo completo da página
  </Card>

  <Card title="Documentação de interação" icon="hand-pointer" href="/pt-BR/features/interact">
    Clique, preencha formulários e extraia conteúdo dinâmico
  </Card>

  <Card title="Referência do SDK de Node" icon="node" href="/pt-BR/sdks/node">
    Referência completa do SDK com rastreamento, mapeamento, extração em lote e muito mais
  </Card>
</CardGroup>