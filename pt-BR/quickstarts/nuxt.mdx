---
title: "Nuxt"
description: "Use o Firecrawl com Nuxt para fazer scraping, pesquisar e interagir com dados da web na sua aplicação Vue."
og:title: "Guia de início rápido do Nuxt | Firecrawl"
og:description: "Use o Firecrawl com Nuxt para fazer scraping, pesquisar e interagir com dados da web na sua aplicação Vue."
---

## Pré-requisitos {#prerequisites}

* Projeto com Nuxt 3+
* Uma chave de API da Firecrawl — [obtenha uma grátis](https://www.firecrawl.dev/app/api-keys)

## Instale o SDK {#install-the-sdk}

```bash
npm install firecrawl
```

Adicione sua chave de API ao arquivo `.env`:

```bash
FIRECRAWL_API_KEY=fc-YOUR-API-KEY
```

## Faça uma busca na web {#search-the-web}

Crie `server/api/search.post.ts`:

```typescript
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({
  apiKey: process.env.FIRECRAWL_API_KEY,
});

export default defineEventHandler(async (event) => {
  const { query } = await readBody(event);
  const results = await firecrawl.search(query, { limit: 5 });
  return results;
});
```

Chame-o em um componente Vue:

```vue
<script setup>
const query = ref("");
const { data, execute } = useFetch("/api/search", {
  method: "POST",
  body: { query },
  immediate: false,
});
</script>

<template>
  <div>
    <input v-model="query" placeholder="Fazer uma busca na web..." />
    <button @click="execute()">Buscar</button>
    <ul v-if="data?.web">
      <li v-for="result in data.web" :key="result.url">
        <a :href="result.url">{{ result.title }}</a>
      </li>
    </ul>
  </div>
</template>
```

## Fazer scraping de uma página {#scrape-a-page}

Crie `server/api/scrape.post.ts`:

```typescript
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({
  apiKey: process.env.FIRECRAWL_API_KEY,
});

export default defineEventHandler(async (event) => {
  const { url } = await readBody(event);
  const result = await firecrawl.scrape(url);
  return result;
});
```

Chame-o em um componente Vue:

```vue
<script setup>
const url = ref("https://example.com");
const { data, execute } = useFetch("/api/scrape", {
  method: "POST",
  body: { url },
  immediate: false,
});
</script>

<template>
  <div>
    <input v-model="url" placeholder="Enter URL" />
    <button @click="execute()">Scrape</button>
    <pre v-if="data">{{ data.markdown }}</pre>
  </div>
</template>
```

## Interagir com uma página {#interact-with-a-page}

Crie `server/api/interact.post.ts`:

```typescript
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({
  apiKey: process.env.FIRECRAWL_API_KEY,
});

export default defineEventHandler(async (event) => {
  const result = await firecrawl.scrape("https://www.amazon.com", {
    formats: ["markdown"],
  });

  const scrapeId = result.metadata?.scrapeId;

  await firecrawl.interact(scrapeId, {
    prompt: "Search for iPhone 16 Pro Max",
  });

  const response = await firecrawl.interact(scrapeId, {
    prompt: "Click on the first result and tell me the price",
  });

  await firecrawl.stopInteraction(scrapeId);

  return { output: response.output };
});
```

## Próximos passos {#next-steps}

<CardGroup cols={2}>
  <Card title="Documentação de scraping" icon="file-lines" href="/pt-BR/features/scrape">
    Todas as opções de scraping, incluindo formatos, ações e proxies
  </Card>

  <Card title="Documentação de busca" icon="magnifying-glass" href="/pt-BR/features/search">
    Faça uma busca na web e obtenha o conteúdo completo da página
  </Card>

  <Card title="Documentação de interação" icon="hand-pointer" href="/pt-BR/features/interact">
    Clique, preencha formulários e extraia conteúdo dinâmico
  </Card>

  <Card title="Referência do SDK de Node" icon="node" href="/pt-BR/sdks/node">
    Referência completa do SDK com rastreamento, map, extração em lote e muito mais
  </Card>
</CardGroup>