---
title: "Guia de início rápido com Go"
description: "Chame a API do Firecrawl diretamente a partir de Go."
---

<div id="overview">
  ## Visão geral
</div>

Use a biblioteca padrão do Go para fazer uma requisição de scraping ao Firecrawl por HTTP.

<div id="prerequisites">
  ## Pré-requisitos
</div>

* Uma chave de API do Firecrawl
* Go 1.20+

<div id="install">
  ## Instalação
</div>

Nenhum pacote extra é necessário; este exemplo usa a biblioteca padrão.

<div id="configure-your-api-key">
  ## Configure sua chave de API
</div>

Defina sua chave de API como uma variável de ambiente antes de executar seu programa.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Envie sua primeira requisição
</div>

```go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
    "os"
)

func main() {
    payload := map[string]any{
        "url": "https://example.com",
        "formats": []string{"markdown"},
    }

    body, _ := json.Marshal(payload)
    req, _ := http.NewRequest("POST", "https://api.firecrawl.dev/v2/scrape", bytes.NewReader(body))
    req.Header.Set("Authorization", "Bearer "+os.Getenv("FIRECRAWL_API_KEY"))
    req.Header.Set("Content-Type", "application/json")

    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    var result map[string]any
    _ = json.NewDecoder(resp.Body).Decode(&result)
    fmt.Println(result)
}
```

<div id="complete-example">
  ## Exemplo completo
</div>

```go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
    "os"
)

func main() {
    payload := map[string]any{
        "url": "https://example.com",
        "formats": []string{"markdown", "html"},
    }

    body, _ := json.Marshal(payload)
    req, _ := http.NewRequest("POST", "https://api.firecrawl.dev/v2/scrape", bytes.NewReader(body))
    req.Header.Set("Authorization", "Bearer "+os.Getenv("FIRECRAWL_API_KEY"))
    req.Header.Set("Content-Type", "application/json")

    resp, err := http.DefaultClient.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    raw, _ := io.ReadAll(resp.Body)
    fmt.Println(string(raw))
}
```

<div id="next-steps">
  ## Próximos passos
</div>

* [endpoint /scrape](/pt-BR/api-reference/endpoint/scrape)