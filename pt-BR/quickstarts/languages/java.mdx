---
title: "Guia de início rápido do SDK Java"
description: "Faça scraping da sua primeira URL com o SDK Java do Firecrawl."
---

<div id="overview">
  ## Visão geral
</div>

Use o SDK Java oficial do Firecrawl para chamar a API v2 em Java.

<div id="prerequisites">
  ## Pré-requisitos
</div>

* Uma chave de API do Firecrawl
* Java 11+
* Maven ou Gradle

<div id="install">
  ## Instalação
</div>

```gradle
implementation("com.firecrawl:firecrawl-java:1.1.1")
```

<div id="configure-your-api-key">
  ## Configure sua chave de API
</div>

Defina sua chave de API como uma variável de ambiente antes de executar seu app.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Envie sua primeira requisição
</div>

```java
import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import java.util.List;

FirecrawlClient client = FirecrawlClient.fromEnv();

Document doc = client.scrape(
    "https://example.com",
    ScrapeOptions.builder().formats(List.of("markdown")).build()
);

System.out.println(doc.getMarkdown());
```

<div id="complete-example">
  ## Exemplo completo
</div>

```java
import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        FirecrawlClient client = FirecrawlClient.fromEnv();
        Document doc = client.scrape(
            "https://example.com",
            ScrapeOptions.builder().formats(List.of("markdown", "html")).build()
        );

                    if (doc.getMetadata() != null) {
                        System.out.println(doc.getMetadata().get("title"));
                    }
        System.out.println(doc.getMarkdown());
    }
}
```

<div id="next-steps">
  ## Próximos passos
</div>

* [Endpoint de scraping](/pt-BR/api-reference/endpoint/scrape)
* [SDK Java](/pt-BR/sdks/java)