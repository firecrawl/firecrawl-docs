---
title: "Guia de início rápido do .NET"
description: "Acesse a API do Firecrawl diretamente pelo .NET."
---

<div id="overview">
  ## Visão geral
</div>

Use o HttpClient para enviar uma solicitação de scraping do Firecrawl em .NET.

<div id="prerequisites">
  ## Pré-requisitos
</div>

* Uma chave de API do Firecrawl
* .NET 6+

<div id="install">
  ## Instalação
</div>

Não é necessário nenhum pacote extra; este exemplo usa HttpClient.

<div id="configure-your-api-key">
  ## Configure a chave de API
</div>

Defina a chave de API como uma variável de ambiente para o processo.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Faça sua primeira requisição
</div>

```csharp
using System.Net.Http.Headers;
using System.Net.Http.Json;

var apiKey = Environment.GetEnvironmentVariable("FIRECRAWL_API_KEY");
var http = new HttpClient();
http.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", apiKey);

var payload = new { url = "https://example.com", formats = new[] { "markdown" } };
var response = await http.PostAsJsonAsync("https://api.firecrawl.dev/v2/scrape", payload);
var body = await response.Content.ReadAsStringAsync();

Console.WriteLine(body);
```

<div id="complete-example">
  ## Exemplo completo
</div>

```csharp
using System.Net.Http.Headers;
using System.Net.Http.Json;

var apiKey = Environment.GetEnvironmentVariable("FIRECRAWL_API_KEY");
var http = new HttpClient();
http.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", apiKey);

var payload = new { url = "https://example.com", formats = new[] { "markdown", "html" } };
var response = await http.PostAsJsonAsync("https://api.firecrawl.dev/v2/scrape", payload);
response.EnsureSuccessStatusCode();

var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

<div id="next-steps">
  ## Próximos passos
</div>

* [Endpoint de scraping](/pt-BR/api-reference/endpoint/scrape)