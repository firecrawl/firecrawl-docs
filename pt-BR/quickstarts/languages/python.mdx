---
title: "Guia de início rápido em Python"
description: "Faça o scraping da sua primeira URL com o SDK Python do Firecrawl."
---

<div id="overview">
  ## Visão geral
</div>

Use o SDK Python oficial do Firecrawl para buscar dados de uma URL e retornar um resultado estruturado.

<div id="prerequisites">
  ## Pré-requisitos
</div>

* Uma chave de API do Firecrawl
* Python 3.8+
* pip

<div id="install">
  ## Instalação
</div>

```bash
pip install firecrawl-py
```

<div id="configure-your-api-key">
  ## Configure sua chave de API
</div>

Mantenha sua chave de API em uma variável de ambiente do servidor.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Faça sua primeira requisição
</div>

```python
import os
from firecrawl import Firecrawl

client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])

doc = client.scrape("https://example.com", formats=["markdown"])
print(doc.markdown)
```

<div id="complete-example">
  ## Exemplo completo
</div>

```python
import os
from firecrawl import Firecrawl

def main() -> None:
    client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])
    doc = client.scrape("https://example.com", formats=["markdown", "html"])
    print(doc.metadata.title if doc.metadata else "")
    print(doc.markdown)

if __name__ == "__main__":
    main()
```

<div id="next-steps">
  ## Próximos passos
</div>

* [Endpoint de scraping](/pt-BR/api-reference/endpoint/scrape)
* [SDK Python](/pt-BR/sdks/python)