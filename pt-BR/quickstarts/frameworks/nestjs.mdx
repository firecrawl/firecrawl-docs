---
title: "Guia de início rápido do NestJS"
description: "Chame o Firecrawl em um controller do NestJS."
---

<div id="overview">
  ## Visão geral
</div>

Integre o Firecrawl a um serviço do NestJS e exponha-o por meio de um controller.

<div id="prerequisites">
  ## Pré-requisitos
</div>

* Uma chave de API do Firecrawl
* Aplicativo NestJS
* Node.js 18+

<div id="install">
  ## Instalação
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-environment">
  ## Configure o ambiente
</div>

Disponibilize a chave de API como uma variável de ambiente acessível apenas no servidor.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Adicione uma rota ao servidor
</div>

src/firecrawl/firecrawl.service.ts

```ts
import { Injectable } from '@nestjs/common';
import Firecrawl from '@mendable/firecrawl-js';

@Injectable()
export class FirecrawlService {
  private readonly client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

  async scrape(url: string) {
    return this.client.scrape(url, { formats: ['markdown'] });
  }
}
```

<div id="call-it-from-your-app">
  ## Use-o no seu aplicativo
</div>

```ts
// src/firecrawl/firecrawl.controller.ts
import { Body, Controller, Post } from '@nestjs/common';
import { FirecrawlService } from './firecrawl.service';

@Controller('api/firecrawl')
export class FirecrawlController {
  constructor(private readonly firecrawl: FirecrawlService) {}

  @Post()
  async create(@Body('url') url: string) {
    const doc = await this.firecrawl.scrape(url);
    return { markdown: doc.markdown };
  }
}
```

<div id="notes">
  ## Notas
</div>

* Registre `FirecrawlService` e `FirecrawlController` em um módulo.
* Mantenha a chave de API fora do código cliente.

<div id="next-steps">
  ## Próximos passos
</div>

* [Endpoint de scraping](/pt-BR/api-reference/endpoint/scrape)
* [SDK para Node](/pt-BR/sdks/node)