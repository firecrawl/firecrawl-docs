---
title: "Guia de início rápido do Remix"
description: "Chame o Firecrawl usando uma action do Remix."
---

<div id="overview">
  ## Visão geral
</div>

Crie uma rota de `action` no Remix para chamar o Firecrawl no lado do servidor.

<div id="prerequisites">
  ## Pré-requisitos
</div>

* Uma chave da API do Firecrawl
* Um app Remix
* Node.js 18+

<div id="install">
  ## Instalação
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-environment">
  ## Configure o ambiente
</div>

Armazene a chave de API no `.env` ou no ambiente de implantação.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Adicione uma rota de servidor
</div>

app/routes/api.firecrawl.ts

```ts
import { json } from '@remix-run/node';
import Firecrawl from '@mendable/firecrawl-js';

export const action = async ({ request }) => {
  const { url } = await request.json();
  const client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

  const doc = await client.scrape(url, { formats: ['markdown'] });
  return json({ markdown: doc.markdown });
};
```

<div id="call-it-from-your-app">
  ## Faça a chamada no seu aplicativo
</div>

```ts
const response = await fetch('/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.markdown);
```

<div id="notes">
  ## Notas
</div>

* Use actions ou loaders do Remix para manter as chamadas ao Firecrawl no servidor.

<div id="next-steps">
  ## Próximos passos
</div>

* [endpoint /scrape](/pt-BR/api-reference/endpoint/scrape)
* [SDK de Node](/pt-BR/sdks/node)