---
title: "Guia de início rápido para Rails"
description: "Chame o Firecrawl em um controller do Rails."
---

<div id="overview">
  ## Visão geral
</div>

Use o Net::HTTP do Ruby para chamar a API do Firecrawl a partir de um controller.

<div id="prerequisites">
  ## Pré-requisitos
</div>

* Uma chave de API do Firecrawl
* Aplicação Rails
* Ruby 3.0+

<div id="install">
  ## Instalação
</div>

Nenhum pacote extra é necessário; use a biblioteca padrão do Ruby.

<div id="configure-environment">
  ## Configure o ambiente
</div>

Defina a chave de API no ambiente do Rails.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Adicione uma rota ao servidor
</div>

app/controllers/firecrawl&#95;controller.rb

```rb
require 'net/http'
require 'uri'
require 'json'

class FirecrawlController < ApplicationController
  protect_from_forgery with: :null_session

  def create
    uri = URI('https://api.firecrawl.dev/v2/scrape')
    request = Net::HTTP::Post.new(uri)
    request['Authorization'] = "Bearer #{ENV.fetch('FIRECRAWL_API_KEY')}"
    request['Content-Type'] = 'application/json'
    request.body = { url: params[:url], formats: ['markdown'] }.to_json

    response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) do |http|
      http.request(request)
    end

    render json: JSON.parse(response.body)
  end
end
```

<div id="call-it-from-your-app">
  ## Faça a chamada no seu aplicativo
</div>

```rb
require 'net/http'
require 'json'

uri = URI('http://localhost:3000/api/firecrawl')
response = Net::HTTP.post(uri, { url: 'https://example.com' }.to_json, 'Content-Type' => 'application/json')

puts JSON.parse(response.body)
```

<div id="notes">
  ## Notas
</div>

* Adicione `post '/api/firecrawl', to: 'firecrawl#create'` ao `config/routes.rb`.

<div id="next-steps">
  ## Próximos passos
</div>

* [Endpoint de scraping](/pt-BR/api-reference/endpoint/scrape)