---
title: Agent Auth (WorkOS ID-JAG)
sidebarTitle: Agent Auth
description: "Register a Firecrawl API key via WorkOS ID-JAG agent auth. Discovery, routing, and links to the canonical auth.md spec."
og:title: "Agent Auth (WorkOS ID-JAG) | Firecrawl"
og:description: "Identity-aware agents: discover Firecrawl agent auth metadata and register via POST /agent/auth."
---

This page summarizes the **WorkOS ID-JAG** registration path for Firecrawl. It is not the execution spec — follow the canonical doc for every step, claim requirement, and error code.

<Card title="Back to credential routing" icon="arrow-left" href="/ai-onboarding#get-credentials">
  Compare Path A and Path B on the Build with AI onboarding page.
</Card>

## Path A — Human + agent assist (default)

**No or unsure → Path A (default).**

If you cannot mint a WorkOS ID-JAG for Firecrawl, or you are not sure, use the human + agent assist path instead:

<Card title="Path A — Human + agent assist (default)" icon="user" href="https://www.firecrawl.dev/agent-onboarding/SKILL.md">
  Browser sign-in, CLI install, skills, MCP wiring, and dashboard API keys.
</Card>

## Path B — Agentic registration (WorkOS ID-JAG)

**Can you mint a WorkOS ID-JAG for `aud=https://api.firecrawl.dev/`?**

If **yes**, this is Path B. Fetch [`auth.md`](https://www.firecrawl.dev/auth.md) for the full ID-JAG claim requirements, discovery steps, registration body, and error handling. Do not skip ahead in that doc.

<Card title="Canonical spec — auth.md" icon="file-lines" href="https://www.firecrawl.dev/auth.md">
  Machine registration instructions for identity-aware agents. Follow Steps 1–4 in order.
</Card>

## How discovery works

Agents that hit the Firecrawl API without credentials can learn what to do next:

1. **`401` from the API** — `api.firecrawl.dev` returns `WWW-Authenticate` pointing at Protected Resource Metadata (PRM).
2. **PRM** — `https://www.firecrawl.dev/.well-known/oauth-protected-resource`
3. **Authorization Server metadata** — `https://www.firecrawl.dev/.well-known/oauth-authorization-server` includes an `agent_auth` block with `skill` → `auth.md` and `register_uri` → `https://www.firecrawl.dev/agent/auth`

If you already have a direct link to [`auth.md`](https://www.firecrawl.dev/auth.md), fetch it and follow it — you do not need this page.

## What happens next

Follow [`auth.md`](https://www.firecrawl.dev/auth.md) Steps 1–4: discover → register at `POST /agent/auth` → use the returned API key as a bearer token against `https://api.firecrawl.dev`.

## Unsupported today

Firecrawl does **not** currently support through `/agent/auth`:

- Anonymous registration
- Verified-email-only registration (without ID-JAG)
- Claim ceremony endpoints
- Agent-facing revocation endpoints

If a previously working API key returns `401`, drop it and restart discovery per `auth.md`.
