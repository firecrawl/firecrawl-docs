---
title: "検索"
openapi: "/ja/api-reference/v2-openapi.json POST /search"
---

<div id="whats-new-in-v2">
  ## v2の新機能
</div>

<div id="search-multiple-sources">
  ### 複数のソースを検索
</div>

ウェブ、画像、ニュースをまとめて検索:

```json
{
  "query": "firecrawl web scraping",
  "sources": [
    {"type": "web", "tbs": "qdr:w", "location": "US"},
    {"type": "images"},
    {"type": "news"}
  ]
}
```

<div id="response-format-changed">
  ### レスポンス形式の変更
</div>

v1: 結果はフラットなリスト。v2: ソースタイプごとに整理。

```json
{
  "success": true,
  "data": {
    "web": [/* web results */],
    "images": [/* 画像結果 */],
    "news": [/* news results */]
  }
}
```


<div id="new-features">
  ### 新機能
</div>

- 時間範囲でのフィルタリング（「過去 1 週間」「過去 1 か月」）
- 特定の国/地域をターゲットに指定
- アルファ版では結果数は最大 500 件に制限

search エンドポイントは、ウェブ検索（search）と Firecrawl のスクレイピング機能を組み合わせて、任意のクエリに対してページ全体のコンテンツを返します。

各検索結果の完全な Markdown コンテンツを取得するには、`scrapeOptions` に `formats: [{"type": "markdown"}]` を含めてください。指定しない場合、デフォルトでは search の結果（url、title、description）が返されます。要約されたコンテンツが必要な場合は、`{"type": "summary"}` のような別のフォーマットも使用できます。

<div id="supported-query-operators">
  ## サポートされているクエリ演算子
</div>

検索をより正確に絞り込むための各種クエリ演算子をサポートしています。

| Operator | 機能 | 例 |
---|-|-|
| `""` | テキストを完全一致で検索する | `"Firecrawl"`
| `-` | 特定のキーワードを除外、または他の演算子を否定する | `-bad`, `-site:firecrawl.dev`
| `site:` | 指定したサイトの結果のみを返す | `site:firecrawl.dev`
| `inurl:` | URL に指定した語を含む結果のみを返す | `inurl:firecrawl`
| `allinurl:` | URL に複数の語を含む結果のみを返す | `allinurl:git firecrawl`
| `intitle:` | ページタイトルに指定した語を含む結果のみを返す | `intitle:Firecrawl`
| `allintitle:` | ページタイトルに複数の語を含む結果のみを返す | `allintitle:firecrawl playground`
| `related:` | 特定のドメインに関連する結果のみを返す | `related:firecrawl.dev`

<div id="location-parameter">
  ## location パラメータ
</div>

`location` パラメータを使うと、地域に最適化された検索結果を取得できます。形式: `"string"`。例: `"Germany"`、`"San Francisco,California,United States"`。

利用可能な国と言語の一覧は、[サポート対象ロケーションの完全なリスト](https://firecrawl.dev/search_locations.json)を参照してください。

<div id="time-based-search">
  ## 時間ベースの検索
</div>

`tbs` パラメータを使うと、カスタムの日付範囲を含む期間で検索結果を絞り込めます。詳しい例や対応フォーマットは、[検索機能のドキュメント](https://docs.firecrawl.dev/features/search#time-based-search)を参照してください。