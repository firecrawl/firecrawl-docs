---
title: "FIRE-1 エージェント（ベータ）"
sidebarTitle: "FIRE-1（ベータ）"
description: "FIRE-1 は、Web ページをインテリジェントに回遊・操作できる AI エージェントです"
og:title: "FIRE-1 AI Agent (Beta) | Firecrawl"
og:description: "FIRE-1 は、Web ページをインテリジェントに回遊・操作できる AI エージェントです"
icon: "robot"
---

FIRE-1 は、賢い Web 回遊と操作によって Firecrawl のスクレイピング機能を拡張する AI エージェントです。ページネーションの処理、ブラウザのアクション制御、複雑なサイト構造の横断に対応し、従来のスクレイピングを超える包括的なデータ抽出を実現します。

<div id="what-fire-1-can-do">
  ### FIRE-1 ができること:
</div>

- ページ分割されたコンテンツを自動でたどります。
- ボタン、リンク、入力欄、動的要素を操作します。
- 複数ページにわたる高度な抽出タスクを実行します。

<div id="enabling-fire-1-agent">
  ## FIRE-1 エージェントの有効化
</div>

FIRE-1 エージェントを有効化するには、`scrape` または `extract` エンドポイント向けの API リクエストのペイロードに `agent` オブジェクトを含めます。

`agent` オブジェクトには次のプロパティがあります:

*   `model` (string, 任意): 使用する AI モデルを指定します。指定がない場合は `FIRE-1` がデフォルトです。現在利用可能なモデルは `FIRE-1` のみです。
*   `prompt` (string, `scrape` エンドポイントでは必須): AI エージェントへの指示。探すべきコンテンツやサイトの操作方法（例: ページネーションの扱い、クリックするボタンなど）を記述します。`/extract` では `prompt` パラメータで渡されたプロンプトが使用されます。

<div id="using-fire-1-with-the-scrape-endpoint">
  ### /scrape エンドポイントでの FIRE-1 の利用
</div>

`/v1/scrape` エンドポイントで FIRE-1（エージェント）を使うと、最終的なコンテンツをスクレイプする前にインテリジェントなナビゲーションを実行できます。

**例（cURL）：**

```bash
curl -X POST https://api.firecrawl.dev/v1/scrape \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer <YOUR_API_KEY>' \
    -d '{
      "url": "https://example.com/products?page=1",
      "formats": ["markdown"],
      "agent": {
        "model": "FIRE-1",
        "prompt": "「次へ」ボタンが無効になるまでクリックして商品一覧を順に移動し、訪れた各ページのコンテンツをスクレイピングしてください。"
      }
    }'
```

この例では、FIRE-1（エージェント）に、最終的なスクレイプを行う前に商品リストをページ送り（ページネーション）するよう指示しています。


<div id="using-fire-1-with-the-extract-endpoint">
  ### FIRE-1 を /extract エンドポイントで使う
</div>

同様に、複数ページの移動や要素との対話が必要な複雑な抽出タスクには、`/v1/extract` エンドポイントで FIRE-1（エージェント）を活用できます。

**例（cURL）：**

```bash
curl -X POST https://api.firecrawl.dev/v1/extract \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer <YOUR_API_KEY>' \
    -d '{
      "urls": ["https://example-forum.com/topic/123"],
      "prompt": "このフォーラムのスレッドから、すべてのユーザーコメントを抽出してください。",
      "schema": {
        "type": "object",
        "properties": {
          "comments": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "author": {"type": "string"},
                "comment_text": {"type": "string"}
              },
              "required": ["author", "comment_text"]
            }
          }
        },
        "required": ["comments"]
      },
      "agent": {
        "model": "FIRE-1"
      }
    }'
```

ここでは、エージェントが提供されたスキーマとプロンプトに基づく抽出を開始する前に、「Load More Comments」ボタンを操作して、ページ上のすべてのコメントが読み込まれていることを確実にします。
**注:** FIRE-1（エージェント）は強力ですが、ナビゲーション指示の複雑さや操作するページ数によっては、クレジット消費が増える場合があります。


<div id="meet-fire-1-intelligent-navigation-and-interaction">
  ### FIRE-1 の紹介: インテリジェントなナビゲーションとインタラクション
</div>

![FIRE-1 Agent Visualization](/launch-week/lw3-d2-3.webp)

FIRE-1 は Firecrawl に高度な知能をもたらし、複雑なウェブサイト構造の横断、ページネーションの処理、動的コンテンツとの対話などを通じてスクレイピングを強化します。この強力な AI エージェントにより、従来のスクレイピング手法を超えた網羅的なデータ抽出が可能になります。

<div id="what-fire-1-can-do">
  ### FIRE-1でできること:
</div>

- ページ分割されたコンテンツを自動で巡回します。
- ボタンやリンクなどの動的要素を操作します。
- 複数ページにまたがる高度なデータ抽出を実行します。

<div id="how-to-enable-fire-1">
  ## FIRE-1 を有効化する方法
</div>

FIRE-1 の有効化は簡単です。/scrape エンドポイントまたは /extract エンドポイントへの API リクエストに、agent オブジェクトを含めるだけです。

```json
"agent": {
  "model": "FIRE-1",
  "prompt": "ここに詳しいナビゲーション手順を入力してください。"
}
```

*注記:* `prompt` フィールドはスクレイプリクエストで必須で、FIRE-1 にウェブページとの具体的なインタラクション方法を正確に指示します。


<div id="example-usage-with-scrape-endpoint">
  ## /scrape エンドポイントの使用例
</div>

FIRE-1 を /scrape エンドポイントと併用して、商品一覧をページ送りしながら取得する簡単な例を紹介します。

```bash
curl -X POST https://api.firecrawl.dev/v1/scrape \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <YOUR_API_KEY>' \
  -d '{
    "url": "https://example.com/products?page=1",
    "formats": ["markdown"],
    "agent": {
      "model": "FIRE-1",
      "prompt": "「次のページ」ボタンが無効になるまでクリックして商品一覧をたどり、各ページをスクレイピングしてください。"
    }
  }'
```

このシナリオでは、FIRE-1（エージェント）がページネーションを自動で適切にたどり、すべての商品を取得します。


<div id="considerations">
  ## 留意事項
</div>

- FIRE-1 の利用は、タスクの複雑さや操作の深さに応じて、消費クレジットが増える場合があります。
- 結果と効率を最大化するため、プロンプトで FIRE-1 に明確な指示を与えてください。

<div id="start-using-fire-1-today">
  ## 今日から FIRE-1 を使い始めましょう
</div>

今すぐ、ウェブスクレイピングの未来を体験しましょう。

- **FIRE-1 を試す:** スクレイピングや抽出のワークフローに、インテリジェントなナビゲーションを組み込みましょう。
- **ドキュメントを見る:** [網羅的なドキュメント](https://docs.firecrawl.dev/agents/fire-1)で詳しく学べます。
- **お困りですか？** [Discord コミュニティ](https://discord.gg/S7Enyh9Abh)に参加するか、[help@firecrawl.com](mailto:help@firecrawl.com) までメールでご連絡ください。
**AI で強化されたスクレイピングを活用する準備はできましたか？** [Firecrawl にサインアップ](https://firecrawl.dev/signup)して、今日から FIRE-1 を始めましょう。
