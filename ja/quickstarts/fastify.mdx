---
title: "Fastify"
description: "Firecrawl と Fastify を使って、高性能なWebスクレイピング API と検索 API を構築します。"
og:title: "Fastify クイックスタート | Firecrawl"
og:description: "Firecrawl と Fastify を使って、高性能なWebスクレイピング API と検索 API を構築します。"
---

## 前提条件 {#prerequisites}

* Node.js 18+
* Firecrawl APIキー — [無料で取得できます](https://www.firecrawl.dev/app/api-keys)

## セットアップ {#setup}

```bash
npm install fastify firecrawl
```

`.env` にAPIキーを追加します：

```bash
FIRECRAWL_API_KEY=fc-YOUR-API-KEY
```

## ウェブを検索 {#search-the-web}

```javascript
import Fastify from "fastify";
import { Firecrawl } from "firecrawl";

const fastify = Fastify({ logger: true });
const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

fastify.post("/search", async (request) => {
  const { query } = request.body;
  return firecrawl.search(query, { limit: 5 });
});

fastify.listen({ port: 3000 });
```

## ページをスクレイピングする {#scrape-a-page}

```javascript
fastify.post("/scrape", async (request) => {
  const { url } = request.body;
  return firecrawl.scrape(url);
});
```

## Interact でページを操作する {#interact-with-a-page}

interact を使用してライブのブラウザセッションを操作し、ボタンのクリック、フォームへの入力、動的コンテンツの抽出を行えます。

```javascript
fastify.post("/interact", async (request) => {
  const { url } = request.body;

  const result = await firecrawl.scrape(url, { formats: ['markdown'] });
  const scrapeId = result.metadata?.scrapeId;

  await firecrawl.interact(scrapeId, { prompt: 'iPhone 16 Pro Maxを検索する' });
  const response = await firecrawl.interact(scrapeId, { prompt: '最初の結果をクリックして価格を教えてください' });

  await firecrawl.stopInteraction(scrapeId);

  return { output: response.output };
});
```

## Fastify プラグインとして {#as-a-fastify-plugin}

複数のルートで再利用できるよう、クライアントをプラグイン化します。

```javascript
import fp from "fastify-plugin";
import { Firecrawl } from "firecrawl";

export default fp(async function firecrawlPlugin(fastify) {
  const client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });
  fastify.decorate("firecrawl", client);
});
```

プラグインを登録したら、任意のルートで `fastify.firecrawl` を使用できます:

```javascript
fastify.register(firecrawlPlugin);

fastify.post("/search", async function (request) {
  const { query } = request.body;
  return this.firecrawl.search(query, { limit: 5 });
});
```

## 試す {#test-it}

```bash
curl -X POST http://localhost:3000/search \
  -H "Content-Type: application/json" \
  -d '{"query": "firecrawl web scraping"}'
```

## 次のステップ {#next-steps}

<CardGroup cols={2}>
  <Card title="スクレイピングのドキュメント" icon="file-lines" href="/ja/features/scrape">
    フォーマット、アクション、プロキシなど、スクレイピングのオプションを網羅
  </Card>

  <Card title="検索ドキュメント" icon="magnifying-glass" href="/ja/features/search">
    ウェブを検索し、ページ全体のコンテンツを取得
  </Card>

  <Card title="Interact ドキュメント" icon="hand-pointer" href="/ja/features/interact">
    クリック、フォーム入力、動的コンテンツの抽出
  </Card>

  <Card title="Node SDK リファレンス" icon="node" href="/ja/sdks/node">
    クロール、map、バッチスクレイプなどを含む完全な SDK リファレンス
  </Card>
</CardGroup>