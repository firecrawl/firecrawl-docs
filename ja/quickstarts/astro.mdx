---
title: "Astro"
description: "Astro と Firecrawl を組み合わせて、コンテンツ重視のサイトで Web データをスクレイピング、検索、Interact できます。"
og:title: "Astro クイックスタート | Firecrawl"
og:description: "Astro と Firecrawl を組み合わせて、コンテンツ重視のサイトで Web データをスクレイピング、検索、Interact できます。"
---

## 前提条件 {#prerequisites}

* SSR が有効化された Astro プロジェクト (`output: "server"` または `"hybrid"`)
* Firecrawl APIキー — [無料で取得できます](https://www.firecrawl.dev/app/api-keys)

## SDKをインストール {#install-the-sdk}

```bash
npm install firecrawl
```

`.env` に API key を追加します:

```bash
FIRECRAWL_API_KEY=fc-YOUR-API-KEY
```

## Webを検索する {#search-the-web}

`src/pages/api/search.ts` を作成します:

```typescript
import type { APIRoute } from "astro";
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({
  apiKey: import.meta.env.FIRECRAWL_API_KEY,
});

export const POST: APIRoute = async ({ request }) => {
  const { query } = await request.json();
  const results = await firecrawl.search(query, { limit: 5 });
  return new Response(JSON.stringify(results), {
    headers: { "Content-Type": "application/json" },
  });
};
```

または、サーバーサイドでレンダリングされるページ (`src/pages/search.astro`) で、リクエスト時に検索します。

```astro
---
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({
  apiKey: import.meta.env.FIRECRAWL_API_KEY,
});

const query = Astro.url.searchParams.get("q");
let results = [];

if (query) {
  const searchData = await firecrawl.search(query, { limit: 5 });
  results = searchData.web || [];
}
---

<html>
  <body>
    <h1>検索結果</h1>
    {results.length > 0 ? (
      <ul>
        {results.map((r) => (
          <li><a href={r.url}>{r.title}</a></li>
        ))}
      </ul>
    ) : (
      <p>ウェブを検索するには ?q= を指定してください</p>
    )}
  </body>
</html>
```

## ページをスクレイピングする {#scrape-a-page}

`src/pages/api/scrape.ts` を作成します:

```typescript
import type { APIRoute } from "astro";
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({
  apiKey: import.meta.env.FIRECRAWL_API_KEY,
});

export const POST: APIRoute = async ({ request }) => {
  const { url } = await request.json();
  const result = await firecrawl.scrape(url);
  return new Response(JSON.stringify(result), {
    headers: { "Content-Type": "application/json" },
  });
};
```

または、サーバーサイドでレンダリングされるページでリクエスト時にスクレイピングします (`src/pages/scrape.astro`) :

```astro
---
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({
  apiKey: import.meta.env.FIRECRAWL_API_KEY,
});

const target = Astro.url.searchParams.get("url");
let markdown = null;

if (target) {
  const result = await firecrawl.scrape(target);
  markdown = result.markdown;
}
---

<html>
  <body>
    <h1>スクレイピングされたコンテンツ</h1>
    {markdown ? <pre>{markdown}</pre> : <p>ページをスクレイピングするには ?url= を渡してください</p>}
  </body>
</html>
```

## ページとInteractする {#interact-with-a-page}

`src/pages/api/interact.ts` を作成します:

```typescript
import type { APIRoute } from "astro";
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({
  apiKey: import.meta.env.FIRECRAWL_API_KEY,
});

export const POST: APIRoute = async () => {
  const result = await firecrawl.scrape("https://www.amazon.com", {
    formats: ["markdown"],
  });

  const scrapeId = result.metadata?.scrapeId;

  await firecrawl.interact(scrapeId, {
    prompt: "Search for iPhone 16 Pro Max",
  });

  const response = await firecrawl.interact(scrapeId, {
    prompt: "Click on the first result and tell me the price",
  });

  await firecrawl.stopInteraction(scrapeId);

  return new Response(JSON.stringify({ output: response.output }), {
    headers: { "Content-Type": "application/json" },
  });
};
```

## 次のステップ {#next-steps}

<CardGroup cols={2}>
  <Card title="スクレイピング ドキュメント" icon="file-lines" href="/ja/features/scrape">
    フォーマット、アクション、プロキシなど、スクレイピングのオプションをすべて掲載
  </Card>

  <Card title="Search ドキュメント" icon="magnifying-glass" href="/ja/features/search">
    Webを検索し、ページ全体のコンテンツを取得
  </Card>

  <Card title="Interact ドキュメント" icon="hand-pointer" href="/ja/features/interact">
    クリック、フォーム入力、動的コンテンツを抽出
  </Card>

  <Card title="Node SDK リファレンス" icon="node" href="/ja/sdks/node">
    クロール、map、バッチスクレイピングなどを含むSDKの完全なリファレンス
  </Card>
</CardGroup>