---
title: "Node.js クイックスタート"
description: "Firecrawl Node SDK を使って最初の URL をスクレイピングする方法。"
---

<div id="overview">
  ## 概要
</div>

公式のFirecrawl Node SDKを使って、URLをクリーンでLLM対応のコンテンツに変換します。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl のAPIキー
* Node.js 18+
* npm、pnpm、または yarn

<div id="install">
  ## インストール
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-your-api-key">
  ## APIキー を設定する
</div>

APIキー は、サーバーからのみアクセスできる環境変数に保存します。

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## 最初のリクエストを送信する
</div>

```js
import Firecrawl from '@mendable/firecrawl-js';

const app = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

(async () => {
  const doc = await app.scrape('https://example.com', { formats: ['markdown'] });
  console.log(doc.markdown);
})();
```

<div id="complete-example">
  ## 完全な例
</div>

```js
import Firecrawl from '@mendable/firecrawl-js';

async function main() {
  const app = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });
  const doc = await app.scrape('https://example.com', { formats: ['markdown', 'html'] });
  console.log(doc.metadata?.title);
  console.log(doc.markdown);
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
```

<div id="next-steps">
  ## 次のステップ
</div>

* [スクレイピング エンドポイント](/ja/api-reference/endpoint/scrape)
* [Node SDK](/ja/sdks/node)