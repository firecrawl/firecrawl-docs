---
title: "Fastify クイックスタート"
description: "Fastify のルートから Firecrawl を呼び出す方法。"
---

<div id="overview">
  ## 概要
</div>

Fastify サーバーのサーバールートで Firecrawl Node SDK を使用します。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl のAPIキー
* Fastifyアプリ
* Node.js 18+

<div id="install">
  ## インストール
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-environment">
  ## 環境を設定する
</div>

APIキーをサーバーの環境変数に追加します。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## サーバールートを追加
</div>

server.js

```js
import Fastify from 'fastify';
import Firecrawl from '@mendable/firecrawl-js';

const app = Fastify();
const client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

app.post('/api/firecrawl', async (request) => {
  const { url } = request.body as { url: string };
  const doc = await client.scrape(url, { formats: ['markdown'] });
  return { markdown: doc.markdown };
});

app.listen({ port: 3000 });
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```js
const response = await fetch('http://localhost:3000/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.markdown);
```

<div id="notes">
  ## 注意事項
</div>

* APIキーを保護するため、Firecrawlへのリクエストはサーバーサイドで行ってください。

<div id="next-steps">
  ## 次のステップ
</div>

* [スクレイピング エンドポイント](/ja/api-reference/endpoint/scrape)
* [Node SDK](/ja/sdks/node)