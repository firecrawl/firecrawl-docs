---
title: "AWS Lambda クイックスタート"
description: "AWS Lambda 関数から Firecrawl を呼び出す方法。"
---

<div id="overview">
  ## 概要
</div>

Lambdaハンドラーを使用して、Firecrawlへのリクエストを安全にプロキシします。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl APIキー
* AWS Lambda (Node.js 18 ランタイム)

<div id="install">
  ## インストール
</div>

追加のパッケージは不要です。Node.js 18 では `fetch` を使用してください。

<div id="configure-environment">
  ## 環境設定
</div>

Lambda の環境変数に `FIRECRAWL_API_KEY` を設定します。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## サーバールートを追加する
</div>

index.mjs

```js
export const handler = async (event) => {
  const { url } = JSON.parse(event.body || '{}');

  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${process.env.FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  const data = await response.json();
  return {
    statusCode: 200,
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  };
};
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```js
const response = await fetch('https://your-api-id.execute-api.region.amazonaws.com/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事項
</div>

* Lambda エンドポイントを公開するには、API Gateway (または Lambda Function URLs) を設定します。

<div id="next-steps">
  ## 次のステップ
</div>

* [/scrape エンドポイント](/ja/api-reference/endpoint/scrape)