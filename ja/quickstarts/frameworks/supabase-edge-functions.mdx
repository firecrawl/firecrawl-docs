---
title: "Supabase Edge Functions クイックスタート"
description: "Supabase Edge Function から Firecrawl を呼び出す方法。"
---

<div id="overview">
  ## 概要
</div>

Deno ベースの Edge Function を使って、Firecrawl の HTTP API を呼び出します。

<div id="prerequisites">
  ## 前提条件
</div>

* FirecrawlのAPIキー
* Supabaseプロジェクト
* Supabase CLI

<div id="install">
  ## インストール
</div>

追加のパッケージは不要です。Supabase Edge Functions は、fetch が使える Deno 上で動作します。

<div id="configure-environment">
  ## 環境を設定する
</div>

APIキーをSupabaseのシークレットに設定します。

```bash
supabase secrets set FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## サーバールートを追加
</div>

supabase/functions/firecrawl/index.ts

```ts
Deno.serve(async (req) => {
  const { url } = await req.json();
  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${Deno.env.get('FIRECRAWL_API_KEY')}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  return new Response(await response.text(), {
    headers: { 'Content-Type': 'application/json' },
  });
});
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```js
const response = await fetch('https://project.supabase.co/functions/v1/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意点
</div>

* Edge Functions は Deno を使用しているため、Node 専用のパッケージは避けてください。

<div id="next-steps">
  ## 次のステップ
</div>

* [スクレイピングエンドポイント](/ja/api-reference/endpoint/scrape)