---
title: "FastAPI クイックスタート"
description: "FastAPI のルートから Firecrawl を呼び出す方法。"
---

<div id="overview">
  ## 概要
</div>

FastAPI のルートハンドラ内で Firecrawl の Python SDK を使用します。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl のAPIキー
* FastAPI アプリ
* Python 3.8+

<div id="install">
  ## インストール
</div>

```bash
pip install firecrawl-py
```

<div id="configure-environment">
  ## 環境を設定する
</div>

サーバー環境にAPIキーを設定します。

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="add-a-server-route">
  ## サーバーのルートを追加する
</div>

app/main.py

```python
import os
from fastapi import FastAPI
from firecrawl import Firecrawl

app = FastAPI()
client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])

@app.post("/api/firecrawl")
def scrape(body: dict):
    doc = client.scrape(body["url"], formats=["markdown"])
    return {"markdown": doc.markdown}
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```python
import requests

response = requests.post(
    "http://localhost:8000/api/firecrawl",
    json={"url": "https://example.com"},
)

print(response.json().get("markdown"))
```

<div id="notes">
  ## 注意事項
</div>

* HTTP 接続を再利用するため、Firecrawl クライアントは一度だけ初期化してください。

<div id="next-steps">
  ## 次のステップ
</div>

* [スクレイピング エンドポイント](/ja/api-reference/endpoint/scrape)
* [Python SDK](/ja/sdks/python)