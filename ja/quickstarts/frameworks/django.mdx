---
title: "Django クイックスタート"
description: "Django ビューから Firecrawl を呼び出す。"
---

<div id="overview">
  ## 概要
</div>

Django のビューで Firecrawl Python SDK を使い、スクレイピングリクエストをプロキシします。

<div id="prerequisites">
  ## 前提条件
</div>

* FirecrawlのAPIキー
* Djangoアプリ
* Python 3.8+

<div id="install">
  ## インストール
</div>

```bash
pip install firecrawl-py
```

<div id="configure-environment">
  ## 環境を設定する
</div>

Django 環境に APIキー を設定します。

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="add-a-server-route">
  ## サーバーのルートを追加する
</div>

app/views.py

```python
import json
import os
from django.http import JsonResponse
from django.views.decorators.csrf import csrf_exempt
from firecrawl import Firecrawl

client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])

@csrf_exempt
def firecrawl_view(request):
    if request.method != "POST":
        return JsonResponse({"error": "Method not allowed"}, status=405)

    body = json.loads(request.body)
    doc = client.scrape(body["url"], formats=["markdown"])
    return JsonResponse({"markdown": doc.markdown})
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```python
import requests

response = requests.post(
    "http://localhost:8000/api/firecrawl",
    json={"url": "https://example.com"},
)

print(response.json().get("markdown"))
```

<div id="notes">
  ## 注記
</div>

* `/api/firecrawl` を `firecrawl_view` にマッピングする URL ルートを追加します。

<div id="next-steps">
  ## 次のステップ
</div>

* [スクレイピングエンドポイント](/ja/api-reference/endpoint/scrape)
* [Python SDK](/ja/sdks/python)