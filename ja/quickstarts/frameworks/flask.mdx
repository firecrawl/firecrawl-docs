---
title: "Flask クイックスタート"
description: "Flask のルートから Firecrawl を呼び出す方法。"
---

<div id="overview">
  ## 概要
</div>

Flask サーバーのエンドポイントで Firecrawl Python SDK を使用します。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl のAPIキー
* Flask アプリ
* Python 3.8+

<div id="install">
  ## インストール
</div>

```bash
pip install firecrawl-py
```

<div id="configure-environment">
  ## 環境を設定する
</div>

Flask を起動する前に、環境変数に APIキーを設定します。

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="add-a-server-route">
  ## サーバールートを追加
</div>

app.py

```python
import os
from flask import Flask, request, jsonify
from firecrawl import Firecrawl

app = Flask(__name__)
client = Firecrawl(api_key=os.environ["FIRECRAWL_API_KEY"])

@app.post("/api/firecrawl")
def firecrawl():
    url = request.json["url"]
    doc = client.scrape(url, formats=["markdown"])
    return jsonify(markdown=doc.markdown)
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```python
import requests

response = requests.post(
    "http://localhost:5000/api/firecrawl",
    json={"url": "https://example.com"},
)

print(response.json().get("markdown"))
```

<div id="notes">
  ## 注意事項
</div>

* Firecrawl クライアントは一度だけ初期化し、各リクエストで再利用してください。

<div id="next-steps">
  ## 次のステップ
</div>

* [/scrape エンドポイント](/ja/api-reference/endpoint/scrape)
* [Python SDK](/ja/sdks/python)