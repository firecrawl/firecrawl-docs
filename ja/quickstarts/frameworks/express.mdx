---
title: "Express クイックスタート"
description: "Express の API ルートから Firecrawl を呼び出す方法。"
---

<div id="overview">
  ## 概要
</div>

ExpressのサーバールートでFirecrawl Node SDKを使用します。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl APIキー
* Express アプリ
* Node.js 18+

<div id="install">
  ## インストール
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-environment">
  ## 環境を設定
</div>

APIキーを `.env` またはプロセスの環境変数に保存します。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## サーバールートを追加
</div>

server.js

```js
import express from 'express';
import Firecrawl from '@mendable/firecrawl-js';

const app = express();
app.use(express.json());

const client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

app.post('/api/firecrawl', async (req, res) => {
  const { url } = req.body;
  const doc = await client.scrape(url, { formats: ['markdown'] });
  res.json({ markdown: doc.markdown });
});

app.listen(3000, () => console.log('Server listening on :3000'));
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```js
const response = await fetch('http://localhost:3000/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.markdown);
```

<div id="notes">
  ## 注意事項
</div>

* Firecrawl の呼び出しはサーバー側で行い、APIキーはクライアントバンドルに決して含めないでください。

<div id="next-steps">
  ## 次のステップ
</div>

* [/scrape エンドポイント](/ja/api-reference/endpoint/scrape)
* [Node SDK](/ja/sdks/node)