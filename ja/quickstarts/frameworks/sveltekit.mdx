---
title: "SvelteKit クイックスタート"
description: "SvelteKit のサーバーエンドポイントから Firecrawl を呼び出す方法。"
---

<div id="overview">
  ## 概要
</div>

サーバー側で Firecrawl を呼び出すには、SvelteKit の `+server.ts` サーバーエンドポイントを使用します。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl APIキー
* SvelteKitアプリ
* Node.js 18+

<div id="install">
  ## インストール
</div>

追加のパッケージは不要です。サーバーエンドポイントで `fetch` を使用してください。

<div id="configure-environment">
  ## 環境を設定する
</div>

APIキーを`.env`に保存し、`$env/static/private`から読み込みます。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## サーバールートを追加
</div>

src/routes/api/firecrawl/+server.ts

```ts
import { json } from '@sveltejs/kit';
import { FIRECRAWL_API_KEY } from '$env/static/private';

export async function POST({ request }) {
  const { url } = await request.json();
  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  const data = await response.json();
  return json(data);
}
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```ts
const response = await fetch('/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意
</div>

* APIキー は非公開の環境変数に保存し、`$env/static/public` 経由で公開しないでください。

<div id="next-steps">
  ## 次のステップ
</div>

* [スクレイピングエンドポイント](/ja/api-reference/endpoint/scrape)