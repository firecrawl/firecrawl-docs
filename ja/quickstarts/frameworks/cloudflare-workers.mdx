---
title: "Cloudflare Workers クイックスタート"
description: "Cloudflare Workers から Firecrawl を呼び出します。"
---

<div id="overview">
  ## 概要
</div>

Worker 内で `fetch` を使って Firecrawl の HTTP API を呼び出します。

<div id="prerequisites">
  ## 前提条件
</div>

* FirecrawlのAPIキー
* Cloudflare Workersプロジェクト
* Wrangler

<div id="install">
  ## インストール
</div>

追加パッケージは不要です。Workers には標準で fetch が含まれています。

<div id="configure-environment">
  ## 環境を設定する
</div>

Wrangler を使って、APIキーを Worker のシークレットとして設定します。

```bash
wrangler secret put FIRECRAWL_API_KEY
```

<div id="add-a-server-route">
  ## サーバールートを追加
</div>

src/index.ts

```ts
export interface Env {
  FIRECRAWL_API_KEY: string;
}

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    if (request.method !== 'POST') {
      return new Response('Method not allowed', { status: 405 });
    }

    const { url } = await request.json();
    const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${env.FIRECRAWL_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ url, formats: ['markdown'] }),
    });

    return new Response(await response.text(), {
      headers: { 'Content-Type': 'application/json' },
    });
  },
};
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```js
const response = await fetch('https://your-worker.example.workers.dev', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事項
</div>

* Worker はエッジで実行されるため、Firecrawl へのリクエストは必ず Worker 経由でプロキシしてください。

<div id="next-steps">
  ## 次のステップ
</div>

* [Scrape エンドポイント](/ja/api-reference/endpoint/scrape)