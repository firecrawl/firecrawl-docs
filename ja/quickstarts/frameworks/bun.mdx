---
title: "Bun クイックスタート"
description: "fetch で Bun サーバーから Firecrawl を呼び出します。"
---

<div id="overview">
  ## 概要
</div>

Bun 標準搭載の fetch を使って、Firecrawl の HTTP API を呼び出します。

<div id="prerequisites">
  ## 前提条件
</div>

* FirecrawlのAPIキー
* Bun 1.x

<div id="install">
  ## インストール
</div>

追加のパッケージは不要です。Bun には `fetch` が最初から含まれています。

<div id="configure-environment">
  ## 環境を設定
</div>

Bun を実行する前に、環境変数に APIキー を設定します。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## サーバー側のルートを追加する
</div>

server.ts

```ts
const server = Bun.serve({
  port: 3000,
  async fetch(req) {
    const url = new URL(req.url);
    if (url.pathname != '/api/firecrawl' || req.method != 'POST') {
      return new Response('Not found', { status: 404 });
    }

    const { url: target } = await req.json();
    const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${process.env.FIRECRAWL_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ url: target, formats: ['markdown'] }),
    });

    return new Response(await response.text(), {
      headers: { 'Content-Type': 'application/json' },
    });
  },
});

console.log(`Listening on http://localhost:${server.port}`);
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```js
const response = await fetch('http://localhost:3000/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事項
</div>

* API キーはサーバー側で保持し、リクエストは Bun ハンドラー経由でプロキシしてください。

<div id="next-steps">
  ## 次のステップ
</div>

* [スクレイピングエンドポイント](/ja/api-reference/endpoint/scrape)