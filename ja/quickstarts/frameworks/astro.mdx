---
title: "Astro クイックスタート"
description: "Astro の API ルートから Firecrawl を呼び出す方法。"
---

<div id="overview">
  ## 概要
</div>

Astro の API ルートを使って、サーバー側で Firecrawl を呼び出します。

<div id="prerequisites">
  ## 前提条件
</div>

* Firecrawl のAPIキー
* Astro のプロジェクト
* Node.js 18+

<div id="install">
  ## インストール
</div>

追加のパッケージは不要です。API ルートでは `fetch` を使用してください。

<div id="configure-environment">
  ## 環境を設定する
</div>

`.env` にAPIキーを追加し、サーバーサイドのコードで読み込みます。

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## サーバー側のルートを追加する
</div>

src/pages/api/firecrawl.ts

```ts
import type { APIRoute } from 'astro';

export const POST: APIRoute = async ({ request }) => {
  const { url } = await request.json();
  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${import.meta.env.FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  return new Response(await response.text(), {
    headers: { 'Content-Type': 'application/json' },
  });
};
```

<div id="call-it-from-your-app">
  ## アプリから呼び出す
</div>

```js
const response = await fetch('/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

<div id="notes">
  ## 注意事項
</div>

* Firecrawl の呼び出しにはサーバー側のルートを使用し、APIキーを使ったクライアント側からのリクエストは避けてください。

<div id="next-steps">
  ## 次のステップ
</div>

* [Scrape エンドポイント](/ja/api-reference/endpoint/scrape)