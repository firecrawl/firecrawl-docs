---
title: "Express"
description: "ExpressでFirecrawlを使って、ウェブスクレイピングと検索APIを構築します。"
og:title: "Express クイックスタート | Firecrawl"
og:description: "ExpressでFirecrawlを使って、ウェブスクレイピングと検索APIを構築します。"
---

## 前提条件 {#prerequisites}

* Node.js 18以上
* Firecrawl APIキー — [無料で取得](https://www.firecrawl.dev/app/api-keys)

## セットアップ {#setup}

```bash
npm install express firecrawl
```

`.env` にAPIキーを追加します：

```bash
FIRECRAWL_API_KEY=fc-YOUR-API-KEY
```

## Webを検索する {#search-the-web}

```javascript
import express from "express";
import { Firecrawl } from "firecrawl";

const app = express();
app.use(express.json());

const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

app.post("/search", async (req, res) => {
  try {
    const { query } = req.body;
    const results = await firecrawl.search(query, { limit: 5 });
    res.json(results);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

app.listen(3000, () => console.log("Server running on port 3000"));
```

## ページのスクレイピング {#scrape-a-page}

```javascript
app.post("/scrape", async (req, res) => {
  try {
    const { url } = req.body;
    const result = await firecrawl.scrape(url);
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
```

## ページをInteractする {#interact-with-a-page}

Interact を使うと、実行中のブラウザセッションを制御できます。ボタンをクリックしたり、フォームに入力したり、動的コンテンツを抽出したりできます。

```javascript
app.post("/interact", async (req, res) => {
  try {
    const { url } = req.body;

    const result = await firecrawl.scrape(url, { formats: ['markdown'] });
    const scrapeId = result.metadata?.scrapeId;

    await firecrawl.interact(scrapeId, { prompt: 'Search for iPhone 16 Pro Max' });
    const response = await firecrawl.interact(scrapeId, { prompt: 'Click on the first result and tell me the price' });

    await firecrawl.stopInteraction(scrapeId);

    res.json({ output: response.output });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
```

## 試してみる {#test-it}

```bash
curl -X POST http://localhost:3000/search \
  -H "Content-Type: application/json" \
  -d '{"query": "firecrawl web scraping"}'
```

## 次のステップ {#next-steps}

<CardGroup cols={2}>
  <Card title="スクレイピングのドキュメント" icon="file-lines" href="/ja/features/scrape">
    フォーマット、アクション、プロキシなど、スクレイピングのオプションを網羅
  </Card>

  <Card title="検索ドキュメント" icon="magnifying-glass" href="/ja/features/search">
    ウェブを検索してページ全体のコンテンツを取得
  </Card>

  <Card title="Interact ドキュメント" icon="hand-pointer" href="/ja/features/interact">
    クリック、フォーム入力、動的コンテンツの抽出
  </Card>

  <Card title="Node SDK リファレンス" icon="node" href="/ja/sdks/node">
    クロール、map、バッチスクレイプなどを含む完全な SDK リファレンス
  </Card>
</CardGroup>