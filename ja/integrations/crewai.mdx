---
title: "CrewAI"
description: "CrewAI での Firecrawl の使い方"
og:title: "CrewAI | Firecrawl"
og:description: "CrewAI での Firecrawl の使い方"
---

<div id="using-firecrawl-with-crewai">
  ## CrewAI で Firecrawl を使う
</div>

Firecrawl は、[AI エージェントをオーケストレーションするフレームワークである CrewAI](https://www.crewai.com/) と統合されています。このページでは、フレームワークに追加された Firecrawl のツールをすべて紹介します。

<div id="installing-firecrawl-tools-inside-of-crewai">
  ### CrewAI 内での Firecrawl Tools のインストール
</div>

* [firecrawl.dev のダッシュボード](https://firecrawl.dev)から API キーを取得し、環境変数（`FIRECRAWL_API_KEY`）に設定します。
* `crewai[tools]` パッケージとあわせて [Firecrawl SDK](https://github.com/firecrawl/firecrawl) をインストールします。

```
pip install firecrawl-py 'crewai[tools]'
```

<div id="tools">
  ## ツール
</div>

<div id="firecrawlcrawlwebsitetool">
  ### FirecrawlCrawlWebsiteTool
</div>

<div id="example">
  #### 例
</div>

エージェントがウェブサイトを読み込めるようにするには、次のように FirecrawlScrapeFromWebsiteTool を利用します：

```python
from crewai_tools import FirecrawlCrawlWebsiteTool

tool = FirecrawlCrawlWebsiteTool(url='firecrawl.dev')
```

<div id="arguments">
  #### 引数
</div>

- `api_key`: 任意。Firecrawl の API キーを指定します。デフォルトは `FIRECRAWL_API_KEY` 環境変数です。
- `url`: クロールを開始するベースとなる URL。
- `page_options`: 任意。
  - `onlyMainContent`: 任意。ヘッダー、ナビゲーション、フッターなどを除いたページの主要コンテンツのみを返します。
  - `includeHtml`: 任意。ページの生の HTML コンテンツを含めます。レスポンスに html キーが含まれます。
- `crawler_options`: 任意。クロール動作を制御するためのオプション。
  - `includes`: 任意。クロールに含める URL パターン。
  - `exclude`: 任意。クロールから除外する URL パターン。
  - `generateImgAltText`: 任意。LLM を使用して画像の代替テキストを生成します（有料プランが必要）。
  - `returnOnlyUrls`: 任意。true の場合、クロールステータスでは URL のみをリストで返します。注意：レスポンスはドキュメントのリストではなく、data 内の URL のリストになります。
  - `maxDepth`: 任意。クロールの最大深さ。深さ 1 はベース URL、深さ 2 はベース URL とその直下の子を含み、以降同様です。
  - `mode`: 任意。使用するクロールモード。Fast モードはサイトマップのないウェブサイトで 4 倍速くクロールしますが、精度が劣る可能性があり、JavaScript のレンダリングが重いウェブサイトでは使用しないでください。
  - `limit`: 任意。クロールするページの最大数。
  - `timeout`: 任意。クロール処理のタイムアウト（ミリ秒）。

<div id="firecrawlscrapewebsitetool">
  ### FirecrawlScrapeWebsiteツール
</div>

<div id="example">
  #### 例
</div>

エージェントがウェブサイトを読み込めるようにするには、次のように FirecrawlScrapeWebsiteTool を使用します。

```python
from crewai_tools import FirecrawlScrapeWebsiteTool

tool = FirecrawlScrapeWebsiteTool(url='firecrawl.dev')
```

<div id="arguments">
  #### 引数
</div>

- `api_key`: 任意。Firecrawl の API キーを指定します。指定しない場合は `FIRECRAWL_API_KEY` 環境変数が使用されます。
- `url`: スクレイプ対象の URL。
- `page_options`: 任意。
  - `onlyMainContent`: 任意。ヘッダー、ナビゲーション、フッターなどを除き、ページの主要コンテンツのみを返します。
  - `includeHtml`: 任意。ページの生の HTML コンテンツを含めます。レスポンスに html キーが含まれます。
- `extractor_options`: 任意。ページコンテンツから構造化情報を LLM ベースで抽出するためのオプション
  - `mode`: 使用する抽出モード。現在は 'llm-extraction' に対応
  - `extractionPrompt`: 任意。ページから抽出する情報を記述するプロンプト
  - `extractionSchema`: 任意。抽出するデータのスキーマ
- `timeout`: 任意。リクエストのタイムアウト（ミリ秒）

<div id="firecrawlsearchtool">
  ### Firecrawl検索ツール
</div>

<div id="example">
  #### 例
</div>

エージェントがウェブサイトを読み込めるようにするには、次のように FirecrawlSearchTool を使用します：

```python
from crewai_tools import FirecrawlSearchTool

tool = FirecrawlSearchTool(query='Firecrawl とは？')
```

<div id="arguments">
  #### 引数
</div>

- `api_key`: 任意。Firecrawl の API キーを指定します。デフォルトは `FIRECRAWL_API_KEY` 環境変数です。
- `query`: 検索に使用するクエリ文字列。
- `page_options`: 任意。結果のフォーマットに関するオプション。
  - `onlyMainContent`: 任意。ヘッダー、ナビゲーション、フッターなどを除いたページの主要コンテンツのみを返します。
  - `includeHtml`: 任意。ページの生の HTML を含めます。レスポンスに html キーが含まれます。
  - `fetchPageContent`: 任意。ページの全コンテンツを取得します。
- `search_options`: 任意。クロール動作を制御するためのオプション。
  - `limit`: 任意。クロールするページの最大数。