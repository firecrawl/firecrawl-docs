---
title: "Curlの信頼できる唯一の参照元"
description: "search、スクレイピング、Interact などの主要エンドポイントを使用するエージェント向けの、Firecrawl の公式 curl リファレンスです。"
---

エージェント向けの、Firecrawl の curl に関する信頼できる唯一の参照元です。SDK のソースと v2 OpenAPI 仕様から生成されています。

<div id="authenticate">
  ## 認証
</div>

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="when-to-use-what">
  ## 用途別の使い分け
</div>

* `search`: クエリから始めて、見つける必要がある場合に使用します。
* `scrape`: すでに URL があり、ページのコンテンツを取得したい場合に使用します。
* `interact`: クリックやフォーム入力、またはスクレイピング後のブラウザアクションが必要なページで使用します。

<div id="search">
  ## Search
</div>

<div id="why-use-it">
  ### 使用する理由
</div>

クエリから関連ページを見つけるには検索を使用し、その後スクレイピングまたはInteractするURLを選択します。`site:` を使って結果をサイトに絞り込むことができます。たとえば `site:docs.firecrawl.dev crawl webhooks` です。

<div id="preferred-sdk-method">
  ### 推奨SDKメソッド
</div>

`POST /search`

<div id="simple-example">
  ### シンプルな例
</div>

```bash
curl -X POST "https://api.firecrawl.dev/v2/search" \
  -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "site:docs.firecrawl.dev webhook retries"
  }'
```

<div id="complex-example">
  ### 複雑な例
</div>

```bash
curl -X POST "https://api.firecrawl.dev/v2/search" \
  -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "site:docs.firecrawl.dev crawl webhooks",
    "sources": ["web", "news"],
    "categories": ["research"],
    "limit": 10,
    "tbs": "qdr:m",
    "location": "San Francisco,California,United States",
    "country": "US",
    "ignoreInvalidURLs": true,
    "timeout": 60000,
    "enterprise": ["anon"],
    "scrapeOptions": {
      "formats": [
        "markdown",
        "links",
        {"type": "json", "prompt": "Extract title and key endpoints."}
      ],
      "onlyMainContent": true,
      "includeTags": ["main", "article"],
      "excludeTags": ["nav", "footer"],
      "waitFor": 1000
    }
  }'
```

<div id="parameters">
  ### パラメータ
</div>

* `query`
  * 型: string
  * 使用する場合: 検索クエリが必要な場合。
  * 注記: 結果を特定のドメインに限定するには、`site:example.com` を使用します。

* `sources`
  * 型: ソース名または型付きソースオブジェクトの配列
  * 使用する場合: 検索対象のソースを制御したい場合。
  * 確認済みの値:
    * `"web"`: Web インデックスの結果
    * `"news"`: ニュース結果
    * `"images"`: 画像結果
    * `{ "type": "web" | "news" | "images" }`: 型付きソースオブジェクト形式

* `categories`
  * 型: カテゴリ名または型付きカテゴリオブジェクトの配列
  * 使用する場合: カテゴリで結果を絞り込みたい場合。
  * 確認済みの値:
    * `"github"`: GitHub に特化した結果
    * `"research"`: 研究・学術関連の結果
    * `"pdf"`: PDF に特化した結果
    * `{ "type": "github" | "research" | "pdf" }`: 型付きカテゴリオブジェクト形式

* `limit`
  * 型: number
  * 使用する場合: 結果数の上限を設定したい場合。

* `tbs`
  * 型: string
  * 使用する場合: 時間ベースの絞り込みが必要な場合 (例: `qdr:d`, `qdr:w`, `sbd:1,qdr:m`) 。

* `location`
  * 型: string
  * 使用する場合: 地域に応じた結果を取得したい場合。

* `country`
  * 型: string
  * 使用する場合: ISO 3166-1 alpha-2 コードで対象国を指定したい場合 (例: `"US"`) 。

* `ignoreInvalidURLs`
  * 型: boolean
  * 使用する場合: 他のエンドポイントでスクレイピングできない URL を除外したい場合。

* `timeout`
  * 型: number
  * 使用する場合: ミリ秒単位のリクエストタイムアウトが必要な場合。

* `enterprise`
  * 型: 文字列の配列
  * 使用する場合: エンタープライズ検索の制御が必要な場合。
  * 確認済みの値:
    * `"zdr"`: エンドツーエンドのゼロデータ保持
    * `"anon"`: 匿名化されたゼロデータ保持

* `scrapeOptions`
  * 型: object
  * 使用する場合: 各検索結果をスクレイピングしたい場合 (フィールドについては Scrape パラメータを参照) 。

<div id="scrape">
  ## スクレイピング
</div>

### 使う理由

URL がすでにあり、1 つ以上のフォーマットで構造化されたコンテンツを取得したい場合は、スクレイピングを使用します。

<div id="preferred-sdk-method">
  ### 推奨SDKメソッド
</div>

`POST /scrape`

<div id="simple-example">
  ### シンプルな例
</div>

```bash
curl -X POST "https://api.firecrawl.dev/v2/scrape" \
  -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://docs.firecrawl.dev",
    "formats": ["markdown"]
  }'
```

### 高度な例

```bash
curl -X POST "https://api.firecrawl.dev/v2/scrape" \
  -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com/pricing",
    "formats": [
      "markdown",
      "links",
      {"type": "json", "prompt": "Extract plan names and prices."},
      {"type": "screenshot", "fullPage": true, "quality": 80, "viewport": {"width": 1280, "height": 720}},
      {"type": "changeTracking", "modes": ["git-diff"], "tag": "pricing"}
    ],
    "headers": {"User-Agent": "FirecrawlDocsBot/1.0"},
    "onlyMainContent": true,
    "waitFor": 1000,
    "parsers": [{"type": "pdf", "mode": "auto", "maxPages": 5}],
    "actions": [
      {"type": "click", "selector": "#accept"},
      {"type": "wait", "milliseconds": 750},
      {"type": "scrape"}
    ],
    "location": {"country": "US", "languages": ["en-US"]},
    "removeBase64Images": true,
    "blockAds": true,
    "proxy": "auto",
    "maxAge": 86400000,
    "minAge": 1,
    "storeInCache": true,
    "profile": {"name": "docs-session", "saveChanges": true},
    "zeroDataRetention": false
  }'
```

<div id="parameters">
  ### パラメータ
</div>

* `url`
  * Type: string
  * 使用する場面: 特定のページをスクレイピングしたい場合。

* `formats`
  * Type: フォーマット文字列またはフォーマットオブジェクトの配列
  * 使用する場面: 複数の出力フォーマットが必要な場合。
  * 確認済みのフォーマット文字列:
    * `"markdown"`: Markdownコンテンツ
    * `"html"`: クリーンなHTML
    * `"rawHtml"`: 生のHTML
    * `"links"`: ページリンク
    * `"images"`: 画像URL
    * `"screenshot"`: スクリーンショット出力
    * `"summary"`: 要約出力
    * `"changeTracking"`: changeTracking出力
    * `"json"`: JSON抽出
    * `"branding"`: ブランドプロファイル出力
    * `"audio"`: 音声抽出
  * フォーマットオブジェクトのフィールド:
    * `type`: 上記のフォーマット文字列のいずれか
    * `prompt`, `schema`: `type: "json"` のJSON抽出オプション
    * `modes`, `schema`, `prompt`, `tag`: `type: "changeTracking"` のchangeTrackingオプション
    * `fullPage`, `quality`, `viewport`: `type: "screenshot"` のスクリーンショットオプション

* `headers`
  * Type: object
  * 使用する場面: カスタムのリクエストヘッダーが必要な場合。

* `includeTags`
  * Type: stringの配列
  * 使用する場面: 特定のHTMLタグだけを含めたい場合。

* `excludeTags`
  * Type: stringの配列
  * 使用する場面: 特定のHTMLタグを除外したい場合。

* `onlyMainContent`
  * Type: boolean
  * 使用する場面: nav、footer、その他の定型要素を取り除きたい場合。

* `timeout`
  * Type: number
  * 使用する場面: ミリ秒単位のタイムアウトが必要な場合。

* `waitFor`
  * Type: number
  * 使用する場面: ページのレンダリング完了を待つ必要がある場合 (ミリ秒) 。

* `mobile`
  * Type: boolean
  * 使用する場面: モバイル用のビューポートを使いたい場合。

* `parsers`
  * Type: パーサー文字列またはオブジェクトの配列
  * 使用する場面: ファイルのパースを制御したい場合。
  * 確認済みの値:
    * `"pdf"`
    * `{ "type": "pdf", "mode": "fast" | "auto" | "ocr", "maxPages": number }`

* `actions`
  * Type: アクションオブジェクトの配列
  * 使用する場面: 軽量なスクレイピング前アクションが必要な場合。
  * 確認済みのアクションタイプ:
    * `wait`: `milliseconds` または `selector` が必須
    * `screenshot`: `fullPage`, `quality`, `viewport` は任意
    * `click`: `selector` が必須、`all` は任意
    * `write`: `text` が必須 (先にクリックして入力欄へフォーカス) 
    * `press`: `key` が必須
    * `scroll`: `direction` (`up` または `down`) が必須、`selector` は任意
    * `scrape`: 追加フィールドなし
    * `executeJavascript`: `script` が必須
    * `pdf`: `format` (A0, A1, A2, A3, A4, A5, A6, Letter, Legal, Tabloid, Ledger) 、`landscape`、`scale` は任意

* `location`
  * Type: `country` と `languages` を含むobject
  * 使用する場面: 地理情報または言語を考慮したスクレイピングが必要な場合。

* `skipTlsVerification`
  * Type: boolean
  * 使用する場面: TLSの確認をスキップする必要がある場合。

* `removeBase64Images`
  * Type: boolean
  * 使用する場面: Markdown出力からbase64画像を除外したい場合。

* `blockAds`
  * Type: boolean
  * 使用する場面: 広告やCookieポップアップをブロックしたい場合。

* `proxy`
  * Type: string
  * 使用する場面: プロキシを制御したい場合。
  * 確認済みの値: `"basic"`, `"enhanced"`, `"auto"`

* `maxAge`
  * Type: number
  * 使用する場面: 指定した最大経過時間までのキャッシュデータを使いたい場合 (ミリ秒) 。

* `minAge`
  * Type: number
  * 使用する場面: 少なくともこの時間が経過したキャッシュデータのみを使いたい場合 (ミリ秒) 。

* `storeInCache`
  * Type: boolean
  * 使用する場面: Firecrawlに結果をキャッシュさせたい場合。

* `profile`
  * Type: `name` と任意の `saveChanges` を含むobject
  * 使用する場面: スクレイピングや操作のあいだで共有される永続的なブラウザプロファイルを使いたい場合。

* `zeroDataRetention`
  * Type: boolean
  * 使用する場面: このスクレイピングでゼロデータ保持を有効にしたい場合。

<div id="interact">
  ## Interact
</div>

### なぜ使うのか

ページで、スクレイピングの開始後にブラウザのアクションやコードの実行が必要な場合は、Interact を使用します。

<div id="preferred-sdk-method">
  ### 推奨SDKメソッド
</div>

`POST /scrape/{jobId}/interact`

### 簡単な例

```bash
curl -X POST "https://api.firecrawl.dev/v2/scrape/<jobId>/interact" \
  -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "code": "console.log(await page.title());",
    "language": "node",
    "timeout": 60
  }'
```

<div id="complex-example">
  ### 複雑な例
</div>

```bash
curl -X POST "https://api.firecrawl.dev/v2/scrape/<jobId>/interact" \
  -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Click the pricing tab and summarize the plans.",
    "language": "node",
    "timeout": 60
  }'
```

<div id="parameters">
  ### パラメータ
</div>

* `jobId`
  * 型: string
  * 使用する場合: スクレイピングジョブ ID がある場合。

* `code`
  * 型: string
  * 使用する場合: ブラウザセッション内でコードを実行したい場合。

* `prompt`
  * 型: string
  * 使用する場合: ブラウザエージェントに自然言語の指示を実行させたい場合。

* `language`
  * 型: string
  * 使用する場合: 特定のランタイムが必要な場合。
  * 確認済みの値: `"python"`, `"node"`, `"bash"`

* `timeout`
  * 型: number
  * 使用する場合: 秒単位で実行タイムアウトを指定する必要がある場合。

<div id="notes">
  ## 注意事項
</div>

* 複数ステップのブラウザワークフローには `interact` を使用し、スクレイピング前の小さな手順には `actions` が適しています。

<div id="source-of-truth">
  ## 信頼できる唯一の参照元
</div>

* `firecrawl-docs/api-reference/v2-openapi.json`
* `firecrawl/apps/js-sdk/firecrawl/src/v2/types.ts`
* `firecrawl/apps/python-sdk/firecrawl/v2/types.py`
* `firecrawl/apps/rust-sdk/src/v2/scrape.rs`