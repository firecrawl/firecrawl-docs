---
title: "Rails Quickstart"
description: "Call Firecrawl from a Rails controller."
---

## Overview
Use Ruby's Net::HTTP to call the Firecrawl API from a controller.

## Prerequisites
- A Firecrawl API key
- Rails app
- Ruby 3.0+

## Install
No extra packages required; use Ruby's standard library.

## Configure environment
Set the API key in your Rails environment.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

## Add a server route
app/controllers/firecrawl_controller.rb

```rb
require 'net/http'
require 'uri'
require 'json'

class FirecrawlController < ApplicationController
  protect_from_forgery with: :null_session

  def create
    uri = URI('https://api.firecrawl.dev/v2/scrape')
    request = Net::HTTP::Post.new(uri)
    request['Authorization'] = "Bearer #{ENV.fetch('FIRECRAWL_API_KEY')}"
    request['Content-Type'] = 'application/json'
    request.body = { url: params[:url], formats: ['markdown'] }.to_json

    response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) do |http|
      http.request(request)
    end

    render json: JSON.parse(response.body)
  end
end
```

## Call it from your app
```rb
require 'net/http'
require 'json'

uri = URI('http://localhost:3000/api/firecrawl')
response = Net::HTTP.post(uri, { url: 'https://example.com' }.to_json, 'Content-Type' => 'application/json')

puts JSON.parse(response.body)
```

## Notes
- Add `post '/api/firecrawl', to: 'firecrawl#create'` to `config/routes.rb`.

## Next steps
- [Scrape endpoint](/api-reference/endpoint/scrape)
