---
title: "AWS Lambda Quickstart"
description: "Call Firecrawl from an AWS Lambda function."
---

## Overview
Use a Lambda handler to proxy Firecrawl requests securely.

## Prerequisites
- A Firecrawl API key
- AWS Lambda (Node.js 18 runtime)

## Install
No extra packages required; use fetch in Node.js 18.

## Configure environment
Set `FIRECRAWL_API_KEY` in your Lambda environment variables.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

## Add a server route
index.mjs

```js
export const handler = async (event) => {
  const { url } = JSON.parse(event.body || '{}');

  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${process.env.FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  const data = await response.json();
  return {
    statusCode: 200,
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  };
};
```

## Call it from your app
```js
const response = await fetch('https://your-api-id.execute-api.region.amazonaws.com/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

## Notes
- Configure API Gateway (or Lambda Function URLs) to expose your Lambda endpoint.

## Next steps
- [Scrape endpoint](/api-reference/endpoint/scrape)
