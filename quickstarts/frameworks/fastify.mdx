---
title: "Fastify Quickstart"
description: "Call Firecrawl from a Fastify route."
---

## Overview
Use the Firecrawl Node SDK inside a Fastify server route.

## Prerequisites
- A Firecrawl API key
- Fastify app
- Node.js 18+

## Install
```bash
npm install @mendable/firecrawl-js
```

## Configure environment
Add the API key to your server environment.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

## Add a server route
server.js

```js
import Fastify from 'fastify';
import Firecrawl from '@mendable/firecrawl-js';

const app = Fastify();
const client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

app.post('/api/firecrawl', async (request) => {
  const { url } = request.body as { url: string };
  const doc = await client.scrape(url, { formats: ['markdown'] });
  return { markdown: doc.markdown };
});

app.listen({ port: 3000 });
```

## Call it from your app
```js
const response = await fetch('http://localhost:3000/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.markdown);
```

## Notes
- Keep Firecrawl requests server-side to protect your API key.

## Next steps
- [Scrape endpoint](/api-reference/endpoint/scrape)
- [Node SDK](/sdks/node)
