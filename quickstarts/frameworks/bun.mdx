---
title: "Bun Quickstart"
description: "Call Firecrawl from a Bun server using fetch."
---

## Overview
Use Bun's built-in fetch to call the Firecrawl HTTP API.

## Prerequisites
- A Firecrawl API key
- Bun 1.x

## Install
No extra packages required; Bun includes fetch out of the box.

## Configure environment
Set the API key in your environment before running Bun.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

## Add a server route
server.ts

```ts
const server = Bun.serve({
  port: 3000,
  async fetch(req) {
    const url = new URL(req.url);
    if (url.pathname != '/api/firecrawl' || req.method != 'POST') {
      return new Response('Not found', { status: 404 });
    }

    const { url: target } = await req.json();
    const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${process.env.FIRECRAWL_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ url: target, formats: ['markdown'] }),
    });

    return new Response(await response.text(), {
      headers: { 'Content-Type': 'application/json' },
    });
  },
});

console.log(`Listening on http://localhost:${server.port}`);
```

## Call it from your app
```js
const response = await fetch('http://localhost:3000/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

## Notes
- Keep the API key on the server and proxy requests through your Bun handler.

## Next steps
- [Scrape endpoint](/api-reference/endpoint/scrape)
