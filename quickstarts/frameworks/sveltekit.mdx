---
title: "SvelteKit Quickstart"
description: "Call Firecrawl from a SvelteKit server endpoint."
---

## Overview
Use a SvelteKit `+server.ts` endpoint to call Firecrawl on the server.

## Prerequisites
- A Firecrawl API key
- SvelteKit app
- Node.js 18+

## Install
No extra packages required; use fetch from the server endpoint.

## Configure environment
Store the API key in `.env` and load it from `$env/static/private`.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

## Add a server route
src/routes/api/firecrawl/+server.ts

```ts
import { json } from '@sveltejs/kit';
import { FIRECRAWL_API_KEY } from '$env/static/private';

export async function POST({ request }) {
  const { url } = await request.json();
  const response = await fetch('https://api.firecrawl.dev/v2/scrape', {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${FIRECRAWL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ url, formats: ['markdown'] }),
  });

  const data = await response.json();
  return json(data);
}
```

## Call it from your app
```ts
const response = await fetch('/api/firecrawl', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ url: 'https://example.com' }),
});

const data = await response.json();
console.log(data.data?.markdown ?? data);
```

## Notes
- Keep the API key in private env vars; avoid exposing it via `$env/static/public`.

## Next steps
- [Scrape endpoint](/api-reference/endpoint/scrape)
