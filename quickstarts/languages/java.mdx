---
title: "Java Quickstart"
description: "Scrape your first URL with the Firecrawl Java SDK."
---

## Overview
Use the official Firecrawl Java SDK to call the v2 API from Java.

## Prerequisites
- A Firecrawl API key
- Java 11+
- Maven or Gradle

## Install
```gradle
implementation("com.firecrawl:firecrawl-java:1.1.1")
```

## Configure your API key
Set your API key as an environment variable before running your app.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

## Make your first request
```java
import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import java.util.List;

FirecrawlClient client = FirecrawlClient.fromEnv();

Document doc = client.scrape(
    "https://example.com",
    ScrapeOptions.builder().formats(List.of("markdown")).build()
);

System.out.println(doc.getMarkdown());
```

## Complete example
```java
import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        FirecrawlClient client = FirecrawlClient.fromEnv();
        Document doc = client.scrape(
            "https://example.com",
            ScrapeOptions.builder().formats(List.of("markdown", "html")).build()
        );

                    if (doc.getMetadata() != null) {
                        System.out.println(doc.getMetadata().get("title"));
                    }
        System.out.println(doc.getMarkdown());
    }
}
```

## Next steps
- [Scrape endpoint](/api-reference/endpoint/scrape)
- [Java SDK](/sdks/java)
