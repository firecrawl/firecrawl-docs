---
title: ".NET Quickstart"
description: "Call the Firecrawl API directly from .NET."
---

## Overview
Use HttpClient to send a Firecrawl scrape request from .NET.

## Prerequisites
- A Firecrawl API key
- .NET 6+

## Install
No extra packages required; this example uses HttpClient.

## Configure your API key
Set your API key as an environment variable for the process.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

## Make your first request
```csharp
using System.Net.Http.Headers;
using System.Net.Http.Json;

var apiKey = Environment.GetEnvironmentVariable("FIRECRAWL_API_KEY");
var http = new HttpClient();
http.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", apiKey);

var payload = new { url = "https://example.com", formats = new[] { "markdown" } };
var response = await http.PostAsJsonAsync("https://api.firecrawl.dev/v2/scrape", payload);
var body = await response.Content.ReadAsStringAsync();

Console.WriteLine(body);
```

## Complete example
```csharp
using System.Net.Http.Headers;
using System.Net.Http.Json;

var apiKey = Environment.GetEnvironmentVariable("FIRECRAWL_API_KEY");
var http = new HttpClient();
http.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", apiKey);

var payload = new { url = "https://example.com", formats = new[] { "markdown", "html" } };
var response = await http.PostAsJsonAsync("https://api.firecrawl.dev/v2/scrape", payload);
response.EnsureSuccessStatusCode();

var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

## Next steps
- [Scrape endpoint](/api-reference/endpoint/scrape)
