---
title: 'Rust'
description: 'El SDK de Rust de Firecrawl es una biblioteca que te ayuda a extraer y rastrear sitios web con facilidad, y a generar datos en un formato listo para usar con modelos de lenguaje (LLMs).'
icon: 'rust'
og:title: "Rust SDK | Firecrawl"
og:description: "El SDK de Rust de Firecrawl es una biblioteca que te ayuda a extraer y rastrear sitios web con facilidad, y a generar datos en un formato listo para usar con modelos de lenguaje (LLMs)."
---

import InstallationRust from '/snippets/es/v1/installation/rust.mdx'
import ScrapeAndCrawlRustExample from '/snippets/es/v1/scrape-and-crawl/rust.mdx'
import ScrapeRustShort from '/snippets/es/v1/scrape/short/rust.mdx'
import CrawlRustShort from '/snippets/es/v1/crawl/short/rust.mdx'
import CrawlSitemapOnlyRust from '/snippets/es/v2/crawl/sitemap-only/rust.mdx'
import CrawlAsyncRustShort from '/snippets/es/v1/crawl-async/short/rust.mdx'
import MapRustShort from '/snippets/es/v1/map/short/rust.mdx'
import LLMExtractRust from '/snippets/es/v1/llm-extract/base/rust.mdx'

<Warning>
  Este SDK actualmente utiliza la versión **v1** de la API de Firecrawl, que no es la más reciente (v2 está disponible). Algunas funciones y mejoras podrían estar disponibles únicamente en v2.
</Warning>


<div id="installation">
  ## Instalación
</div>

Para instalar el SDK de Rust de Firecrawl, agrega lo siguiente a tu `Cargo.toml`:

<InstallationRust />

<div id="usage">
  ## Uso
</div>

Primero, obtén una clave de API en [firecrawl.dev](https://firecrawl.dev). Luego, inicializa `FirecrawlApp`. A partir de ahí, puedes acceder a funciones como `FirecrawlApp::scrape_url`, que te permiten usar nuestra API.

Aquí tienes un ejemplo de cómo usar el SDK en Rust:

<ScrapeAndCrawlRustExample />

<div id="scraping-a-url">
  ### Extracción de una URL
</div>

Para extraer una única URL, utiliza el método `scrape_url`. Recibe la URL como parámetro y devuelve los datos extraídos como un `Document`.

<ScrapeRustShort />

<div id="scraping-with-extract">
  ### Extracción con Extract
</div>

Con Extract, puedes extraer fácilmente datos estructurados de cualquier URL. Debes definir tu esquema en formato JSON Schema, usando la macro `serde_json::json!`.

<LLMExtractRust />

<div id="crawling-a-website">
  ### Rastreo de un sitio web
</div>

Para rastrear un sitio web, utiliza el método `crawl_url`. Este método esperará a que el rastreo termine, lo que puede llevar bastante tiempo según la URL inicial y las opciones que elijas.

<CrawlRustShort />

<div id="sitemap-only-crawl-v2-api">
  ### Rastreo solo mediante sitemap (API v2)
</div>

El SDK de Rust actualmente se dirige a v1. Para usar `sitemap: "only"`, llama directamente al endpoint de v2:

<CrawlSitemapOnlyRust />

<div id="crawling-asynchronously">
  #### Rastreo asíncrono
</div>

Para rastrear sin esperar el resultado, usa el método `crawl_url_async`. Acepta los mismos parámetros, pero devuelve una estructura `CrawlAsyncResponse` que contiene el ID del rastreo. Puedes usar ese ID con el método `check_crawl_status` para consultar el estado en cualquier momento. Ten en cuenta que los rastreos completados se borran después de 24 horas.

<CrawlAsyncRustShort />

<div id="map-a-url">
  ### Mapear una URL
</div>

Enumera todos los enlaces asociados a partir de una URL inicial.

<MapRustShort />

<div id="error-handling">
  ## Manejo de errores
</div>

El SDK gestiona los errores devueltos por la API de Firecrawl y por nuestras dependencias, y los unifica en el enum `FirecrawlError`, que implementa `Error`, `Debug` y `Display`. Todos nuestros métodos devuelven un `Result<T, FirecrawlError>`.