---
title: "Bloques de código"
description: "Muestra código en línea y bloques de código"
icon: 'code'
---

<div id="basic">
  ## Conceptos básicos
</div>

<div id="inline-code">
  ### Código en línea
</div>

Para indicar que una `palabra` o `frase` es código, enciérrala entre comillas invertidas (`).

```
Para indicar una `palabra` o `frase` como código, enciérrala entre acentos graves (`).
```

<div id="code-block">
  ### Bloque de código
</div>

Usa [bloques de código delimitados](https://www.markdownguide.org/extended-syntax/#fenced-code-blocks) encerrando el código entre tres acentos graves. Para obtener resaltado de sintaxis, indica a continuación de los acentos el lenguaje de programación del fragmento. Opcionalmente, también puedes escribir el nombre del fragmento después del lenguaje.

```java HelloWorld.java
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("¡Hola, Mundo!");
    }
}
```

````md
```java HelloWorld.java
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("¡Hola, mundo!");
    }
}
```
````
