---
title: "Introducción"
description: "Referencia de la API de Firecrawl (v2)"
---

La API de Firecrawl proporciona acceso programático a datos de la web. Todos los endpoints comparten la misma URL base, el mismo esquema de autenticación y el formato de respuesta descritos en esta página.

<div id="features">
  ## Funcionalidades
</div>

<CardGroup cols={3}>
  <Card title="Scrape" icon="markdown" href="/es/api-reference/endpoint/scrape" color="FF713C">
    Extrae contenido de cualquier página web en formato markdown o JSON.
  </Card>

  <Card title="Parse" icon="markdown" href="/es/api-reference/endpoint/parse" color="FF713C">
    Sube archivos y procésalos en markdown u otros formatos.
  </Card>

  <Card title="Crawl" icon="spider" href="/es/api-reference/endpoint/crawl-post" color="FF713C">
    Rastrea sitios web completos y obtén el contenido de todas sus páginas.
  </Card>

  <Card title="Map" icon="map" href="/es/api-reference/endpoint/map" color="FF713C">
    Obtén un listado completo de URLs de cualquier sitio web de forma rápida y confiable.
  </Card>

  <Card title="Search" icon="magnifying-glass" href="/es/api-reference/endpoint/search" color="FF713C">
    Busca en la web y obtén el contenido completo de la página en cualquier formato.
  </Card>
</CardGroup>

<div id="agentic-features">
  ## Capacidades de agente
</div>

<CardGroup cols={3}>
  <Card title="Agent" icon="robot" href="/es/api-reference/endpoint/agent" color="FF713C">
    Recopilación autónoma de datos web impulsada por IA.
  </Card>

  <Card title="Browser" icon="browser" href="/es/api-reference/endpoint/browser-create" color="FF713C">
    Crea y controla sesiones de navegador para tareas web interactivas.
  </Card>
</CardGroup>

<div id="base-url">
  ## URL base
</div>

Todas las solicitudes utilizan la siguiente URL base:

```bash
https://api.firecrawl.dev
```

<div id="authentication">
  ## Autenticación
</div>

Todas las solicitudes deben incluir un encabezado `Authorization` con tu clave de API:

```bash
Authorization: Bearer fc-YOUR-API-KEY
```

Incluye este encabezado en todas las llamadas a la API. Puedes encontrar tu clave de API en el [dashboard de Firecrawl](https://www.firecrawl.dev/app/api-keys).

<CodeGroup>
  ```bash cURL
  curl -X POST "https://api.firecrawl.dev/v2/scrape" \
    -H "Authorization: Bearer fc-YOUR-API-KEY" \
    -H "Content-Type: application/json" \
    -d '{"url": "https://example.com"}'
  ```

  ```python Python
  from firecrawl import Firecrawl

  firecrawl = Firecrawl(api_key="fc-YOUR-API-KEY")

  result = firecrawl.scrape("https://example.com")
  ```

  ```js Node
  import Firecrawl from '@mendable/firecrawl-js';

  const firecrawl = new Firecrawl({ apiKey: "fc-YOUR-API-KEY" });

  const result = await firecrawl.scrape('https://example.com');
  ```
</CodeGroup>

<div id="response-codes">
  ## Códigos de respuesta
</div>

Firecrawl utiliza códigos de estado HTTP convencionales para indicar el resultado de tus solicitudes. Los códigos en el rango `2xx` indican éxito, los códigos `4xx` indican errores del cliente y los códigos `5xx` indican errores del servidor.

Consulta [Errores](/es/api-reference/errors) para obtener la referencia completa, incluida la cadena `error` que se devuelve para cada tipo de fallo, la guía de reintentos y un fragmento de backoff listo para copiar y pegar.

<div id="rate-limit">
  ## Límite de solicitudes
</div>

La API de Firecrawl aplica límites de solicitudes (rate limits) en todos los endpoints para garantizar la estabilidad del servicio. Estos límites se basan en el número de solicitudes dentro de una ventana de tiempo determinada.

Si superas el límite de solicitudes, la API devuelve un código de estado `429`. Espera un momento y vuelve a intentar la solicitud después de un breve retraso.