---
title: "Fastify"
description: "Usa Firecrawl con Fastify para crear APIs de scraping web y búsqueda de alto rendimiento."
og:title: "Inicio rápido de Fastify | Firecrawl"
og:description: "Usa Firecrawl con Fastify para crear APIs de scraping web y búsqueda de alto rendimiento."
---

## Prerrequisitos {#prerequisites}

* Node.js 18+
* Una clave de API de Firecrawl — [consigue una gratis](https://www.firecrawl.dev/app/api-keys)

## Configuración {#setup}

```bash
npm install fastify firecrawl
```

Añade tu clave de API a `.env`:

```bash
FIRECRAWL_API_KEY=fc-YOUR-API-KEY
```

## Buscar en la web {#search-the-web}

```javascript
import Fastify from "fastify";
import { Firecrawl } from "firecrawl";

const fastify = Fastify({ logger: true });
const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

fastify.post("/search", async (request) => {
  const { query } = request.body;
  return firecrawl.search(query, { limit: 5 });
});

fastify.listen({ port: 3000 });
```

## Hacer scraping de una página {#scrape-a-page}

```javascript
fastify.post("/scrape", async (request) => {
  const { url } = request.body;
  return firecrawl.scrape(url);
});
```

## Interacción con una página {#interact-with-a-page}

Usa interacción para controlar una sesión activa del navegador: haz clic en botones, completa formularios y extrae contenido dinámico.

```javascript
fastify.post("/interact", async (request) => {
  const { url } = request.body;

  const result = await firecrawl.scrape(url, { formats: ['markdown'] });
  const scrapeId = result.metadata?.scrapeId;

  await firecrawl.interact(scrapeId, { prompt: 'Search for iPhone 16 Pro Max' });
  const response = await firecrawl.interact(scrapeId, { prompt: 'Click on the first result and tell me the price' });

  await firecrawl.stopInteraction(scrapeId);

  return { output: response.output };
});
```

## Como plugin de Fastify {#as-a-fastify-plugin}

Encapsula el cliente en un plugin para reutilizarlo en distintas rutas:

```javascript
import fp from "fastify-plugin";
import { Firecrawl } from "firecrawl";

export default fp(async function firecrawlPlugin(fastify) {
  const client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });
  fastify.decorate("firecrawl", client);
});
```

Registra el plugin y luego usa `fastify.firecrawl` en cualquier ruta:

```javascript
fastify.register(firecrawlPlugin);

fastify.post("/search", async function (request) {
  const { query } = request.body;
  return this.firecrawl.search(query, { limit: 5 });
});
```

## Pruébalo {#test-it}

```bash
curl -X POST http://localhost:3000/search \
  -H "Content-Type: application/json" \
  -d '{"query": "firecrawl web scraping"}'
```

## Siguientes pasos {#next-steps}

<CardGroup cols={2}>
  <Card title="Documentación de scraping" icon="file-lines" href="/es/features/scrape">
    Todas las opciones de scraping, incluidos los formatos, las acciones y los proxies
  </Card>

  <Card title="Documentación de búsqueda" icon="magnifying-glass" href="/es/features/search">
    Busca en la web y obtén el contenido completo de la página
  </Card>

  <Card title="Documentación de interacción" icon="hand-pointer" href="/es/features/interact">
    Haz clic, completa formularios y extrae contenido dinámico
  </Card>

  <Card title="Referencia del SDK de Node" icon="node" href="/es/sdks/node">
    Referencia completa del SDK con crawl, mapeo, extracción por lotes y más
  </Card>
</CardGroup>