---
title: "Next.js"
description: "Usa Firecrawl con Next.js para hacer scraping, buscar e interactuar con datos web en tu aplicación React."
og:title: "Inicio rápido de Next.js | Firecrawl"
og:description: "Usa Firecrawl con Next.js para hacer scraping, buscar e interactuar con datos web en tu aplicación React."
---

<div id="prerequisites">
  ## Requisitos previos {#prerequisites}
</div>

* Un proyecto de Next.js 14+ (App Router)
* Una clave de la API de Firecrawl — [obtén una gratis](https://www.firecrawl.dev/app/api-keys)

<div id="install-the-sdk">
  ## Instala el SDK {#install-the-sdk}
</div>

```bash
npm install firecrawl
```

<div id="set-your-api-key">
  ## Configura tu clave de API {#set-your-api-key}
</div>

Añade tu clave de API a `.env.local`:

```bash
FIRECRAWL_API_KEY=fc-YOUR-API-KEY
```

<div id="search-the-web">
  ## Buscar en la web {#search-the-web}
</div>

El SDK solo debe ejecutarse en el servidor, ya que requiere tu clave de API.

<div id="route-handler">
  ### Route Handler {#route-handler}
</div>

Crea `app/api/search/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

export async function POST(request: Request) {
  const { query } = await request.json();
  const results = await firecrawl.search(query, { limit: 5 });

  return NextResponse.json(results);
}
```

<div id="server-action">
  ### Acción del servidor {#server-action}
</div>

Crea `app/actions.ts` para usarlo desde componentes de cliente:

```typescript
"use server";

import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

export async function searchWeb(query: string) {
  const results = await firecrawl.search(query, { limit: 5 });
  return (results.web || []).map((r) => ({ title: r.title, url: r.url }));
}
```

<div id="scrape-a-page">
  ## Hacer scraping de una página {#scrape-a-page}
</div>

<div id="route-handler-2">
  ### Route Handler {#route-handler-2}
</div>

Crea `app/api/scrape/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

export async function POST(request: Request) {
  const { url } = await request.json();
  const result = await firecrawl.scrape(url);

  return NextResponse.json(result);
}
```

<div id="server-component">
  ### Componente de servidor {#server-component}
</div>

Obtén datos directamente en un componente de servidor en `app/page.tsx`:

```tsx
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

export default async function Page() {
  const result = await firecrawl.scrape("https://example.com");

  return (
    <article>
      <h1>Scraped Content</h1>
      <pre>{result.markdown}</pre>
    </article>
  );
}
```

<div id="interact-with-a-page">
  ## Interactúa con una página {#interact-with-a-page}
</div>

Usa interact para controlar una sesión activa del navegador: haz clic en botones, completa formularios y extrae contenido dinámico.

<div id="route-handler-3">
  ### Route Handler {#route-handler-3}
</div>

Crea `app/api/interact/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { Firecrawl } from "firecrawl";

const firecrawl = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

export async function POST(request: Request) {
  const { url, prompts } = await request.json();

  const result = await firecrawl.scrape(url, { formats: ['markdown'] });
  const scrapeId = result.metadata?.scrapeId;

  await firecrawl.interact(scrapeId, { prompt: 'Search for iPhone 16 Pro Max' });
  const response = await firecrawl.interact(scrapeId, { prompt: 'Click on the first result and tell me the price' });

  await firecrawl.stopInteraction(scrapeId);

  return NextResponse.json({ output: response.output });
}
```

<div id="next-steps">
  ## Siguientes pasos {#next-steps}
</div>

<CardGroup cols={2}>
  <Card title="Documentación de scraping" icon="file-lines" href="/es/features/scrape">
    Todas las opciones de scraping, incluidos los formatos, las acciones y los proxies
  </Card>

  <Card title="Documentación de búsqueda" icon="magnifying-glass" href="/es/features/search">
    Busca en la web y obtén el contenido completo de la página
  </Card>

  <Card title="Documentación de Interact" icon="hand-pointer" href="/es/features/interact">
    Haz clic, completa formularios y extrae contenido dinámico
  </Card>

  <Card title="Referencia del SDK de Node" icon="node" href="/es/sdks/node">
    Referencia completa del SDK con crawl, mapeo, extracción por lotes y más
  </Card>
</CardGroup>