---
title: "Inicio rápido con Java"
description: "Realiza scraping de tu primera URL con el SDK de Java de Firecrawl."
---

<div id="overview">
  ## Descripción general
</div>

Usa el SDK oficial de Java de Firecrawl para llamar a la API v2 desde Java.

<div id="prerequisites">
  ## Requisitos previos
</div>

* Una clave de API de Firecrawl
* Java 11+
* Maven o Gradle

<div id="install">
  ## Instalación
</div>

```gradle
implementation("com.firecrawl:firecrawl-java:1.1.1")
```

<div id="configure-your-api-key">
  ## Configura tu clave de API
</div>

Define tu clave de API como una variable de entorno antes de ejecutar tu aplicación.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Realiza tu primera solicitud
</div>

```java
import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import java.util.List;

FirecrawlClient client = FirecrawlClient.fromEnv();

Document doc = client.scrape(
    "https://example.com",
    ScrapeOptions.builder().formats(List.of("markdown")).build()
);

System.out.println(doc.getMarkdown());
```

<div id="complete-example">
  ## Ejemplo completo
</div>

```java
import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        FirecrawlClient client = FirecrawlClient.fromEnv();
        Document doc = client.scrape(
            "https://example.com",
            ScrapeOptions.builder().formats(List.of("markdown", "html")).build()
        );

                    if (doc.getMetadata() != null) {
                        System.out.println(doc.getMetadata().get("title"));
                    }
        System.out.println(doc.getMarkdown());
    }
}
```

<div id="next-steps">
  ## Siguientes pasos
</div>

* [Endpoint de scraping](/es/api-reference/endpoint/scrape)
* [SDK de Java](/es/sdks/java)