---
title: "Inicio rápido con .NET"
description: "Llama directamente a la API de Firecrawl desde .NET."
---

<div id="overview">
  ## Descripción general
</div>

Usa HttpClient para enviar una solicitud de scraping a Firecrawl desde .NET.

<div id="prerequisites">
  ## Requisitos previos
</div>

* Una clave de API de Firecrawl
* .NET 6+

<div id="install">
  ## Instalación
</div>

No se requieren paquetes adicionales; este ejemplo usa HttpClient.

<div id="configure-your-api-key">
  ## Configura tu clave de API
</div>

Configura tu clave de API como una variable de entorno para el proceso.

```bash
export FIRECRAWL_API_KEY="fc-your-api-key"
```

<div id="make-your-first-request">
  ## Realiza tu primera solicitud
</div>

```csharp
using System.Net.Http.Headers;
using System.Net.Http.Json;

var apiKey = Environment.GetEnvironmentVariable("FIRECRAWL_API_KEY");
var http = new HttpClient();
http.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", apiKey);

var payload = new { url = "https://example.com", formats = new[] { "markdown" } };
var response = await http.PostAsJsonAsync("https://api.firecrawl.dev/v2/scrape", payload);
var body = await response.Content.ReadAsStringAsync();

Console.WriteLine(body);
```

<div id="complete-example">
  ## Ejemplo completo
</div>

```csharp
using System.Net.Http.Headers;
using System.Net.Http.Json;

var apiKey = Environment.GetEnvironmentVariable("FIRECRAWL_API_KEY");
var http = new HttpClient();
http.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", apiKey);

var payload = new { url = "https://example.com", formats = new[] { "markdown", "html" } };
var response = await http.PostAsJsonAsync("https://api.firecrawl.dev/v2/scrape", payload);
response.EnsureSuccessStatusCode();

var body = await response.Content.ReadAsStringAsync();
Console.WriteLine(body);
```

<div id="next-steps">
  ## Próximos pasos
</div>

* [Endpoint de scraping](/es/api-reference/endpoint/scrape)