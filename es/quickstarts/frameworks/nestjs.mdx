---
title: "Inicio rápido de NestJS"
description: "Llama a Firecrawl desde un controlador de NestJS."
---

<div id="overview">
  ## Descripción general
</div>

Integra Firecrawl en un servicio de NestJS y expónlo a través de un controlador.

<div id="prerequisites">
  ## Requisitos previos
</div>

* Una clave de la API de Firecrawl
* Una aplicación de NestJS
* Node.js 18+

<div id="install">
  ## Instalación
</div>

```bash
npm install @mendable/firecrawl-js
```

<div id="configure-environment">
  ## Configurar el entorno
</div>

Expón la clave de API como una variable de entorno exclusiva del servidor.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Agrega una ruta del servidor
</div>

src/firecrawl/firecrawl.service.ts

```ts
import { Injectable } from '@nestjs/common';
import Firecrawl from '@mendable/firecrawl-js';

@Injectable()
export class FirecrawlService {
  private readonly client = new Firecrawl({ apiKey: process.env.FIRECRAWL_API_KEY });

  async scrape(url: string) {
    return this.client.scrape(url, { formats: ['markdown'] });
  }
}
```

<div id="call-it-from-your-app">
  ## Llámalo desde tu aplicación
</div>

```ts
// src/firecrawl/firecrawl.controller.ts
import { Body, Controller, Post } from '@nestjs/common';
import { FirecrawlService } from './firecrawl.service';

@Controller('api/firecrawl')
export class FirecrawlController {
  constructor(private readonly firecrawl: FirecrawlService) {}

  @Post()
  async create(@Body('url') url: string) {
    const doc = await this.firecrawl.scrape(url);
    return { markdown: doc.markdown };
  }
}
```

<div id="notes">
  ## Notas
</div>

* Registra `FirecrawlService` y `FirecrawlController` en un módulo.
* Mantén la clave de la API fuera del código del cliente.

<div id="next-steps">
  ## Próximos pasos
</div>

* [Endpoint de scraping](/es/api-reference/endpoint/scrape)
* [SDK de Node](/es/sdks/node)