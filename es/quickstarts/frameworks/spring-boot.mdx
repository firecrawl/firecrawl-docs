---
title: "Inicio rápido de Spring Boot"
description: "Cómo llamar a Firecrawl desde un controlador de Spring Boot."
---

<div id="overview">
  ## Descripción general
</div>

Usa el SDK de Java de Firecrawl en un servicio y un controlador de Spring Boot.

<div id="prerequisites">
  ## Requisitos previos
</div>

* Una clave de API de Firecrawl
* Una aplicación Spring Boot
* Java 11+

<div id="install">
  ## Instalación
</div>

```gradle
implementation("com.firecrawl:firecrawl-java:1.1.1")
```

<div id="configure-environment">
  ## Configurar el entorno
</div>

Establece la clave de API en el entorno o en la configuración de la aplicación.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Añade una ruta del servidor
</div>

src/main/java/com/example/firecrawl/FirecrawlService.java

```java
package com.example.firecrawl;

import com.firecrawl.client.FirecrawlClient;
import com.firecrawl.models.Document;
import com.firecrawl.models.ScrapeOptions;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class FirecrawlService {
    private final FirecrawlClient client = FirecrawlClient.fromEnv();

    public Document scrape(String url) {
        return client.scrape(url, ScrapeOptions.builder().formats(List.of("markdown")).build());
    }
}
```

<div id="call-it-from-your-app">
  ## Haz la llamada desde tu aplicación
</div>

```java
// src/main/java/com/example/firecrawl/FirecrawlController.java
package com.example.firecrawl;

import com.firecrawl.models.Document;
import org.springframework.web.bind.annotation.*;

import java.util.Map;

@RestController
@RequestMapping("/api/firecrawl")
public class FirecrawlController {
    private final FirecrawlService service;

    public FirecrawlController(FirecrawlService service) {
        this.service = service;
    }

    @PostMapping
    public Map<String, String> scrape(@RequestBody Map<String, String> body) {
        Document doc = service.scrape(body.get("url"));
        return Map.of("markdown", doc.getMarkdown());
    }
}
```

<div id="notes">
  ## Notas
</div>

* Registra los paquetes de service/controller en el escaneo de componentes.

<div id="next-steps">
  ## Siguientes pasos
</div>

* [Endpoint de scraping](/es/api-reference/endpoint/scrape)
* [Java SDK](/es/sdks/java)