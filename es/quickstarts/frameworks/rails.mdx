---
title: "Inicio rápido de Rails"
description: "Llama a Firecrawl desde un controlador de Rails."
---

<div id="overview">
  ## Resumen
</div>

Usa Net::HTTP de Ruby para llamar a la API de Firecrawl desde un controlador.

<div id="prerequisites">
  ## Requisitos previos
</div>

* Una clave de la API de Firecrawl
* Una aplicación Rails
* Ruby 3.0+

<div id="install">
  ## Instalación
</div>

No se requieren paquetes adicionales; usa la biblioteca estándar de Ruby.

<div id="configure-environment">
  ## Configura el entorno
</div>

Define la clave de API en tu entorno de Rails.

```bash
FIRECRAWL_API_KEY=fc-your-api-key
```

<div id="add-a-server-route">
  ## Añade una ruta del lado del servidor
</div>

app/controllers/firecrawl&#95;controller.rb

```rb
require 'net/http'
require 'uri'
require 'json'

class FirecrawlController < ApplicationController
  protect_from_forgery with: :null_session

  def create
    uri = URI('https://api.firecrawl.dev/v2/scrape')
    request = Net::HTTP::Post.new(uri)
    request['Authorization'] = "Bearer #{ENV.fetch('FIRECRAWL_API_KEY')}"
    request['Content-Type'] = 'application/json'
    request.body = { url: params[:url], formats: ['markdown'] }.to_json

    response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: true) do |http|
      http.request(request)
    end

    render json: JSON.parse(response.body)
  end
end
```

<div id="call-it-from-your-app">
  ## Invócalo desde tu aplicación
</div>

```rb
require 'net/http'
require 'json'

uri = URI('http://localhost:3000/api/firecrawl')
response = Net::HTTP.post(uri, { url: 'https://example.com' }.to_json, 'Content-Type' => 'application/json')

puts JSON.parse(response.body)
```

<div id="notes">
  ## Notas
</div>

* Agrega `post '/api/firecrawl', to: 'firecrawl#create'` a `config/routes.rb`.

<div id="next-steps">
  ## Siguientes pasos
</div>

* [Endpoint de scraping](/es/api-reference/endpoint/scrape)