---
title: "Índice para desarrolladores (beta)"
description: "Busca issues de GitHub, pull requests fusionadas, archivos README de repositorios y sitios de documentación seleccionados. Beta privada."
---

<Note>
  **Beta privada.** El índice para desarrolladores está en fase beta. Esta página no está listada (no tiene enlaces desde la navegación, la página de la función Search ni la página de herramientas MCP), para poder compartirla como referencia mientras la función está en beta. Los tipos de resultados, las señales de clasificación y los filtros aún pueden cambiar.
</Note>

<Warning>
  **El acceso se concede por organización.** Cada solicitud a continuación requiere una clave de API perteneciente a una organización habilitada para la beta para desarrolladores. Las solicitudes sin ese acceso reciben `403` con `Developer search is in beta and is not enabled for this team.` en el endpoint para desarrolladores y, en `/search`, la categoría `developer` se elimina de la respuesta sin ningún error. Contáctanos para habilitar tu organización.
</Warning>

Firecrawl Developer es un índice diseñado para agentes de programación. Incluye issues de GitHub, pull requests fusionadas, archivos README de repositorios y sitios de documentación seleccionados, para que un agente pueda responder preguntas sobre el comportamiento del código, una biblioteca o un framework, un contrato de API, un mensaje de error o un error conocido a partir de fuentes primarias, en lugar de una página web general.

* Encuentra el issue o pull request en el que se informó y corrigió un error
* Lee los pasajes de un README o una página de documentación que respondan una pregunta concreta
* Rastrea un contrato de API hasta el pull request que lo modificó
* Recupera la discusión relacionada con un mensaje de error

<div id="two-ways-in">
  ## Dos formas de acceder
</div>

| Superficie                                                | Úsalo cuando                                                                                                                   |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| La categoría `developer` en [`/search`](/es/features/search) | Quieres resultados para desarrolladores junto con resultados web generales en una sola llamada                                 |
| El endpoint de búsqueda para desarrolladores              | Quieres únicamente resultados para desarrolladores ordenados por relevancia, con pasajes coincidentes y los filtros del índice |

En esta página, `categories` es el parámetro de la solicitud y siempre acepta un array. Un único valor, como `developer`, es una categoría.

<div id="developer-category-on-search">
  ## Categoría `developer` en `/search`
</div>

Pasa `developer` en el array `categories` de `/search`. La API devuelve resultados para desarrolladores en un grupo `developer` junto a `web`, y ambos SDK exponen ese grupo como `.developer`.

`/search` acepta solicitudes keyless, pero la categoría `developer` no: una solicitud sin una clave de API habilitada sigue devolviendo `200` con resultados `web` normales y sin ningún grupo `developer`. Envía una clave de una organización habilitada para obtener resultados para desarrolladores.

<CodeGroup>
  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v2/search \
    -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
    -H "Content-Type: application/json" \
    -d '{
      "query": "how do I configure retries",
      "categories": ["developer"],
      "limit": 10
    }'
  ```

  ```python Python
  from firecrawl import Firecrawl

  firecrawl = Firecrawl(
      api_key="fc-YOUR-API-KEY",
  )

  result = firecrawl.search(
      "how do I configure retries",
      categories=["developer"],
      limit=10,
  )
  for item in result.developer or []:
      print(item.url, item.title)
  ```

  ```js Node
  import { Firecrawl } from "firecrawl";

  const firecrawl = new Firecrawl({
    apiKey: "fc-YOUR-API-KEY",
  });

  const result = await firecrawl.search("how do I configure retries", {
    categories: ["developer"],
    limit: 10,
  });
  for (const item of result.developer ?? []) {
    console.log(item.url, item.title);
  }
  ```
</CodeGroup>

Los resultados para desarrolladores de este grupo incluyen `url`, `title`, `description` y `position`, con la misma estructura que un resultado web, además de `category: "developer"`. Los resultados web de la misma respuesta no tienen `category`, por lo que debes usar ese campo si fusionas ambos grupos.

```json
{
  "success": true,
  "data": {
    "web": [
      {
        "url": "https://example.com/blog/retry-strategies",
        "title": "Retry strategies for HTTP clients",
        "description": "An overview of backoff and retry patterns"
      }
    ],
    "developer": [
      {
        "url": "https://github.com/firecrawl/firecrawl/issues/1234",
        "title": "Retries are not applied to 429 responses",
        "description": "Reported behavior and the fix that landed",
        "position": 1,
        "category": "developer"
      }
    ]
  }
}
```

<Note>
  **Usuarios del SDK:** los resultados de desarrolladores se incluyen en su propio grupo, no en `.web`. Acceda a ellos con `result.developer`.
</Note>

<div id="developer-search-endpoint">
  ## Endpoint de búsqueda para desarrolladores
</div>

| Tarea                                  | Endpoint                            |
| -------------------------------------- | ----------------------------------- |
| Buscar en el índice de desarrolladores | `GET` o `POST /v2/developer/search` |
| Misma búsqueda, ruta de alias          | `GET` o `POST /v2/search/developer` |

La ruta canónica es `/v2/developer/search`. `/v2/search/developer` es un alias que ofrece la misma búsqueda. Ambas requieren una clave de API de una organización habilitada para la beta para desarrolladores; de lo contrario, ambas devuelven `403`. Una búsqueda para desarrolladores se factura como una búsqueda.

<CodeGroup>
  ```bash cURL
  curl -s "https://api.firecrawl.dev/v2/developer/search?query=how%20do%20I%20configure%20retries&k=10" \
    -H "Authorization: Bearer $FIRECRAWL_API_KEY"
  ```

  ```bash cURL (POST)
  curl -X POST https://api.firecrawl.dev/v2/developer/search \
    -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
    -H "Content-Type: application/json" \
    -d '{
      "query": "how do I configure retries",
      "k": 10,
      "types": ["issue", "pull_request"]
    }'
  ```
</CodeGroup>

<Note>
  Los SDK de Python y Node acceden al índice de desarrolladores mediante la categoría `developer` mostrada arriba. Aún no exponen un método específico para este endpoint, así que, por ahora, llámalo mediante HTTP.
</Note>

<div id="filters">
  ### Filtros
</div>

Todos los filtros son opcionales.

| Filtro      | Significado                                                                                                                     |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `k`         | Número de resultados ordenados que se devolverán. El valor predeterminado es 20; el máximo es 100                               |
| `types`     | Tipos de resultados en los que buscar: `doc`, `issue`, `pull_request` o `readme`. De forma predeterminada, se buscan los cuatro |
| `repos`     | Slugs de repositorios a los que limitar la búsqueda, como `firecrawl/firecrawl`                                                 |
| `sources`   | IDs de fuentes de documentación a los que limitar la búsqueda; máximo 20. Consulta la sección siguiente                         |
| `passages`  | Pasajes coincidentes que se devolverán por resultado. El valor predeterminado es 1; el máximo es 5                              |
| `language`  | Lenguaje del repositorio                                                                                                        |
| `topic`     | Tema del repositorio                                                                                                            |
| `license`   | Licencia del repositorio                                                                                                        |
| `min_stars` | Límite inferior de estrellas del repositorio                                                                                    |
| `max_stars` | Límite superior de estrellas del repositorio                                                                                    |
| `archived`  | Incluir o excluir repositorios archivados                                                                                       |
| `fork`      | Incluir o excluir bifurcaciones                                                                                                 |

Los filtros repetibles aceptan cualquiera de los dos formatos en `GET`: un parámetro de consulta repetido, como `types=issue&types=pull_request`, o un único valor separado por comas, como `types=issue,pull_request`. En `POST`, pasa un array en el cuerpo JSON.

<div id="how-repos-and-sources-scope-a-search">
  ### Cómo `repos` y `sources` acotan una búsqueda
</div>

El índice se divide en dos partes, y estos filtros acotan cada una de forma independiente:

* `repos` acota la parte de GitHub, es decir, los tipos `issue`, `pull_request` y `readme`
* `sources` acota la parte de documentación, es decir, el tipo `doc`
* Al pasar ambos, se combinan las dos partes en lugar de intersectarse, por lo que obtienes resultados coincidentes de cualquiera de las dos

Como cada filtro solo se aplica a una parte, se rechaza cualquier filtro que no pueda coincidir con ninguno de los tipos solicitados, en lugar de no devolver resultados de forma silenciosa:

* `repos` sin ningún tipo de GitHub en `types` devuelve `400` con `repos cannot match any requested type; add github types or drop repos`
* `sources` sin `doc` en `types` devuelve `400` con `sources cannot match any requested type; add doc or drop sources`

<div id="which-values-sources-accepts">
  ### Valores que acepta `sources`
</div>

`sources` no es un enum fijo. Acepta IDs de fuentes de documentación: cada uno debe ser una cadena no vacía de hasta 512 bytes, con un máximo de 20 por solicitud. Los IDs disponibles dependen de los sitios de documentación incluidos actualmente en el índice, por lo que el conjunto cambia a medida que se añaden sitios.

Para confirmar que un ID se resuelve, envíalo y consulta el array `sources` que añade la respuesta. Solo aparece si enviaste `sources` e indica cada ID exactamente como lo solicitaste, junto con si está indexado:

```json
{
  "success": true,
  "results": [],
  "sources": [
    { "source": "some-docs-site", "indexed": true },
    { "source": "not-enrolled", "indexed": false }
  ]
}
```

`indexed: true` significa que la fuente tiene una generación publicada, por lo que puede aparecer documentación procedente de ella. `indexed: false` significa que nada de ese id puede coincidir, lo que distingue una fuente no registrada de una query que simplemente no encontró resultados.

`repos` se devuelve de la misma forma, como un array `repos` que indica `indexed` y un desglose por tipo de `issue`, `pullRequest` y `readme`.

<div id="response">
  ### Respuesta
</div>

```json
{
  "success": true,
  "results": [
    {
      "id": "issue:firecrawl/firecrawl#1234",
      "type": "issue",
      "url": "https://github.com/firecrawl/firecrawl/issues/1234",
      "title": "Retries are not applied to 429 responses",
      "passages": [
        {
          "text": "The client treats 429 as a terminal status, so the backoff never runs."
        }
      ]
    }
  ],
  "coverage": {
    "doc": "ok",
    "issue": "ok",
    "pull_request": "ok",
    "readme": "ok"
  },
  "reranked": true
}
```

* `id` es un identificador de resultado estable, como `issue:owner/repo#123`
* `type` puede ser `doc`, `issue`, `pull_request` o `readme`
* `title` es opcional y suele estar ausente en los resultados `doc`, donde la página de origen no tiene un título utilizable. Usa `url` como alternativa en lugar de asumir que el campo está presente
* `passages` contiene los pasajes coincidentes en Markdown para conservar las tablas y los bloques de código
* `coverage` indica el estado del índice para cada tipo de resultado: `ok`, `degraded`, `unavailable` o `skipped`
* `reranked` indica si la lista ordenada pasó por la fase de reordenamiento

Comprueba `coverage` si falta un tipo de resultado que esperabas. Un estado `degraded` o `unavailable` significa que la ausencia se debe al índice y no a la query.

<div id="use-it-from-mcp">
  ## Úsalo desde MCP
</div>

<Warning>
  **Aún no está disponible en el MCP Server.** Las herramientas siguientes aún no están disponibles: el servidor alojado no expone `firecrawl_developer_search`, y el enum `categories` de `firecrawl_search` solo acepta `github`, `research` y `pdf`, por lo que se rechaza pasar `developer`. Hasta que llegue la compatibilidad con MCP, accede al índice mediante HTTP a través del endpoint anterior. Esta sección describe la estructura prevista.
</Warning>

Una vez lanzado, el [MCP Server](/es/mcp-server) expondrá ambas funciones. Ninguna realiza escrituras.

| Herramienta                                        | Propósito                                                                         |
| -------------------------------------------------- | --------------------------------------------------------------------------------- |
| `firecrawl_search` con `categories: ["developer"]` | Resultados para desarrolladores junto con resultados web                          |
| `firecrawl_developer_search`                       | Resultados para desarrolladores ordenados por relevancia con pasajes coincidentes |

```json
{
  "name": "firecrawl_developer_search",
  "arguments": {
    "query": "how do I configure retries",
    "k": 10
  }
}
```

`firecrawl_developer_search` solo acepta `query` y un `k` opcional, y devuelve cada resultado como un bloque Markdown con su ID, tipo, URL, título y pasajes coincidentes. Usa el endpoint directamente cuando necesites filtrar por repositorio y tipo de resultado.